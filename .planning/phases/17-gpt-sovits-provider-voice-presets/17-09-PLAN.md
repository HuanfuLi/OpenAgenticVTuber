---
phase: 17-gpt-sovits-provider-voice-presets
plan: 09
type: execute
gap_closure: true
wave: 9
depends_on: [17-08]
files_modified:
  - apps/renderer/src/ws/store.ts
  - apps/renderer/tests/ChatStreaming.test.tsx
  - .planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
autonomous: true
requirements: [TTS-01, TTS-04, TTS-06]
must_haves:
  truths:
    - "One incoming renderer audio payload produces one visible assistant sentence, even after Vite HMR/module re-evaluation."
    - "The first audio payload after a Thinking bubble replaces Thinking once and does not append a duplicate copy."
    - "GPT-SoVITS failed-audio payloads keep sentence text visible once, mark the failed sentence, and do not silently switch to Piper."
    - "UAT Test 6 remains marked as issue/pending retest until the user confirms live GPT-SoVITS chat behavior."
  artifacts:
    - path: "apps/renderer/src/ws/store.ts"
      provides: "Idempotent, HMR-safe renderer WS store dispatcher registration with retained unsubscribe handles."
      contains: "subscribe(" 
    - path: "apps/renderer/tests/ChatStreaming.test.tsx"
      provides: "Focused regression coverage for duplicate/HMR store registration and single audio dispatch behavior."
      contains: "one audio payload"
    - path: ".planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md"
      provides: "Gap-fix evidence without falsely marking Test 6 passed before user retest."
      contains: "Plan 17-09"
  key_links:
    - from: "apps/renderer/src/ws/store.ts"
      to: "apps/renderer/src/ws/client.ts"
      via: "retained unsubscribe functions returned by subscribe and subscribeSidecarReconnect"
      pattern: "unsubscribe|dispose|ensure"
    - from: "apps/renderer/src/ws/store.ts"
      to: "apps/renderer/src/screens/Chat/useStreamingMessages.ts"
      via: "audio dispatcher calls appendAssistantSentence exactly once per WS audio payload"
      pattern: "appendAssistantSentence"
    - from: "apps/renderer/tests/ChatStreaming.test.tsx"
      to: "apps/renderer/src/ws/store.ts"
      via: "test imports/calls dispatcher registration twice to simulate HMR and dispatches one audio payload"
      pattern: "ensure.*Store|HMR|duplicate"
---

<objective>
Close Phase 17 UAT Test 6's duplicated visible text gap by making renderer WebSocket store dispatcher registration idempotent and Vite HMR-safe.

Purpose: In dev UAT, module re-evaluation can leave multiple `store.ts` listeners subscribed to the singleton `ws/client.ts`; one GPT-SoVITS audio payload is then processed twice and duplicates sentence text in Chat.

Output: A minimal renderer-only fix with failing-first regression coverage and UAT evidence that does not claim a manual pass before retest.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-08-SUMMARY.md
@apps/renderer/src/ws/store.ts
@apps/renderer/src/ws/client.ts
@apps/renderer/src/screens/Chat/useStreamingMessages.ts
@apps/renderer/tests/ChatStreaming.test.tsx

<interfaces>
From `apps/renderer/src/ws/client.ts`:
```ts
export function subscribe(cb: Listener): () => void
export function subscribeSidecarReconnect(cb: OpenListener): () => void
export function subscribeState(cb: (open: boolean) => void): () => void
```

From `apps/renderer/src/screens/Chat/useStreamingMessages.ts`:
```ts
export function setThinking(on: boolean): void
export function appendAssistantSentence(text: string, sentenceId: number, audioFailure?: Omit<SentenceAudioFailure, 'sentenceId'>): void
export function resetStreaming(): void
export function _internalState(): StreamingState
```

Current diagnosed gap: `apps/renderer/src/ws/store.ts` calls `subscribe(...)` and `subscribeSidecarReconnect(...)` at module top level and ignores the returned unsubscribe functions. In a Vite dev UAT session, HMR/module re-evaluation can leave multiple store dispatch listeners subscribed to the singleton client. Each incoming `audio` payload is processed twice, so `appendAssistantSentence` replaces Thinking on the first call and appends the same sentence on the second.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Phase 17 users can use GPT-SoVITS voice output without losing Piper fallback safety. | Task 2 preserves the existing GPT-SoVITS chat audio/failure path while preventing duplicate renderer dispatch. |
| REQ TTS-01 | User can select GPT-SoVITS as active TTS provider and use it for chat output. | Task 1/2 verify active chat audio text is rendered once after dispatcher re-registration. |
| REQ TTS-04 | Reference-audio/preset path remains valid after activation. | Out of direct code scope; plan only fixes renderer dispatch after activation, without changing preset/reference persistence. |
| REQ TTS-06 | GPT-SoVITS failures stay visible and do not silently switch provider mid-turn. | Task 1 includes failed-audio visible-text coverage; Task 2 preserves existing failure banner/no-Piper-switch semantics. |
| UAT | Test 6 duplicated chunks failure. | Tasks 1-3 directly cover Test 6 root cause, implementation, and UAT evidence. |
| CONTEXT | Diagnosed root cause: top-level store dispatcher registration ignores unsubscribe handles; sidecar/provider double-send ruled out. | Task 2 fixes exactly `store.ts` registration; no sidecar/provider changes. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing duplicate-dispatch regression tests</name>
  <files>apps/renderer/tests/ChatStreaming.test.tsx</files>
  <behavior>
    - Test 1: importing/calling the store dispatcher initializer twice to simulate Vite HMR/module re-evaluation leaves only one active `subscribe` listener for audio dispatch.
    - Test 2: after `conversation-chain-start`, one normal `audio` payload with display text `Hello!` replaces the Thinking bubble exactly once; state text is `Hello!`, not `Hello!Hello!`.
    - Test 3: after duplicate registration simulation, one failed GPT-SoVITS audio payload appends visible sentence text once, records one `audioFailures` entry, and does not call `playAudioPayload`.
  </behavior>
  <action>
    Extend the existing `vi.mock('@/ws/client')` in `ChatStreaming.test.tsx` so it can retain all subscribed listeners and expose unsubscribe behavior in the test harness instead of only overwriting `wsClientMock.listener`. Add a focused describe block for the UAT Test 6 regression. The tests must fail against current `store.ts` because duplicate store registration leaves two active listeners and a single audio payload duplicates visible text. Prefer testing through the real `store.ts` dispatcher path, not by calling `appendAssistantSentence` directly, because the root cause is dispatcher subscription registration.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run ChatStreaming.test.tsx</automated>
  </verify>
  <done>ChatStreaming contains red/green-capable tests that reproduce duplicate visible text from duplicate store registration and cover first-audio-after-Thinking plus GPT-SoVITS failed-audio visible text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make WS store dispatcher registration idempotent and HMR-safe</name>
  <files>apps/renderer/src/ws/store.ts, apps/renderer/tests/ChatStreaming.test.tsx</files>
  <behavior>
    - Calling the store registration initializer more than once does not add multiple active message/reconnect listeners.
    - Vite HMR dispose unsubscribes the store's message and sidecar-reconnect listeners before module replacement.
    - Runtime module-load behavior remains automatic for production: importing `@/ws/store` still registers the dispatcher once.
  </behavior>
  <action>
    Refactor `store.ts` top-level registration into explicit idempotent functions, for example `ensureWSStoreSubscriptions()` and `disposeWSStoreSubscriptions()`, while preserving automatic registration on module load. Store the unsubscribe handles returned by `subscribe(...)` and `subscribeSidecarReconnect(...)`; if registration already exists, return without subscribing again. On `import.meta.hot?.dispose(...)`, call the dispose function so Vite HMR removes old listeners before re-evaluating the module. Keep the existing message routing logic unchanged except for wrapping it in a named dispatcher function. Do not add sidecar, GPT-SoVITS provider, TTS manager, or `appendAssistantSentence` dedupe logic; the fix belongs at the renderer WS store subscription boundary because sidecar/provider double-send was ruled out.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run ChatStreaming.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>One audio payload reaches `appendAssistantSentence` once even after simulated duplicate store registration; HMR disposal unregisters message/reconnect callbacks; existing failed-audio tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Record gap-fix evidence without claiming live UAT pass</name>
  <files>.planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md</files>
  <action>
    Update `17-UAT.md` with a new Plan 17-09 gap-fix evidence subsection listing the renderer test and typecheck results. Keep Test 6 `result: issue` or otherwise clearly awaiting user retest; do not mark Test 6 as passed until the user confirms a live GPT-SoVITS chat turn no longer duplicates visible text. Add the new implementation artifacts under the existing Test 6 gap as resolved-by/plan evidence only if the file's structure supports it without implying manual pass.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run ChatStreaming.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>`17-UAT.md` records automated Plan 17-09 evidence and explicitly states that live GPT-SoVITS Test 6 remains awaiting user retest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar WS -> renderer dispatcher | Localhost WebSocket messages cross into renderer UI state and audio playback. |
| renderer HMR/runtime module boundary -> singleton WS client | Dev-time module re-evaluation can mutate listener state on a long-lived singleton client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09-01 | D | `apps/renderer/src/ws/store.ts` | mitigate | Make store subscriptions idempotent and dispose old listeners during HMR so one payload cannot fan out into duplicate UI/audio work. |
| T-17-09-02 | T | `apps/renderer/src/ws/store.ts` | mitigate | Preserve existing contract guards (`isAudioPayload`, `isControl`, etc.) while moving routing into a named dispatcher; do not loosen message validation. |
| T-17-09-03 | I | `17-UAT.md` | mitigate | Record automated evidence only and avoid false live-UAT pass claims until user retests. |
</threat_model>

<verification>
Run the focused renderer checks:

```bash
npm --workspace apps/renderer run test -- --run ChatStreaming.test.tsx
npm --workspace apps/renderer run typecheck
```

Optional low-cost broader renderer regression if the focused checks pass quickly:

```bash
npm --workspace apps/renderer run test -- --run Settings.test.tsx ChatStreaming.test.tsx
```
</verification>

<success_criteria>
- Duplicate/HMR store registration regression tests fail before the fix and pass after the fix.
- `apps/renderer/src/ws/store.ts` owns and cleans up unsubscribe handles from `subscribe` and `subscribeSidecarReconnect`.
- A single normal GPT-SoVITS audio payload after Thinking produces one visible sentence, not duplicated chunks.
- A single failed GPT-SoVITS audio payload keeps visible text once and preserves existing failed-audio UI semantics.
- `17-UAT.md` documents Plan 17-09 automated evidence without marking Test 6 passed before user retest.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-09-SUMMARY.md`.
</output>
