---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - apps/electron-main/src/safe-storage.ts
  - apps/electron-main/src/reference-audio.ts
  - apps/electron-main/src/ipc.ts
  - apps/electron-main/preload/index.ts
  - apps/electron-main/preload/index.d.ts
  - apps/electron-main/tests/reference-audio.test.ts
  - apps/electron-main/tests/safe-storage.test.ts
  - sidecar/src/sidecar/admin/audio.py
  - sidecar/tests/admin/test_reference_audio_validation_endpoint.py
autonomous: true
requirements:
  - TTS-01
  - TTS-02
  - TTS-06
  - PRESET-01
  - PRESET-02
  - PRESET-03
  - PRESET-04
must_haves:
  truths:
    - "Global voice presets and active avatar/session associations persist in app audio settings without touching avatar import catalogs."
    - "Reference audio import copies files to sanitized app-managed storage and validates format, readable metadata, and duration using sidecar soundfile before use."
    - "Deleting active presets or in-use reference audio is blocked until reassignment; no silent Piper fallback and no cascade delete."
  artifacts:
    - path: apps/electron-main/src/safe-storage.ts
      provides: StoredConfig migration/defaults for voicePresets, referenceAudioAssets, and activePresetByAvatarSession
    - path: apps/electron-main/src/reference-audio.ts
      provides: sanitized import/delete guard helpers
    - path: sidecar/src/sidecar/admin/audio.py
      provides: soundfile-backed reference-audio validation endpoint
  key_links:
    - from: apps/electron-main/src/reference-audio.ts
      to: sidecar/src/sidecar/admin/audio.py
      via: IPC calls `/admin/audio/reference-audio/validate` before accepting imported assets
      pattern: "reference-audio/validate|validateReferenceAudio"
    - from: apps/electron-main/src/safe-storage.ts
      to: packages/contracts/ts/voice-preset.ts
      via: generated preset/reference contracts
      pattern: "voicePresets|referenceAudioAssets|activePresetByAvatarSession"
---

# 17-02 Plan: Preset Persistence and Reference-Audio Validation IPC

<objective>
Implement Electron-owned voice preset persistence and reference-audio asset management, with concrete sidecar `soundfile` validation before imported audio can be used.

Purpose: Presets and copied reference audio must be safe, validated, and available to later GPT-SoVITS provider/UI plans.
Output: StoredConfig migration/defaults, reference-audio import/delete helpers, sidecar validation endpoint, IPC/preload APIs, and tests.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-01-SUMMARY.md

<interfaces>
Use generated `VoicePreset`, `ReferenceAudioAsset`, and GPT-SoVITS validation contracts from Plan 17-01. The validation path is concrete: Electron copies/picks the file, sidecar validates readable metadata/duration via `soundfile`, Electron accepts the asset only on validation success.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StoredConfig voice preset library and delete guards</name>
  <files>apps/electron-main/src/safe-storage.ts, apps/electron-main/tests/safe-storage.test.ts</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-05 through D-08 and D-16
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` safe-storage pattern
    - `apps/electron-main/src/safe-storage.ts`
    - `apps/electron-main/tests/safe-storage.test.ts`
  </read_first>
  <behavior>
    - Test 1: Existing config migrates without losing provider/plugin/audio/history/avatar settings.
    - Test 2: Defaults include `voicePresets: []`, `referenceAudioAssets: []`, and `activePresetByAvatarSession: {}`.
    - Test 3: `canDeletePreset` blocks active preset deletion until reassignment per D-08.
    - Test 4: Active association key is stored in audio settings and no avatar catalog import artifacts are mutated per D-06.
  </behavior>
  <action>Bump StoredConfig schema only as needed by the existing Phase 16 schema. Add pure helpers: `defaultVoicePresetLibrary()`, `getAvatarSessionPresetKey(avatarId, sessionId)`, `canDeletePreset(presetId, activePresetByAvatarSession)`, and `canDeleteReferenceAudio(assetId, voicePresets)`. Preserve all existing config fields.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/electron-main/src/safe-storage.ts | grep -c "activePresetByAvatarSession"` returns at least 1.
    - Tests prove active preset deletion does not silently set Piper and does not leave broken active ids.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/electron-main run test -- --run safe-storage.test.ts</automated>
  </verify>
  <done>Preset library and active associations persist safely with referential integrity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sidecar soundfile reference-audio validation endpoint</name>
  <files>sidecar/pyproject.toml, sidecar/src/sidecar/admin/audio.py, sidecar/tests/admin/test_reference_audio_validation_endpoint.py</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-13 through D-15
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` Standard Stack, Don't Hand-Roll, and Open Questions (RESOLVED)
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` Wave 0 requirements
    - `sidecar/src/sidecar/admin/audio.py`
    - `sidecar/tests/admin/test_audio_status_endpoint.py`
  </read_first>
  <behavior>
    - Test 1: `POST /admin/audio/reference-audio/validate` rejects missing files, unsupported extensions, unreadable files, and unreadable metadata.
    - Test 2: The endpoint accepts `.wav`, `.flac`, `.mp3`, and `.ogg` only when `soundfile.info()` can read duration/sample rate/channels.
    - Test 3: Duration outside `1s <= duration <= 30s` fails with typed validation detail per D-14.
    - Test 4: Response redacts original absolute path in summary/normal diagnostics.
  </behavior>
  <action>Add `soundfile==0.13.1` if missing. Implement `POST /admin/audio/reference-audio/validate` under the existing audio admin router. Request includes candidate managed path/display basename/transcript/language; response includes `ok`, `format`, `duration_seconds`, `sample_rate`, `channels`, `errors`, and `redacted_diagnostics`. Use `soundfile.info()`; do not hand-roll RIFF parsing.</action>
  <acceptance_criteria>
    - `grep -v '^#' sidecar/src/sidecar/admin/audio.py | grep -c "reference-audio/validate"` returns at least 1.
    - Endpoint tests cover missing, unsupported, too-short, too-long, unreadable, and valid audio cases.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest sidecar/tests/admin/test_reference_audio_validation_endpoint.py -q</automated>
  </verify>
  <done>Reference-audio validation has a concrete sidecar fallback using soundfile-readable metadata and duration bounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reference-audio and preset IPC/preload APIs</name>
  <files>apps/electron-main/src/reference-audio.ts, apps/electron-main/src/ipc.ts, apps/electron-main/preload/index.ts, apps/electron-main/preload/index.d.ts, apps/electron-main/tests/reference-audio.test.ts</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` IPC/preload and file picker patterns
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` Open Questions (RESOLVED) reference path accessibility resolution
    - `apps/electron-main/src/ipc.ts`
    - `apps/electron-main/preload/index.ts`
    - `apps/electron-main/preload/index.d.ts`
  </read_first>
  <behavior>
    - Test 1: `referenceAudio:pickAndImport` copies to `app.getPath('userData')/reference-audio/{assetId}-{sanitizedBasename}` and then calls validation before saving.
    - Test 2: `referenceAudio:validate` proxies to `/admin/audio/reference-audio/validate` and rejects invalid audio without saving an asset.
    - Test 3: `referenceAudio:delete` blocks in-use assets per D-16 and never cascade-deletes presets.
    - Test 4: Preload exposes typed `listVoicePresets`, `saveVoicePreset`, `deleteVoicePreset`, `setActiveVoicePresetForAvatarSession`, `pickAndImportReferenceAudio`, `validateReferenceAudio`, and `deleteReferenceAudio`.
  </behavior>
  <action>Create `reference-audio.ts` with generated ids, sanitized basenames, managed storage copy, import validation integration, and delete guard helpers. Add IPC channels `voicePresets:list`, `voicePresets:save`, `voicePresets:delete`, `voicePresets:setActiveForAvatarSession`, `referenceAudio:pickAndImport`, `referenceAudio:validate`, and `referenceAudio:delete`. Add matching preload methods and declarations. Do not write `AvatarOverrides` or avatar catalog JSON.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/electron-main/src/reference-audio.ts | grep -c "app.getPath('userData')"` returns at least 1.
    - `grep -v '^#' apps/electron-main/preload/index.d.ts | grep -c "validateReferenceAudio"` returns at least 1.
    - Tests prove original absolute paths are not returned as the primary asset path.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/electron-main run test -- --run reference-audio.test.ts safe-storage.test.ts</automated>
    <automated>npm --workspace apps/electron-main run build</automated>
  </verify>
  <done>Renderer can manage presets/reference audio through typed IPC, and imported reference audio is validated before use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→Electron IPC | Renderer requests preset CRUD and reference-audio file import/delete. |
| user filesystem→app storage | User-selected audio crosses into managed app storage. |
| Electron→sidecar admin | Electron sends candidate managed path to validation endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | T/I | `reference-audio.ts` | mitigate | Generate asset ids, sanitize basenames, copy under userData, and never persist original absolute path as primary asset path per D-13. |
| T-17-05 | T/D | preset/reference delete helpers | mitigate | Block active preset deletion until reassignment and block in-use reference-audio deletion per D-08/D-16. |
| T-17-06 | I | validation endpoint responses | mitigate | Redact original absolute paths and raw exceptions in normal responses. |
</threat_model>

<verification>
Run after all tasks:

```powershell
npm --workspace apps/electron-main run test -- --run reference-audio.test.ts safe-storage.test.ts
npm --workspace apps/electron-main run build
python -m pytest sidecar/tests/admin/test_reference_audio_validation_endpoint.py -q
```
</verification>

<success_criteria>
- Presets are global reusable data and active association is app audio settings keyed by avatar/session.
- Reference audio is copied into sanitized app-managed storage and validated with soundfile before use.
- Deletion guards prevent broken active preset or reference-audio references.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-02-SUMMARY.md`.
</output>
