---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-06
type: execute
wave: 6
depends_on:
  - 17-02
  - 17-03
  - 17-04
  - 17-05
files_modified:
  - apps/renderer/src/screens/Settings/Settings.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/src/index.css
  - apps/renderer/tests/Settings.test.tsx
autonomous: true
requirements:
  - TTS-01
  - TTS-02
  - TTS-03
  - TTS-04
  - TTS-06
  - PRESET-01
  - PRESET-02
  - PRESET-03
  - PRESET-04
must_haves:
  truths:
    - "User can explicitly select Piper or GPT-SoVITS; Piper selection saves active_provider='piper' immediately, while GPT-SoVITS activation is disabled until health and audible test synthesis pass."
    - "User can hear/preview returned test synthesis audio without sending a chat turn or writing conversation history."
    - "User can manage global voice presets and validated reference audio with guarded active/in-use deletes."
    - "Settings shows concise failure/fallback state and routes details to existing logs."
  artifacts:
    - path: apps/renderer/src/screens/Settings/Settings.tsx
      provides: Phase 17 TTS provider/preset/reference-audio/test playback UI
    - path: apps/renderer/tests/Settings.test.tsx
      provides: activation gate, audible preview, and no chat/history regression coverage
  key_links:
    - from: apps/renderer/src/screens/Settings/Settings.tsx
      to: window.api.testGptSoVitsSynthesis
      via: base64 audio Blob/ObjectURL/Audio preview path
      pattern: "testGptSoVitsSynthesis|createObjectURL|Audio"
---

# 17-06 Plan: Settings Provider, Preset, Reference-Audio, and Audible Test UI

<objective>
Replace the compact Piper-only Settings TTS section with GPT-SoVITS setup, activation gate, audible test-synthesis preview, voice preset library, reference-audio management, and concise failure surfaces.

Purpose: Users need to configure and validate GPT-SoVITS plus presets before using them in chat, and TTS-04 requires hearing the test result without creating a chat turn.
Output: Settings UI, centralized copy strings, small CSS extensions, and renderer tests.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-02-SUMMARY.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-03-SUMMARY.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-04-SUMMARY.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-05-SUMMARY.md

<interfaces>
Use preload APIs from prior plans: `checkGptSoVitsHealth`, `testGptSoVitsSynthesis`, `validateReferenceAudio`, preset CRUD, reference-audio import/delete, active avatar/session preset association, and app-managed process status/start/stop/restart. Health/test synthesis preload methods come from Plan 17-04; process lifecycle preload methods come from Plan 17-05. Keep Settings as the existing long-scroll surface; do not create STT/mic/PTT/VAD UI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build provider setup, health gate, and audible test synthesis preview</name>
  <files>apps/renderer/src/screens/Settings/Settings.tsx, apps/renderer/src/lib/copy.ts, apps/renderer/src/index.css, apps/renderer/tests/Settings.test.tsx</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md` layout, activation gate, provider controls, accessibility, and copywriting sections
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-01 through D-04 and D-09 through D-12
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` TTS-04 validation row
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` Settings/copy/test patterns
    - `apps/renderer/src/screens/Settings/Settings.tsx`
    - `apps/renderer/src/lib/copy.ts`
    - `apps/renderer/tests/Settings.test.tsx`
  </read_first>
  <behavior>
    - Test 1: `Piper local TTS` and `GPT-SoVITS` radio rows are keyboard-accessible; selecting `Piper local TTS` explicitly saves `active_provider='piper'` without GPT-SoVITS gates, while selecting GPT-SoVITS creates candidate state but does not save active provider.
    - Test 2: External mode shows one `Base URL`; app-launched mode shows command, working directory, optional health URL, and lifecycle controls.
    - Test 3: `Activate voice preset` is disabled until health check and `Test synthesis` both pass; failed test shows `Test failed. Keeping the previous provider and preset.` and does not save activation.
    - Test 4: Successful `testGptSoVitsSynthesis` converts returned base64/audio bytes to a `Blob`/ObjectURL or `Audio` element, plays/previews it, revokes old ObjectURLs, and does not call chat submission or conversation-history write APIs.
    - Test 5: A non-localhost GPT-SoVITS base URL such as `http://192.168.1.50:9880` renders the amber warning copy `Advanced: this sends text/reference paths to a non-local GPT-SoVITS server.` from `COPY.SETTINGS`.
  </behavior>
  <action>Replace `TTSSection` contents with UI-SPEC group order. Implement two distinct provider-selection paths: selecting `Piper local TTS` calls the config save/preload path with `active_provider: 'piper'` immediately and clears only GPT-SoVITS candidate UI state; selecting `GPT-SoVITS` creates a candidate and does not save active provider until gates pass. `Test synthesis` must call `window.api.testGptSoVitsSynthesis(candidate)`, decode returned base64/audio bytes, create a `Blob` + ObjectURL (or `Audio` with equivalent cleanup), play/preview the result, and revoke previous URLs on new preview/unmount. Activation save sets `active_provider='gpt_sovits'`, selected active preset association, and gate flags only after health plus audible test success. Detect non-localhost base URLs (`hostname` not `localhost`, `127.0.0.1`, or `::1`) and render the exact amber warning copy from `COPY.SETTINGS`: `Advanced: this sends text/reference paths to a non-local GPT-SoVITS server.` Do not send a chat turn, do not append a message, and do not write conversation history from this path.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/renderer/src/screens/Settings/Settings.tsx | grep -c "Activate voice preset"` returns 0 because copy is centralized.
    - Settings tests assert selecting `Piper local TTS` calls the config save/preload path with `active_provider: 'piper'` and does not require GPT-SoVITS health/test gates.
    - Settings tests assert non-localhost base URL renders `Advanced: this sends text/reference paths to a non-local GPT-SoVITS server.`
    - Settings tests assert failed health/test does not call activation save.
    - Settings tests assert test synthesis playback uses Blob/ObjectURL or Audio and no chat/history API is called.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run Settings.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>Settings can configure, health-check, audibly test, and gate GPT-SoVITS activation without chat/history side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build preset CRUD and active association UI</name>
  <files>apps/renderer/src/screens/Settings/Settings.tsx, apps/renderer/src/lib/copy.ts, apps/renderer/tests/Settings.test.tsx</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-05 through D-08
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md` preset CRUD section
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` destructive dialog and renderer test patterns
    - `apps/renderer/src/screens/Settings/Settings.tsx`
    - `apps/renderer/tests/Settings.test.tsx`
  </read_first>
  <behavior>
    - Test 1: Empty library renders `No voice presets yet.` and the required empty-state body.
    - Test 2: User can create, rename, select, and delete global presets through window.api without avatar catalog mutation.
    - Test 3: Deleting the active preset shows `Delete voice preset? Choose another active preset before deleting this one.` and blocks confirmation until reassignment.
    - Test 4: Active preset association is saved through `activePresetByAvatarSession` using current avatar/session key.
  </behavior>
  <action>Add preset list/radio rows with name, provider, language, validation state, create/rename controls, and delete alert dialog. Preset fields: `name`, `prompt_text`, `prompt_lang`, `text_lang`, `reference_audio_id`, `top_k`, `top_p`, `temperature`, `speed_factor`, `repetition_penalty`, `text_split_method`. Do not put base URL or launch command in preset forms.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/renderer/src/screens/Settings/Settings.tsx | grep -c "avatar_overrides\|AvatarOverrides"` returns 0.
    - Tests verify active preset delete is blocked and no Piper fallback is selected implicitly.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run Settings.test.tsx</automated>
  </verify>
  <done>Users can manage global presets and active avatar/session associations with delete safeguards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build reference-audio management and validation summary UI</name>
  <files>apps/renderer/src/screens/Settings/Settings.tsx, apps/renderer/src/lib/copy.ts, apps/renderer/src/index.css, apps/renderer/tests/Settings.test.tsx</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-13 through D-16
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md` reference-audio import section
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` Open Questions (RESOLVED) reference path accessibility resolution
    - `apps/renderer/src/screens/Settings/Settings.tsx`
    - `apps/renderer/src/lib/copy.ts`
  </read_first>
  <behavior>
    - Test 1: `Import reference audio` calls native picker/import API and displays managed asset metadata, not original absolute path.
    - Test 2: Transcript text and language are required before asset activation.
    - Test 3: Validation summary includes `Format`, `Duration`, `Metadata`, and `Server access`; failed items block activation.
    - Test 4: Deleting in-use reference audio shows `Reference audio is used by {N} preset(s). Reassign those presets before deleting it.` and blocks deletion.
  </behavior>
  <action>Add the Reference audio group below Voice presets. Use concise validation labels and UI-SPEC status colors. When test synthesis returns server-read/path failure, show `GPT-SoVITS could not read the copied reference audio. Open logs for details.` without raw stack traces or original paths.</action>
  <acceptance_criteria>
    - Settings tests assert imported UI does not render `C:\\Users` or `/Users/` original absolute paths.
    - Tests assert in-use reference delete is blocked and no presets are cascade-deleted.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run Settings.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>Users can import/manage validated reference audio and see actionable blocking errors before activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→renderer forms | User enters base URL, command/cwd, preset metadata, and reference transcript. |
| renderer→preload IPC | Settings requests provider tests, config saves, preset CRUD, process controls, and file import. |
| test synthesis audio→renderer playback | Returned audio bytes are converted to a local preview object URL/Audio element. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | I | Settings main UI | mitigate | Main UI shows concise statuses and `Open logs`; no raw stderr/stack traces/original absolute paths/full transcripts. |
| T-17-14 | T | activation gate | mitigate | Disable activation until health and audible test pass; failed candidate leaves previous provider/preset intact. |
| T-17-15 | D/I | object URL preview | mitigate | Revoke old ObjectURLs on replacement/unmount and never persist test audio into conversation history. |
</threat_model>

<verification>
```powershell
npm --workspace apps/renderer run test -- --run Settings.test.tsx
npm --workspace apps/renderer run typecheck
```
</verification>

<success_criteria>
- Settings implements UI-SPEC scope and excludes STT/mic/PTT/VAD/code-switch/AEC/installer/training/cloning UI.
- Piper selection is an explicit save path; GPT-SoVITS activation requires health plus audible test synthesis and preserves previous active settings on failure.
- Test synthesis audio is heard/previewed without chat submission or conversation-history writes.
- Presets/reference assets are global, validated, sanitized, and guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-06-SUMMARY.md`.
</output>
