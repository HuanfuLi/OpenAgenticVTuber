---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-07
type: execute
wave: 7
depends_on:
  - 17-03
  - 17-06
files_modified:
  - apps/renderer/src/screens/Chat/useStreamingMessages.ts
  - apps/renderer/src/ws/store.ts
  - apps/renderer/src/screens/Chat/Chat.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/tests/ChatStreaming.test.tsx
  - apps/renderer/tests/Settings.test.tsx
  - sidecar/tests/test_tts_manager.py
  - .planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
autonomous: false
requirements:
  - TTS-01
  - TTS-04
  - TTS-06
  - PRESET-01
  - PRESET-02
  - PRESET-03
  - PRESET-04
must_haves:
  truths:
    - "GPT-SoVITS chat-turn synthesis failure shows sentence text plus visible failed-audio state, not an empty or silently replaced turn."
    - "Piper fallback is available only as a next-turn explicit user selection/action after a visible notice."
    - "Test synthesis can be heard/previewed without sending a chat turn or writing conversation history."
    - "Final regression proves contracts, sidecar, Electron bridge, Settings, chat failure, preset/reference integrity, and no-silent-fallback behavior together."
  artifacts:
    - path: apps/renderer/src/screens/Chat/useStreamingMessages.ts
      provides: failed-audio state in streaming chat messages
    - path: apps/renderer/src/ws/store.ts
      provides: renderer routing for `AudioPayloadMessage.audio === null` plus failed metadata
    - path: .planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
      provides: manual GPT-SoVITS server UAT checklist and evidence template
  key_links:
    - from: sidecar/src/sidecar/tts/tts_manager.py
      to: apps/renderer/src/ws/store.ts
      via: `AudioPayloadMessage` with `audio=null` and `failed_audio.provider_id="gpt_sovits"`
      pattern: "failed_audio|audio === null"
---

# 17-07 Plan: Chat Failure Surface, Final Regression, and UAT

<objective>
Finish Phase 17 by making GPT-SoVITS mid-turn audio failures visible in chat, proving no silent fallback, and collecting final automated/manual evidence.

Purpose: GPT-SoVITS must be safe to use for character voice output without hiding provider failures or corrupting playback/conversation semantics.
Output: Failed-audio chat UI, final regression tests, and `17-UAT.md`.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-03-SUMMARY.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-06-SUMMARY.md

<interfaces>
Use `AudioPayloadMessage` failed-audio metadata from Plan 17-01, sidecar failure behavior from Plan 17-03, and Settings/fallback copy from Plan 17-06. Renderer must append sentence text exactly as before and add failure affordance; it must not change active provider in chat reducers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render GPT-SoVITS failed-audio state in chat</name>
  <files>apps/renderer/src/screens/Chat/useStreamingMessages.ts, apps/renderer/src/ws/store.ts, apps/renderer/src/screens/Chat/Chat.tsx, apps/renderer/src/lib/copy.ts, apps/renderer/tests/ChatStreaming.test.tsx</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-09 through D-11
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md` failure/fallback and copy sections
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` chat failure UI pattern
    - `apps/renderer/src/screens/Chat/useStreamingMessages.ts`
    - `apps/renderer/src/ws/store.ts`
    - `apps/renderer/src/screens/Chat/Chat.tsx`
    - `apps/renderer/src/lib/copy.ts`
  </read_first>
  <behavior>
    - Test 1: When renderer receives `AudioPayloadMessage` with `audio=null`, `failed_audio.provider_id="gpt_sovits"`, and display text, sentence text is appended exactly once.
    - Test 2: The affected sentence renders `Audio failed for this sentence. Text is still shown.` and banner `GPT-SoVITS failed. Select Piper for the next turn if you want fallback audio.`
    - Test 3: Chat reducer/store does not call config save and does not set provider to Piper after failed GPT-SoVITS sentence.
    - Test 4: After a failed GPT-SoVITS sentence, the explicit next-turn fallback path is only the Settings `Piper local TTS` selection from Plan 17-06; selecting Piper saves `active_provider='piper'` for subsequent turns and does not mutate the already-failed in-progress turn.
    - Test 5: Technical failure details are not dumped inline; UI offers existing logs affordance/copy.
  </behavior>
  <action>Extend streaming message state with optional per-sentence audio failure marker, using names like `audioFailed`, `failedProviderId`, and `failureSummary`. In `ws/store.ts`, handle `isAudioPayload(msg)` where `msg.audio === null` and `msg.failed_audio?.provider_id === 'gpt_sovits'` by appending sentence text and marking failed audio. Do not treat all silent/action-only payloads as errors; require failed metadata. Chat failure handling must never call config save and must never set `active_provider='piper'`; the explicit next-turn fallback is the Settings Piper selection path planned in 17-06. Add copy strings exactly from UI-SPEC.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/renderer/src/ws/store.ts | grep -c "saveStoredConfig\|active_provider.*piper"` returns 0 for failure handling additions.
    - Chat tests prove failed GPT-SoVITS audio still displays sentence text and does not auto-select Piper.
    - Settings regression tests from 17-06 prove explicit Piper selection saves `active_provider: 'piper'` for subsequent turns after visible failure notice.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run ChatStreaming.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>GPT-SoVITS failed audio is visible and actionable without silent provider switching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run cross-tier regression and close remaining gaps</name>
  <files>apps/renderer/tests/Settings.test.tsx, apps/renderer/tests/ChatStreaming.test.tsx, sidecar/tests/test_tts_manager.py, sidecar/tests/tts/test_gpt_sovits_provider.py, sidecar/tests/admin/test_audio_test_tts_endpoint.py</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` full suite command and Wave 0 requirements
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` validation architecture
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-01 through D-16
    - Existing tests modified by Plans 17-01 through 17-06
  </read_first>
  <behavior>
    - Test 1: Contracts, sidecar provider/admin/reference validation tests, renderer Settings/Chat tests, renderer typecheck, and Electron main build pass together.
    - Test 2: No implementation path enables silent Piper fallback after GPT-SoVITS failure.
    - Test 3: Test synthesis path remains distinct from chat turn path and does not write conversation history.
  </behavior>
  <action>Run the full focused Phase 17 suite and fix cross-tier mismatches. If a live GPT-SoVITS server is unavailable, automated tests must still pass with mocked HTTP/provider tests; document live-server UAT as manual in Task 3.</action>
  <acceptance_criteria>
    - All verify commands pass.
    - Grep gate: `grep -R "silent.*fallback\|fallback.*mid-turn" apps sidecar packages | grep -v node_modules | grep -v "must not\|never\|No silent"` shows no implementation path enabling silent fallback.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:contracts</automated>
    <automated>npm --workspace apps/renderer run test -- --run Settings.test.tsx ChatStreaming.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
    <automated>npm --workspace apps/electron-main run build</automated>
    <automated>python -m pytest sidecar/tests/tts/test_gpt_sovits_provider.py sidecar/tests/admin/test_audio_test_tts_endpoint.py sidecar/tests/admin/test_audio_status_endpoint.py sidecar/tests/admin/test_reference_audio_validation_endpoint.py sidecar/tests/test_tts_gateway.py sidecar/tests/test_tts_manager.py -q</automated>
  </verify>
  <done>Phase 17 automated evidence covers contracts, sidecar, Electron bridge, Settings, chat failure, and no-silent-fallback behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual GPT-SoVITS server UAT evidence</name>
  <files>.planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 17 success criteria
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` all decisions D-01 through D-16
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-UI-SPEC.md` interaction contract
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` manual-only verifications
  </read_first>
  <action>Create `17-UAT.md` containing the checklist below, automated command results, live server availability note, and user-observed pass/fail evidence. If no live GPT-SoVITS server is available, mark live synthesis UAT as blocked by environment and include mocked automated coverage results.</action>
  <acceptance_criteria>
    - `17-UAT.md` exists and records automated regression results plus live-server UAT result or explicit environment block.
    - UAT confirms no silent mid-turn Piper fallback and no chat-turn side effect from test synthesis.
  </acceptance_criteria>
  <verify>
    <automated>Test-Path -LiteralPath ".planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md"</automated>
  </verify>
  <what-built>Automated GPT-SoVITS provider, preset/reference storage, Settings activation gate with audible test synthesis, app-managed process controls, and chat failed-audio surfaces.</what-built>
  <how-to-verify>
    1. Start or provide a GPT-SoVITS API v2 server, normally at `http://127.0.0.1:9880`, with reference audio accessible to that server.
    2. Open Settings → TTS. Confirm Piper remains active initially.
    3. Enter GPT-SoVITS base URL. Run health check. Expected: health can pass without activation; helper says test synthesis is still required.
    4. Import reference audio. Expected: UI shows managed asset metadata, transcript/language fields, and validation summary; it does not show the original absolute path.
    5. Create a voice preset and click `Test synthesis`. Expected: audible/previewable result without sending a chat turn or adding conversation history.
    6. Click `Activate voice preset`. Expected: `GPT-SoVITS is active for the next turn.`
    7. Send a chat turn. Expected: sentence text appears, audio plays through existing renderer payload/RMS/lipsync path.
    8. Stop the external server during a later GPT-SoVITS turn. Expected: affected sentence text remains visible, audio is marked failed, logs have detail, and the app does not switch to Piper until the user explicitly selects Piper for a next turn.
  </how-to-verify>
  <resume-signal>Type "approved" if live/manual checks pass, or describe observed failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar audio payload→renderer chat | Failed provider metadata crosses into user-visible chat UI. |
| manual live GPT-SoVITS server→app | External service behavior is validated by user-run server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | I | Chat failed-audio UI | mitigate | Show concise failure and log pointer only; no raw provider stderr/paths inline per D-11. |
| T-17-17 | T/R | renderer failure reducer | mitigate | Reducer cannot mutate provider config; tests assert no silent Piper selection. |
| T-17-18 | D | live test dependency | accept | Live GPT-SoVITS server may be absent; automated mocked tests still gate code, UAT records environment block. |
</threat_model>

<verification>
```powershell
npm run check:contracts
npm --workspace apps/renderer run test -- --run Settings.test.tsx ChatStreaming.test.tsx
npm --workspace apps/renderer run typecheck
npm --workspace apps/electron-main run build
python -m pytest sidecar/tests/tts/test_gpt_sovits_provider.py sidecar/tests/admin/test_audio_test_tts_endpoint.py sidecar/tests/admin/test_audio_status_endpoint.py sidecar/tests/admin/test_reference_audio_validation_endpoint.py sidecar/tests/test_tts_gateway.py sidecar/tests/test_tts_manager.py -q
```
</verification>

<success_criteria>
- GPT-SoVITS failure during chat visibly marks failed audio while preserving sentence text.
- Piper fallback requires explicit next-turn user selection/action, implemented as the Settings Piper save path rather than chat-side mutation.
- Test synthesis produces a hearable/previewable output without sending a chat turn.
- Final evidence covers all Phase 17 roadmap success criteria and decisions D-01 through D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-07-SUMMARY.md`.
</output>
