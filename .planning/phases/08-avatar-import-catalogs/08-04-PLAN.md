---
phase: 08-avatar-import-catalogs
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - packages/contracts/py/contracts/action_binding.py
  - packages/contracts/py/contracts/avatar_overrides.py
  - packages/contracts/py/contracts/avatar_import_plan.py
  - packages/contracts/py/contracts/rig_capabilities.py
  - packages/contracts/py/contracts/__init__.py
  - packages/contracts/scripts/codegen.py
  - packages/contracts/generated/json-schema/action-binding.schema.json
  - packages/contracts/generated/json-schema/avatar-import-plan.schema.json
  - packages/contracts/generated/json-schema/avatar-overrides.schema.json
  - packages/contracts/generated/json-schema/rig-capabilities.schema.json
  - packages/contracts/ts/action-binding.ts
  - packages/contracts/ts/avatar-import-plan.ts
  - packages/contracts/ts/avatar-overrides.ts
  - packages/contracts/ts/rig-capabilities.ts
  - packages/contracts/ts/index.ts
  - sidecar/schemas/avatar_overrides.schema.json
  - sidecar/src/sidecar/avatar/extractors/olvt.py
  - sidecar/src/sidecar/avatar/rig_capabilities.py
  - sidecar/src/sidecar/admin/avatar.py
  - sidecar/tests/avatar/test_extract_olvt.py
  - sidecar/tests/avatar/test_overrides_writer.py
  - sidecar/tests/avatar/test_rig_capabilities.py
  - sidecar/tests/avatar/test_admin_avatar.py
  - avatars/teto/_avatar_overrides.yaml
  - .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
  - .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
autonomous: false
gap_closure: true
requirements:
  - IMP-05
  - IMP-08
  - IMP-09
  - IMP-10
  - ARCH-02

must_haves:
  truths:
    - "OLVT model_dict.json import preserves BOTH surfaces: actionMap as variants and emotionMap as default-plugin action-code bindings."
    - "AvatarOverrides persists default_plugin_action_bindings and RigCapabilities exposes the same bindings for Phase 6 default-plugin on_load consumption."
    - "The schema and generated TS contracts include default_plugin_action_bindings, so commit validation and renderer IPC cannot silently drop emotionMap data."
    - "A dogfooded avatars/teto/_avatar_overrides.yaml exists, validates, contains variants/events/default_plugin_action_bindings keys, and is backed by a completed native-dialog UAT evidence file."
    - "VTS introspection against actual Teto is either PASS with captured output or explicitly recorded as BLOCKED with date, operator, command, exit code, and reason."
  artifacts:
    - path: "packages/contracts/py/contracts/action_binding.py"
      provides: "DefaultPluginActionBinding Pydantic contract"
      contains: "class DefaultPluginActionBinding"
    - path: "packages/contracts/py/contracts/avatar_overrides.py"
      provides: "Persisted default-plugin action bindings in AvatarOverrides"
      contains: "default_plugin_action_bindings"
    - path: "packages/contracts/py/contracts/rig_capabilities.py"
      provides: "RigCapabilities action binding surface for Phase 6 default plugin"
      contains: "default_plugin_action_bindings"
    - path: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      provides: "emotionMap ingestion plus existing actionMap variant catalog"
      contains: "emotionMap"
    - path: "sidecar/schemas/avatar_overrides.schema.json"
      provides: "jsonschema validation for default_plugin_action_bindings"
      contains: "default_plugin_action_bindings"
    - path: "avatars/teto/_avatar_overrides.yaml"
      provides: "Dogfooded Teto catalog artifact from native-dialog review/save flow"
      contains: "source_rig_path"
    - path: ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md"
      provides: "Persisted manual dogfood evidence"
      contains: "status:"
    - path: ".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"
      provides: "Persisted VTS smoke evidence"
      contains: "uv run python scripts/vts_introspect_smoke.py"
  key_links:
    - from: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      to: "packages/contracts/py/contracts/action_binding.py"
      via: "extract_olvt reads emotionMap into DefaultPluginActionBinding(action_code, expression_index)"
      pattern: "DefaultPluginActionBinding"
    - from: "sidecar/src/sidecar/admin/avatar.py"
      to: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      via: "OLVT import branch stores default_plugin_action_bindings on AvatarImportPlan"
      pattern: "default_plugin_action_bindings"
    - from: "sidecar/src/sidecar/admin/avatar.py"
      to: "sidecar/src/sidecar/avatar/overrides_writer.py"
      via: "commit writes AvatarOverrides.default_plugin_action_bindings through jsonschema"
      pattern: "write_avatar_overrides_atomic"
    - from: "sidecar/src/sidecar/avatar/rig_capabilities.py"
      to: "packages/contracts/py/contracts/rig_capabilities.py"
      via: "build_rig_capabilities copies bindings from AvatarOverrides"
      pattern: "default_plugin_action_bindings"
---

<objective>
Close Phase 8 verification gaps without rewriting completed plans 08-01 through 08-03.

Purpose: The implementation shipped a two-catalog schema that intentionally ignored OLVT `emotionMap`, but ROADMAP/REQUIREMENTS require `emotionMap` to survive as a default-plugin action-code binding surface. This plan adds that narrow binding surface and persists the manual evidence required for native-dialog dogfood and live VTS introspection.

Output: `default_plugin_action_bindings` added to contracts/schema/import path/RigCapabilities, OLVT tests updated, generated contracts refreshed, `avatars/teto/_avatar_overrides.yaml` produced by dogfood, and two UAT evidence files completed.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-avatar-import-catalogs/08-CONTEXT.md
@.planning/phases/08-avatar-import-catalogs/08-RESEARCH.md
@.planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md
@.planning/phases/08-avatar-import-catalogs/08-01-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-02-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-03-SUMMARY.md

<interfaces>
From current `packages/contracts/py/contracts/avatar_overrides.py`:
```python
class AvatarOverrides(BaseModel):
    voice: Voice = Field(default_factory=Voice)
    variants: list[VariantEntry] = Field(default_factory=list)
    events: list[EventEntry] = Field(default_factory=list)
    source_rig_path: str = ""
```

From current `packages/contracts/py/contracts/rig_capabilities.py`:
```python
class RigCapabilities(BaseModel):
    writable_param_ids: list[str] = Field(default_factory=list)
    param_ranges: dict[str, tuple[float, float] | None] = Field(default_factory=dict)
    expressions: list[Expression] = Field(default_factory=list)
    hotkeys: list[Hotkey] = Field(default_factory=list)
    cdi3_display_names: dict[str, str] = Field(default_factory=dict)
    sign_inversions: list[str] = Field(default_factory=list)
```

From current `sidecar/src/sidecar/avatar/extractors/olvt.py`:
```python
def extract_olvt(path: Path) -> tuple[list[VariantEntry], list[EventEntry], list[ImportWarning]]:
    # Currently reads actionMap only and comments that emotionMap is intentionally ignored.
```

From OLVT local reference `C:/Users/16079/Code/OpenLLM_Vtuber/model_dict.json`:
```json
"emotionMap": {
  "neutral": 0,
  "anger": 2,
  "disgust": 2,
  "fear": 1,
  "joy": 3,
  "smirk": 3,
  "sadness": 1,
  "surprise": 3
}
```
</interfaces>

<decision_override>
This gap closure narrowly supersedes Phase 8 CONTEXT D-A2-1/D-A2-3/D-A2-5. The executor MUST implement the required binding surface rather than preserving "emotionMap ignored." Do not remove or weaken IMP-05/ARCH-02 in ROADMAP/REQUIREMENTS.
</decision_override>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default-plugin action binding contract and preserve OLVT emotionMap</name>
  <files>
    packages/contracts/py/contracts/action_binding.py,
    packages/contracts/py/contracts/avatar_overrides.py,
    packages/contracts/py/contracts/avatar_import_plan.py,
    packages/contracts/py/contracts/rig_capabilities.py,
    packages/contracts/py/contracts/__init__.py,
    packages/contracts/scripts/codegen.py,
    packages/contracts/generated/json-schema/action-binding.schema.json,
    packages/contracts/generated/json-schema/avatar-import-plan.schema.json,
    packages/contracts/generated/json-schema/avatar-overrides.schema.json,
    packages/contracts/generated/json-schema/rig-capabilities.schema.json,
    packages/contracts/ts/action-binding.ts,
    packages/contracts/ts/avatar-import-plan.ts,
    packages/contracts/ts/avatar-overrides.ts,
    packages/contracts/ts/rig-capabilities.ts,
    packages/contracts/ts/index.ts,
    sidecar/schemas/avatar_overrides.schema.json,
    sidecar/src/sidecar/avatar/extractors/olvt.py,
    sidecar/src/sidecar/avatar/rig_capabilities.py,
    sidecar/src/sidecar/admin/avatar.py,
    sidecar/tests/avatar/test_extract_olvt.py,
    sidecar/tests/avatar/test_overrides_writer.py,
    sidecar/tests/avatar/test_rig_capabilities.py,
    sidecar/tests/avatar/test_admin_avatar.py
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md (failed truth #3 and missing binding surface)
    - .planning/REQUIREMENTS.md (IMP-05 and ARCH-02 wording)
    - C:/Users/16079/Code/OpenLLM_Vtuber/model_dict.json (actual emotionMap/actionMap fixture)
    - sidecar/src/sidecar/avatar/extractors/olvt.py (currently ignores emotionMap)
    - packages/contracts/py/contracts/avatar_overrides.py
    - packages/contracts/py/contracts/avatar_import_plan.py
    - packages/contracts/py/contracts/rig_capabilities.py
    - packages/contracts/scripts/codegen.py (Phase 5 codegen is active; generated TS/schema must not be hand-edited only)
    - sidecar/src/sidecar/admin/avatar.py (import/commit path that must carry bindings)
    - sidecar/src/sidecar/avatar/rig_capabilities.py (builder must expose bindings)
    - sidecar/tests/avatar/test_extract_olvt.py
    - sidecar/tests/avatar/test_admin_avatar.py
    - sidecar/tests/avatar/test_overrides_writer.py
  </read_first>
  <behavior>
    - Test 1: `extract_olvt(C:/Users/16079/Code/OpenLLM_Vtuber/model_dict.json)` returns the existing 6 Teto variants from `actionMap` unchanged.
    - Test 2: The same extractor returns 8 `DefaultPluginActionBinding` rows from `emotionMap`: neutral=0, anger=2, disgust=2, fear=1, joy=3, smirk=3, sadness=1, surprise=3.
    - Test 3: `/admin/avatar/import` for an OLVT folder returns `default_plugin_action_bindings` in the AvatarImportPlan response.
    - Test 4: `/admin/avatar/import/commit` writes `_avatar_overrides.yaml` with `default_plugin_action_bindings` and excludes wire-only fields.
    - Test 5: `build_rig_capabilities()` copies `AvatarOverrides.default_plugin_action_bindings` into `RigCapabilities.default_plugin_action_bindings`.
    - Test 6: `npm run check:contracts` passes with no generated contract drift.
  </behavior>
  <action>
    Create `packages/contracts/py/contracts/action_binding.py` with:
    ```python
    from __future__ import annotations
    from typing import Literal
    from pydantic import BaseModel, Field

    class DefaultPluginActionBinding(BaseModel):
        plugin_name: Literal["default"] = "default"
        action_code: str = Field(pattern=r"^[a-z][a-z0-9-]{0,30}$")
        expression_index: int = Field(ge=0)
        expression_name: str = ""
        source: Literal["olvt_emotionMap", "manual"] = "olvt_emotionMap"
    ```

    Add `default_plugin_action_bindings: list[DefaultPluginActionBinding] = Field(default_factory=list)` to `AvatarOverrides`, `AvatarImportPlan`, and `RigCapabilities`. Update `packages/contracts/py/contracts/__init__.py` exports.

    Update `packages/contracts/scripts/codegen.py` so `DefaultPluginActionBinding` emits `packages/contracts/ts/action-binding.ts` and `packages/contracts/generated/json-schema/action-binding.schema.json`, and so the generated TS/index exports include the new type. Run `npm run check:contracts`; accept generated TS/schema output.

    Update `sidecar/schemas/avatar_overrides.schema.json` with a `default_plugin_action_bindings` property:
    - array default `[]`
    - object additionalProperties false
    - required `plugin_name`, `action_code`, `expression_index`, `expression_name`, `source`
    - `plugin_name.enum == ["default"]`
    - `action_code.pattern == "^[a-z][a-z0-9-]{0,30}$"` and same reserved-name `not.enum` used by variants/events
    - `expression_index.integer minimum 0`
    - `expression_name.string default ""`
    - `source.enum == ["olvt_emotionMap", "manual"]`

    Update `sidecar/src/sidecar/avatar/extractors/olvt.py`:
    - Remove the "emotionMap is intentionally ignored" comment.
    - Import `DefaultPluginActionBinding`.
    - Change the return type to `tuple[list[VariantEntry], list[EventEntry], list[ImportWarning], list[DefaultPluginActionBinding]]`.
    - Continue reading `actionMap` exactly as today for variants.
    - Also read `emotionMap` from the same selected model and create one binding per item using `action_code=slug_from_hotkey_name(str(code))`, `expression_index=int(value)`, `expression_name=""`, `source="olvt_emotionMap"`, `plugin_name="default"`.
    - Return `(variants, [], [], bindings)`.

    Update `sidecar/src/sidecar/admin/avatar.py`:
    - For `AvatarType.OLVT`, call the 4-tuple OLVT extractor and set `AvatarImportPlan.default_plugin_action_bindings=bindings`.
    - For non-OLVT extractors, keep existing 3-tuple behavior and set bindings to `[]`.
    - In `commit_avatar`, pass `default_plugin_action_bindings=plan.default_plugin_action_bindings` into `AvatarOverrides`.

    Update `sidecar/src/sidecar/avatar/rig_capabilities.py` so `build_rig_capabilities()` returns `default_plugin_action_bindings=overrides.default_plugin_action_bindings`.

    Update tests:
    - `sidecar/tests/avatar/test_extract_olvt.py` must unpack 4 values and assert the exact 8 binding pairs above.
    - `sidecar/tests/avatar/test_overrides_writer.py::_valid_overrides()` must include `default_plugin_action_bindings` with at least one valid row.
    - `sidecar/tests/avatar/test_rig_capabilities.py` must assert copied bindings.
    - `sidecar/tests/avatar/test_admin_avatar.py` must assert OLVT import returns bindings and commit persists them.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/avatar/test_extract_olvt.py tests/avatar/test_admin_avatar.py tests/avatar/test_overrides_writer.py tests/avatar/test_rig_capabilities.py -q --tb=short && cd .. && npm run check:contracts</automated>
  </verify>
  <done>
    OLVT import now preserves `emotionMap` bindings and `actionMap` variants; AvatarOverrides/RigCapabilities/schema/TS contracts all expose `default_plugin_action_bindings`.
  </done>
  <acceptance_criteria>
    - `grep -F "class DefaultPluginActionBinding" packages/contracts/py/contracts/action_binding.py` matches.
    - `grep -F "default_plugin_action_bindings" packages/contracts/py/contracts/avatar_overrides.py` matches.
    - `grep -F "default_plugin_action_bindings" packages/contracts/py/contracts/rig_capabilities.py` matches.
    - `grep -F "default_plugin_action_bindings" sidecar/schemas/avatar_overrides.schema.json` matches.
    - `grep -F "emotionMap is intentionally ignored" sidecar/src/sidecar/avatar/extractors/olvt.py` returns NO matches.
    - `grep -F "emotionMap" sidecar/src/sidecar/avatar/extractors/olvt.py` matches in code that reads the field.
    - `grep -F "DefaultPluginActionBinding" sidecar/src/sidecar/avatar/extractors/olvt.py` matches.
    - `grep -F "default_plugin_action_bindings" sidecar/src/sidecar/admin/avatar.py` matches.
    - `cd sidecar && uv run pytest tests/avatar/test_extract_olvt.py -q` exits 0.
    - `cd sidecar && uv run pytest tests/avatar/test_admin_avatar.py tests/avatar/test_overrides_writer.py tests/avatar/test_rig_capabilities.py -q` exits 0.
    - `npm run check:contracts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create executable dogfood and VTS evidence artifacts</name>
  <files>
    .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md (Human Verification Required section)
    - .planning/phases/08-avatar-import-catalogs/08-03-SUMMARY.md (manual dogfood not run)
    - sidecar/scripts/vts_introspect_smoke.py (exact command and PASS text)
    - apps/renderer/src/screens/Settings/Settings.tsx (Settings entrypoint label)
    - apps/renderer/src/screens/AvatarImport/AvatarImport.tsx (save flow and IPC methods)
  </read_first>
  <action>
    Create `.planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` with YAML frontmatter:
    ```yaml
    ---
    phase: 08-avatar-import-catalogs
    evidence: dogfood-native-dialog
    status: pending
    date: ""
    operator: ""
    ---
    ```
    Body must include this exact executable checklist:
    1. Run `npm run dev`.
    2. Open Electron Settings.
    3. Click `Edit avatar catalogs`.
    4. Click `Choose folder...`.
    5. Select `Live2D/重音テト/`.
    6. Verify the review screen shows variants from Teto; delete irrelevant/watermark rows if present; rename any invalid rows; verify Save is enabled.
    7. Click Save.
    8. Persist evidence fields: selected folder, number of variants before Save, rows renamed, rows deleted, resulting path, final status PASS/FAIL.
    9. Run `cd sidecar && uv run python -c "from pathlib import Path; import yaml, jsonschema, json; from sidecar.avatar.overrides_writer import _SCHEMA; p=Path('../avatars/teto/_avatar_overrides.yaml'); data=yaml.safe_load(p.read_text(encoding='utf-8')); jsonschema.validate(data, _SCHEMA); print('VALID', len(data.get('variants', [])), len(data.get('events', [])), len(data.get('default_plugin_action_bindings', [])))"` and paste output.

    Create `.planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md` with YAML frontmatter:
    ```yaml
    ---
    phase: 08-avatar-import-catalogs
    evidence: vts-introspection-smoke
    status: pending
    date: ""
    operator: ""
    ---
    ```
    Body must include this exact command and expectation:
    - Command: `cd sidecar && uv run python scripts/vts_introspect_smoke.py`
    - PASS evidence: exit code `0` and output containing `[SMOKE] PASS: pyvts 0.3.3 + VTS API "1.0" introspection shape verified`
    - BLOCKED evidence when VTube Studio is unavailable: exit code `3`, output containing `Is VTube Studio running?`, date/time attempted, and reason VTS/Teto was unavailable.
    - FAIL evidence: exit code `2`, full assertion mismatch output, and whether pyvts vendor patch is needed.
  </action>
  <verify>
    <automated>Test-Path .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md; Test-Path .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md; Select-String -Path .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md -Pattern "npm run dev","Live2D/重音テト","_avatar_overrides.yaml","jsonschema.validate"; Select-String -Path .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md -Pattern "uv run python scripts/vts_introspect_smoke.py","[SMOKE] PASS","Is VTube Studio running?" -SimpleMatch</automated>
  </verify>
  <done>
    Manual evidence files exist and contain exact commands, expected outputs, and fields the executor/operator must complete before Phase 8 can verify.
  </done>
  <acceptance_criteria>
    - `grep -F "status: pending" .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` matches before human execution.
    - `grep -F "Live2D/重音テト/" .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` matches.
    - `grep -F "jsonschema.validate" .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` matches.
    - `grep -F "uv run python scripts/vts_introspect_smoke.py" .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md` matches.
    - `grep -F "[SMOKE] PASS" .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md` matches.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run native-dialog Teto dogfood and VTS smoke, then persist evidence</name>
  <files>
    avatars/teto/_avatar_overrides.yaml,
    .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
    - .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
    - .planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md
    - avatars/teto/teto_overrides.yaml (legacy source, if still present)
    - sidecar/scripts/vts_introspect_smoke.py
  </read_first>
  <action>
    Human/operator must complete the `08-DOGFOOD-UAT.md` checklist through the real Electron native folder dialog, selecting `Live2D/重音テト/`, saving the review screen, and ensuring the final artifact is committed at `avatars/teto/_avatar_overrides.yaml`.

    If the UI writes `_avatar_overrides.yaml` beside `Live2D/重音テト/`, copy that YAML to `avatars/teto/_avatar_overrides.yaml` only after preserving `source_rig_path: Live2D/重音テト` and validating it with the command in `08-DOGFOOD-UAT.md`. Do not fabricate the YAML without running the native-dialog flow; the evidence file must say which path the UI wrote.

    Human/operator must also run the VTS smoke command if VTube Studio with Teto is available. If unavailable, update `08-VTS-SMOKE-UAT.md` to `status: blocked`, include the attempted command, exit code `3`, output, date, and reason. If available, update it to `status: pass` and paste the PASS output.

    Update both evidence files from `status: pending` to `status: pass` or `status: blocked` with concrete evidence. `08-DOGFOOD-UAT.md` must be `status: pass`; VTS smoke may be `status: blocked` only if VTube Studio/Teto is unavailable and the blocked evidence is complete.
  </action>
  <verify>
    <automated>Test-Path avatars/teto/_avatar_overrides.yaml; Select-String -Path avatars/teto/_avatar_overrides.yaml -Pattern "variants:","events:","default_plugin_action_bindings:","source_rig_path:"; Select-String -Path .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md -Pattern "status: pass"; Select-String -Path .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md -Pattern "status: pass","status: blocked"; cd sidecar; uv run python -c "from pathlib import Path; import yaml, jsonschema; from sidecar.avatar.overrides_writer import _SCHEMA; p=Path('../avatars/teto/_avatar_overrides.yaml'); data=yaml.safe_load(p.read_text(encoding='utf-8')); jsonschema.validate(data, _SCHEMA); assert 'default_plugin_action_bindings' in data; print('VALID', len(data.get('variants', [])), len(data.get('events', [])), len(data.get('default_plugin_action_bindings', [])))"</automated>
  </verify>
  <done>
    `avatars/teto/_avatar_overrides.yaml` exists and validates; dogfood evidence is PASS; VTS smoke evidence is PASS or explicitly BLOCKED with command/output/date/reason.
  </done>
  <acceptance_criteria>
    - `Test-Path avatars/teto/_avatar_overrides.yaml` returns True.
    - `grep -F "default_plugin_action_bindings:" avatars/teto/_avatar_overrides.yaml` matches.
    - `grep -F "status: pass" .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` matches.
    - `.planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` records selected folder, variants before Save, rows renamed, rows deleted, resulting path, validation output, and PASS status.
    - `.planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md` records either PASS output from `vts_introspect_smoke.py` or BLOCKED exit-code-3 evidence with date and reason.
    - `cd sidecar && uv run python -c "from pathlib import Path; import yaml, jsonschema; from sidecar.avatar.overrides_writer import _SCHEMA; data=yaml.safe_load(Path('../avatars/teto/_avatar_overrides.yaml').read_text(encoding='utf-8')); jsonschema.validate(data, _SCHEMA); assert 'default_plugin_action_bindings' in data; print('OK')"` exits 0.
  </acceptance_criteria>
  <how-to-verify>
    Run the exact steps in `08-DOGFOOD-UAT.md` and `08-VTS-SMOKE-UAT.md`. Resume execution only after the evidence files and `avatars/teto/_avatar_overrides.yaml` are updated.
  </how-to-verify>
  <resume-signal>Type "approved" after evidence files and YAML artifact are present, or describe the failed/blocked step.</resume-signal>
</task>

</tasks>

<verification>
- `cd sidecar && uv run pytest tests/avatar/test_extract_olvt.py tests/avatar/test_admin_avatar.py tests/avatar/test_overrides_writer.py tests/avatar/test_rig_capabilities.py -q --tb=short`
- `npm run check:contracts`
- `Test-Path avatars/teto/_avatar_overrides.yaml`
- `grep -F "default_plugin_action_bindings:" avatars/teto/_avatar_overrides.yaml`
- `grep -F "status: pass" .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md`
- `grep -E "status: (pass|blocked)" .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md`
</verification>

<success_criteria>
- IMP-05 is closed by implementation: OLVT `emotionMap` produces default-plugin action bindings and `actionMap` still produces variants.
- IMP-09/ARCH-02 are closed by contract: `AvatarOverrides` persists bindings and `RigCapabilities` exposes them to Phase 6/9 consumers.
- IMP-08 manual gap is closed by evidence: native-dialog Teto dogfood produces `avatars/teto/_avatar_overrides.yaml` and `08-DOGFOOD-UAT.md` records PASS.
- IMP-10 evidence gap is closed or explicitly blocked: `08-VTS-SMOKE-UAT.md` records PASS against live Teto or BLOCKED with command/output/date/reason.
</success_criteria>

<output>
After completion, create `.planning/phases/08-avatar-import-catalogs/08-04-SUMMARY.md` documenting:
- The new `default_plugin_action_bindings` contract and generated files.
- The exact OLVT emotionMap bindings verified from local model_dict.json.
- The dogfood evidence status and path to `avatars/teto/_avatar_overrides.yaml`.
- The VTS smoke result: PASS or BLOCKED with reason.
</output>
