---
phase: 08-avatar-import-catalogs
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - sidecar/src/sidecar/admin/avatar.py
  - sidecar/tests/avatar/test_admin_avatar.py
  - sidecar/tests/avatar/test_reimport.py
  - apps/renderer/src/screens/AvatarImport/AvatarImport.tsx
  - apps/renderer/src/screens/AvatarImport/AvatarImport.module.css
  - apps/renderer/tests/AvatarImport.test.tsx
  - sidecar/scripts/vts_introspect_smoke.py
  - sidecar/tests/vts/test_vts_introspect_smoke.py
  - .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
  - .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  - avatars/teto/_avatar_overrides.yaml
autonomous: false
gap_closure: true
requirements:
  - IMP-05
  - IMP-08
  - IMP-09
  - IMP-10
  - ARCH-02

must_haves:
  truths:
    - "Saving a native-dialog Teto import writes the runtime override to avatars/teto/_avatar_overrides.yaml, not to the selected external Live2D source folder."
    - "The saved runtime override still preserves source_rig_path as provenance metadata for the selected external rig folder."
    - "Avatar import Save/Cancel controls sit naturally at the bottom of the page without a sticky footer banner."
    - "VTS introspection smoke uses a stable AgenticLLMVTuber plugin identity/token path and fails before HotkeyList when authentication is rejected."
    - "Final evidence records dogfood PASS and VTS smoke PASS or complete BLOCKED/reauth instructions."
  artifacts:
    - path: "sidecar/src/sidecar/admin/avatar.py"
      provides: "Commit destination policy: app-managed avatars/<avatar_id>/_avatar_overrides.yaml"
      contains: "AGENTICLLMVTUBER_REPO_ROOT"
    - path: "sidecar/tests/avatar/test_admin_avatar.py"
      provides: "Regression tests for runtime save destination and source folder non-mutation"
      contains: "avatars/teto/_avatar_overrides.yaml"
    - path: "apps/renderer/src/screens/AvatarImport/AvatarImport.module.css"
      provides: "Non-sticky bottom-of-page import action layout"
      contains: ".footer"
    - path: "apps/renderer/tests/AvatarImport.test.tsx"
      provides: "Renderer assertion that footer actions are regular bottom controls"
      contains: "footer"
    - path: "sidecar/scripts/vts_introspect_smoke.py"
      provides: "Stable VTS auth identity, fail-fast auth handling, and reset guidance"
      contains: "request_authenticate"
    - path: "sidecar/tests/vts/test_vts_introspect_smoke.py"
      provides: "Script-level auth rejection and HotkeyList prevention tests"
      contains: "HotkeyList"
  key_links:
    - from: "apps/renderer/src/screens/AvatarImport/AvatarImport.tsx"
      to: "sidecar/src/sidecar/admin/avatar.py"
      via: "window.api.commitAvatarOverrides posts AvatarImportPlan unchanged through Electron IPC"
      pattern: "commitAvatarOverrides"
    - from: "sidecar/src/sidecar/admin/avatar.py"
      to: "avatars/<avatar_id>/_avatar_overrides.yaml"
      via: "commit derives destination from repo root plus plan.avatar_id, not plan.source_rig_path"
      pattern: "avatars.*avatar_id.*_avatar_overrides.yaml"
    - from: "sidecar/scripts/vts_introspect_smoke.py"
      to: "VTube Studio HotkeyList"
      via: "HotkeyList only after request_authenticate() returns true/authenticated"
      pattern: "if not authenticated"
---

<objective>
Close the diagnosed Phase 8 UAT gaps without waiting behind the stale 08-05 human-action checkpoint.

Purpose: The original 08-05 automated sidecar binding work is already complete, but UAT found new blocker/cosmetic gaps before the final evidence gate. This revised gap-closure plan fixes the save destination policy, footer layout, and VTS smoke authentication behavior first, then reruns the final human evidence gate against the corrected behavior.

Output: App-managed avatar override writes, updated save-path tests, non-sticky import footer layout, renderer layout assertion, VTS smoke fail-fast auth handling/tests, updated evidence templates, and final dogfood/VTS evidence.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-avatar-import-catalogs/08-UAT.md
@.planning/debug/phase-8-uat-save-path.md
@.planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md
@.planning/phases/08-avatar-import-catalogs/08-04-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
@.planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md

<interfaces>
From `sidecar/src/sidecar/admin/avatar.py`:
```python
@router.post("/import/commit", response_model=CommitResponse)
async def commit_avatar(plan: AvatarImportPlan) -> CommitResponse:
    target_dir = Path(plan.source_rig_path)
    ...
    target = target_dir / "_avatar_overrides.yaml"
```
This is the diagnosed bug. `source_rig_path` is provenance metadata and must remain in YAML, but it is not the persistence destination.

From `AvatarImportPlan`/08-04 contracts:
```text
avatar_id: optional string identifying the app-managed avatar directory
source_rig_path: selected external source rig folder
default_plugin_action_bindings: DefaultPluginActionBinding[]
```

Runtime destination policy for this plan:
```text
repo_root = AGENTICLLMVTUBER_REPO_ROOT if set, else current working directory
avatar_id = plan.avatar_id or "teto"
target = repo_root / "avatars" / avatar_id / "_avatar_overrides.yaml"
```

Do not edit, delete, or normalize these unrelated dirty/user files:
```text
avatars/teto/teto_overrides.yaml
Live2D/重音テト/_avatar_overrides.yaml
backup/
.planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md
.planning/phases/06-plugin-runtime-default-plugin/06-VERIFICATION.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix avatar override commit destination policy</name>
  <files>
    sidecar/src/sidecar/admin/avatar.py,
    sidecar/tests/avatar/test_admin_avatar.py,
    sidecar/tests/avatar/test_reimport.py
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-UAT.md
    - .planning/debug/phase-8-uat-save-path.md
    - sidecar/src/sidecar/admin/avatar.py
    - sidecar/tests/avatar/test_admin_avatar.py
    - sidecar/tests/avatar/test_reimport.py
  </read_first>
  <behavior>
    - Test 1: POST `/admin/avatar/import/commit` with `avatar_id="teto"` and `source_rig_path` pointing at an external temp source folder writes `AGENTICLLMVTUBER_REPO_ROOT/avatars/teto/_avatar_overrides.yaml`.
    - Test 2: The same commit does not create or overwrite `<source_rig_path>/_avatar_overrides.yaml`.
    - Test 3: The written YAML contains `source_rig_path` equal to the selected external source folder string.
    - Test 4: Validation failure leaves both runtime destination and external source folder without `_avatar_overrides.yaml` or `.tmp` files.
    - Test 5: Re-edit/current-avatar behavior still loads `avatars/<avatar_id>/_avatar_overrides.yaml` and preserves `source_rig_path`.
  </behavior>
  <action>
    Update tests first. Replace the existing wrong expectation in `test_commit_writes_yaml` that reads from `tmp_path / "_avatar_overrides.yaml"`. Use `monkeypatch.setenv("AGENTICLLMVTUBER_REPO_ROOT", str(tmp_path / "repo"))`, create `repo/avatars/teto`, create a separate `external_source` folder for `source_rig_path`, and assert the saved file is `repo/avatars/teto/_avatar_overrides.yaml`. Assert `external_source/_avatar_overrides.yaml` and `.tmp` do not exist. Assert the saved YAML keeps `source_rig_path` exactly as `str(external_source)` and keeps `default_plugin_action_bindings`.

    Update validation-failure tests the same way: the invalid payload must not create runtime or external `_avatar_overrides.yaml` artifacts.

    If `test_reimport.py` encodes same-folder save assumptions, update it to distinguish app-managed avatar dir from external source dir.

    Update `sidecar/src/sidecar/admin/avatar.py::commit_avatar`:
    - Validate `Path(plan.source_rig_path)` exists and is a directory, but do not write there.
    - Resolve `repo_root = Path(os.environ.get("AGENTICLLMVTUBER_REPO_ROOT", os.getcwd()))`.
    - Resolve `avatar_id = plan.avatar_id or "teto"` to preserve existing fallback behavior.
    - Write to `repo_root / "avatars" / avatar_id / "_avatar_overrides.yaml"`, creating the avatar directory if missing.
    - Continue to store `source_rig_path=plan.source_rig_path` inside `AvatarOverrides`.
    - Return `CommitResponse.path` as the runtime destination.
    - Do not touch `avatars/teto/teto_overrides.yaml` or any existing `Live2D/重音テト/_avatar_overrides.yaml`.
  </action>
  <verify>
    <automated>cd sidecar; uv run pytest tests/avatar/test_admin_avatar.py tests/avatar/test_reimport.py -q --tb=short; if ($LASTEXITCODE -ne 0) { throw "avatar admin save-path tests failed with exit code $LASTEXITCODE" }; cd ..; if (Select-String -Path sidecar/src/sidecar/admin/avatar.py -Pattern "target_dir = Path(plan.source_rig_path)" -SimpleMatch -Quiet) { throw "commit_avatar still uses source_rig_path as write destination" }; if (!(Select-String -Path sidecar/tests/avatar/test_admin_avatar.py -Pattern "avatars/teto/_avatar_overrides.yaml" -SimpleMatch -Quiet)) { throw "missing explicit runtime path assertion" }; if (!(Select-String -Path sidecar/tests/avatar/test_admin_avatar.py -Pattern "source_rig_path" -SimpleMatch -Quiet)) { throw "missing source_rig_path preservation assertion" }</automated>
  </verify>
  <done>
    Commit writes validated YAML to app-managed `avatars/<avatar_id>/_avatar_overrides.yaml`, never to the selected external source folder, while preserving `source_rig_path` in the YAML.
  </done>
  <acceptance_criteria>
    - Runtime output path is `avatars/teto/_avatar_overrides.yaml` for the Teto UAT flow.
    - No code path writes `_avatar_overrides.yaml` into `plan.source_rig_path`.
    - Tests fail if `source_rig_path` is used as the destination again.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix import footer layout and VTS smoke authentication</name>
  <files>
    apps/renderer/src/screens/AvatarImport/AvatarImport.tsx,
    apps/renderer/src/screens/AvatarImport/AvatarImport.module.css,
    apps/renderer/tests/AvatarImport.test.tsx,
    sidecar/scripts/vts_introspect_smoke.py,
    sidecar/tests/vts/test_vts_introspect_smoke.py,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-UAT.md
    - apps/renderer/src/screens/AvatarImport/AvatarImport.tsx
    - apps/renderer/src/screens/AvatarImport/AvatarImport.module.css
    - apps/renderer/tests/AvatarImport.test.tsx
    - sidecar/scripts/vts_introspect_smoke.py
    - sidecar/src/sidecar/vts/handshake.py
    - sidecar/tests/vts/test_pyvts_writer.py
  </read_first>
  <behavior>
    - Test 1: Renderer test asserts the Save/Cancel action container is a regular page footer with no `position: sticky`, no `bottom: 0`, and no dedicated banner/background treatment.
    - Test 2: Save-disabled placeholder message remains visible/clickable when placeholders remain.
    - Test 3: `vts_introspect_smoke.py` uses the stable app plugin identity, not a phase-specific plugin name with a shared token.
    - Test 4: When `request_authenticate()` returns false or an AuthenticationResponse with `authenticated=False`, the smoke exits `2`, prints a reset/re-auth instruction naming the token file, and does not request HotkeyList.
    - Test 5: Connection-refused behavior remains exit `3` with `Is VTube Studio running?`.
  </behavior>
  <action>
    Footer/layout:
    - In `AvatarImport.module.css`, remove sticky banner behavior from `.footer`: no `position: sticky`, no `bottom: 0`, no dedicated `background: var(--background)` banner treatment.
    - Keep `.footer` as a normal bottom-of-page flex row after the tables. Use simple top spacing/padding only if needed for visual separation.
    - Keep `.footerActions` as the Save/Cancel button row.
    - If useful for automated DOM assertions, add stable `data-testid` attributes to the footer and action row in `AvatarImport.tsx`; do not add visible explanatory copy.
    - Add/update `apps/renderer/tests/AvatarImport.test.tsx` so it proves Save and Cancel render in the regular footer and so a static CSS assertion fails if `.footer` regains `position: sticky`, `bottom: 0`, or a banner background.

    VTS smoke auth:
    - In `sidecar/scripts/vts_introspect_smoke.py`, replace the phase-specific plugin name with the stable AgenticLLMVTuber identity used for normal VTS auth. Use one stable token path for that identity; do not mix a new plugin name with `sidecar/.vts_token.txt`.
    - Capture the return value from `await client.request_authenticate()`. Treat `False`, `None` with an AuthenticationResponse payload showing `authenticated=False`, or any equivalent pyvts false-auth result as a hard failure before authenticated requests.
    - On auth failure, print a clear `[SMOKE] FAIL: VTube Studio authentication failed...` message that includes the token path and these reset steps: close the app/smoke, delete the token file, open VTube Studio settings > Plugins, remove/revoke the stale AgenticLLMVTuber plugin entry if present, rerun the smoke/app, and approve the VTS plugin permission prompt.
    - Return exit code `2` for auth rejection. Do not issue `APIStateRequest`, `HotkeyList`, or `TrackingParameterList` after failed auth.
    - Preserve exit code `3` for VTube Studio unavailable/connection-refused.
    - Add `sidecar/tests/vts/test_vts_introspect_smoke.py` with fake pyvts clients or monkeypatching to prove auth rejection fails before HotkeyList and connection-refused still returns `3`.
    - Update `08-VTS-SMOKE-UAT.md` to include the reset/re-auth steps for real VTS auth failures.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- --run AvatarImport; if ($LASTEXITCODE -ne 0) { throw "renderer AvatarImport tests failed with exit code $LASTEXITCODE" }; $css = Get-Content -Raw apps/renderer/src/screens/AvatarImport/AvatarImport.module.css; $footer = [regex]::Match($css, "(?s)\\.footer\\s*\\{.*?\\}").Value; if ($footer -match "position\\s*:\\s*sticky") { throw "AvatarImport footer is still sticky" }; if ($footer -match "bottom\\s*:\\s*0") { throw "AvatarImport footer still pins to bottom" }; if ($footer -match "background\\s*:") { throw "AvatarImport footer still has banner background treatment" }; cd sidecar; uv run pytest tests/vts/test_vts_introspect_smoke.py -q --tb=short; if ($LASTEXITCODE -ne 0) { throw "VTS smoke auth tests failed with exit code $LASTEXITCODE" }; cd ..; if (!(Select-String -Path sidecar/scripts/vts_introspect_smoke.py -Pattern "request_authenticate()" -SimpleMatch -Quiet)) { throw "smoke script does not inspect request_authenticate" }; if (!(Select-String -Path sidecar/scripts/vts_introspect_smoke.py -Pattern "HotkeyList" -SimpleMatch -Quiet)) { throw "smoke script no longer checks HotkeyList shape" }; if (!(Select-String -Path .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md -Pattern "delete the token file" -SimpleMatch -Quiet)) { throw "VTS UAT lacks reset instructions" }</automated>
  </verify>
  <done>
    Save/Cancel controls are regular bottom-of-page controls, and VTS smoke auth either succeeds before HotkeyList or fails loudly with reset/re-auth instructions before any authenticated request.
  </done>
  <acceptance_criteria>
    - No sticky footer/banner styling remains for AvatarImport Save/Cancel.
    - Auth failure cannot be misreported as a HotkeyList shape failure.
    - Real VTS reset steps are documented where the operator records smoke evidence.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rerun native-dialog dogfood and VTS smoke evidence after fixes</name>
  <files>
    avatars/teto/_avatar_overrides.yaml,
    .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <read_first>
    - .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
    - .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
    - .planning/phases/08-avatar-import-catalogs/08-UAT.md
  </read_first>
  <action>
    Complete the corrected evidence gate only after Tasks 1 and 2 pass.

    Native-dialog dogfood:
    - Run `npm run dev`.
    - Open Settings and click `Edit avatar catalogs`.
    - Choose `Live2D/重音テト/` through the native folder dialog.
    - Review the extracted catalog, rename at least one row, delete at least one row, and Save.
    - Confirm the returned/saved path is `avatars/teto/_avatar_overrides.yaml`.
    - Confirm `Live2D/重音テト/_avatar_overrides.yaml` was not newly written or modified by this save. If an old dirty file already exists there, leave it alone and record that it was not the target.
    - Update `08-DOGFOOD-UAT.md` to `status: pass` with selected folder, variants before Save, rows renamed, rows deleted, resulting path, validation command, and validation output.

    VTS smoke:
    - Run `cd sidecar && uv run python scripts/vts_introspect_smoke.py` with VTube Studio and Teto available when possible.
    - If VTS auth fails, follow the documented reset/re-auth steps in `08-VTS-SMOKE-UAT.md`, rerun the smoke, and record the final result.
    - If VTS/Teto is genuinely unavailable, record `status: blocked`, date/time attempted, attempted command, exit code `3`, output containing `Is VTube Studio running?`, and reason.
    - If VTS is available and auth succeeds, record `status: pass`, attempted command, exit code `0`, and PASS output.
  </action>
  <verify>
    <automated>if (!(Test-Path "avatars/teto/_avatar_overrides.yaml")) { throw "Missing avatars/teto/_avatar_overrides.yaml" }; foreach ($pattern in @("variants:","events:","default_plugin_action_bindings:","source_rig_path:")) { if (!(Select-String -Path "avatars/teto/_avatar_overrides.yaml" -Pattern $pattern -SimpleMatch -Quiet)) { throw "Missing '$pattern' in avatars/teto/_avatar_overrides.yaml" } }; cd sidecar; uv run python -c "from pathlib import Path; import yaml, jsonschema; from sidecar.avatar.overrides_writer import _SCHEMA; p=Path('../avatars/teto/_avatar_overrides.yaml'); data=yaml.safe_load(p.read_text(encoding='utf-8')); jsonschema.validate(data, _SCHEMA); assert data['source_rig_path']; assert 'default_plugin_action_bindings' in data; print('OK')"; if ($LASTEXITCODE -ne 0) { throw "avatar overrides schema validation failed with exit code $LASTEXITCODE" }; cd ..; if (!(Select-String -Path ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md" -Pattern "status: pass" -SimpleMatch -Quiet)) { throw "Dogfood UAT is not status: pass" }; foreach ($pattern in @("selected folder","variants before Save","rows renamed","rows deleted","resulting path","validation output","avatars/teto/_avatar_overrides.yaml")) { if (!(Select-String -Path ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md" -Pattern $pattern -SimpleMatch -Quiet)) { throw "Dogfood UAT missing evidence field: $pattern" } }; $dogfood = Get-Content -Raw ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md"; if ($dogfood -notmatch "Live2D/重音テト") { throw "Dogfood evidence must name selected source folder" }; $vts = Get-Content -Raw ".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"; if ($vts -match "status: pass") { foreach ($pattern in @('exit code `0`',"[SMOKE] PASS","uv run python scripts/vts_introspect_smoke.py","output")) { if ($vts -notlike "*$pattern*") { throw "VTS PASS evidence missing: $pattern" } } } elseif ($vts -match "status: blocked") { foreach ($pattern in @("date/time attempted","uv run python scripts/vts_introspect_smoke.py",'exit code `3`',"Is VTube Studio running?","output","reason")) { if ($vts -notlike "*$pattern*") { throw "VTS BLOCKED evidence missing: $pattern" } } } else { throw "VTS smoke evidence must be status: pass or status: blocked after reset/re-auth attempts" }</automated>
  </verify>
  <done>
    `avatars/teto/_avatar_overrides.yaml` exists and validates, dogfood evidence is PASS for the corrected destination path, and VTS evidence is PASS or explicitly BLOCKED with complete evidence.
  </done>
  <acceptance_criteria>
    - Native-dialog save proves app-managed runtime path, not external source-folder mutation.
    - Footer visual issue has been checked during dogfood.
    - VTS smoke either passes after stable-token re-auth or documents an environment block with exit code 3.
  </acceptance_criteria>
  <how-to-verify>
    Complete the action list above and run the automated verification block. The checkpoint is not complete until the automated verification passes.
  </how-to-verify>
  <resume-signal>Type "done" after the corrected YAML artifact and both evidence files are present, updated, and pass the automated verification.</resume-signal>
</task>

</tasks>

<verification>
Run these automated checks before the final checkpoint:
```powershell
cd sidecar
uv run pytest tests/avatar/test_admin_avatar.py tests/avatar/test_reimport.py tests/vts/test_vts_introspect_smoke.py -q --tb=short
if ($LASTEXITCODE -ne 0) { throw "sidecar gap tests failed" }
cd ..
npm --workspace apps/renderer run test -- --run AvatarImport
if ($LASTEXITCODE -ne 0) { throw "renderer AvatarImport tests failed" }
$css = Get-Content -Raw apps/renderer/src/screens/AvatarImport/AvatarImport.module.css
$footer = [regex]::Match($css, "(?s)\.footer\s*\{.*?\}").Value
if ($footer -match "position\s*:\s*sticky|bottom\s*:\s*0|background\s*:") { throw "footer still has sticky/banner styling" }
```

Run the final checkpoint verification after dogfood/VTS evidence is updated.
</verification>

<success_criteria>
- IMP-08/IMP-09 save policy gap is closed: app-managed `avatars/<avatar_id>/_avatar_overrides.yaml` is the only commit destination, with `source_rig_path` preserved as metadata.
- The UAT cosmetic footer gap is closed: Save/Cancel controls are normal bottom-of-page controls, not a sticky banner.
- IMP-10 VTS smoke gap is closed or actionable: stable token identity is used, auth rejection fails before HotkeyList, and reset/re-auth steps are documented for real VTS.
- Final evidence no longer depends on the stale pre-gap 08-05 checkpoint wording.
</success_criteria>

<output>
After completion, create `.planning/phases/08-avatar-import-catalogs/08-05-SUMMARY.md` documenting the save-destination fix, footer layout fix, VTS smoke auth/token behavior, dogfood evidence status, `avatars/teto/_avatar_overrides.yaml`, and the VTS smoke PASS/BLOCKED result.
</output>
