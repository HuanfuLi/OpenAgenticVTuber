---
phase: 08-avatar-import-catalogs
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - sidecar/src/sidecar/avatar/extractors/olvt.py
  - sidecar/src/sidecar/avatar/rig_capabilities.py
  - sidecar/src/sidecar/admin/avatar.py
  - sidecar/tests/avatar/test_extract_olvt.py
  - sidecar/tests/avatar/test_overrides_writer.py
  - sidecar/tests/avatar/test_rig_capabilities.py
  - sidecar/tests/avatar/test_admin_avatar.py
  - avatars/teto/_avatar_overrides.yaml
  - .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md
  - .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
autonomous: false
gap_closure: true
requirements:
  - IMP-05
  - IMP-08
  - IMP-09
  - IMP-10
  - ARCH-02

must_haves:
  truths:
    - "OLVT model_dict.json import preserves actionMap as variants and emotionMap as default-plugin action-code bindings."
    - "Admin import/commit and RigCapabilities carry default_plugin_action_bindings end to end."
    - "A dogfooded avatars/teto/_avatar_overrides.yaml exists, validates, and contains variants/events/default_plugin_action_bindings/source_rig_path."
    - "VTS introspection evidence is either PASS or BLOCKED with date, command, exit code 3, output, and reason."
  artifacts:
    - path: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      provides: "emotionMap ingestion plus existing actionMap variant catalog"
      contains: "emotionMap"
    - path: "sidecar/src/sidecar/admin/avatar.py"
      provides: "Import and commit propagation for default_plugin_action_bindings"
      contains: "default_plugin_action_bindings"
    - path: "sidecar/src/sidecar/avatar/rig_capabilities.py"
      provides: "RigCapabilities builder copies persisted bindings"
      contains: "default_plugin_action_bindings"
    - path: "avatars/teto/_avatar_overrides.yaml"
      provides: "Dogfooded Teto overrides artifact"
      contains: "default_plugin_action_bindings"
    - path: ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md"
      provides: "Native-dialog dogfood evidence"
      contains: "status: pass"
    - path: ".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"
      provides: "VTS smoke PASS or complete BLOCKED evidence"
      contains: "status:"
  key_links:
    - from: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      to: "packages/contracts/py/contracts/action_binding.py"
      via: "extract_olvt reads emotionMap into DefaultPluginActionBinding(action_code, expression_index)"
      pattern: "DefaultPluginActionBinding"
    - from: "sidecar/src/sidecar/admin/avatar.py"
      to: "sidecar/src/sidecar/avatar/extractors/olvt.py"
      via: "OLVT import branch stores default_plugin_action_bindings on AvatarImportPlan"
      pattern: "default_plugin_action_bindings"
    - from: "sidecar/src/sidecar/avatar/rig_capabilities.py"
      to: "packages/contracts/py/contracts/rig_capabilities.py"
      via: "build_rig_capabilities copies bindings from AvatarOverrides"
      pattern: "default_plugin_action_bindings"
---

<objective>
Close the sidecar integration and evidence portion of the Phase 8 verification gap.

Purpose: Plan 08-04 creates the binding contracts. This plan wires those contracts through OLVT import, admin commit, and RigCapabilities, then captures the dogfood and VTS evidence the verifier requires.

Output: OLVT `emotionMap` binding extraction, admin import/commit propagation, RigCapabilities copy behavior, updated sidecar tests, `avatars/teto/_avatar_overrides.yaml`, and completed dogfood/VTS UAT artifacts.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md
@.planning/phases/08-avatar-import-catalogs/08-04-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-01-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-02-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-03-SUMMARY.md

<interfaces>
From 08-04, use `DefaultPluginActionBinding` and the `default_plugin_action_bindings` fields on `AvatarOverrides`, `AvatarImportPlan`, and `RigCapabilities`. Do not invent a separate binding shape.

From OLVT local reference `C:/Users/16079/Code/OpenLLM_Vtuber/model_dict.json`, expected `emotionMap` pairs are:
```text
neutral=0, anger=2, disgust=2, fear=1, joy=3, smirk=3, sadness=1, surprise=3
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire OLVT emotionMap through import, commit, and RigCapabilities</name>
  <files>
    sidecar/src/sidecar/avatar/extractors/olvt.py,
    sidecar/src/sidecar/avatar/rig_capabilities.py,
    sidecar/src/sidecar/admin/avatar.py,
    sidecar/tests/avatar/test_extract_olvt.py,
    sidecar/tests/avatar/test_overrides_writer.py,
    sidecar/tests/avatar/test_rig_capabilities.py,
    sidecar/tests/avatar/test_admin_avatar.py
  </files>
  <behavior>
    - Test 1: `extract_olvt(C:/Users/16079/Code/OpenLLM_Vtuber/model_dict.json)` returns the existing 6 variants from `actionMap` unchanged.
    - Test 2: The same extractor returns 8 `DefaultPluginActionBinding` rows from `emotionMap`: neutral=0, anger=2, disgust=2, fear=1, joy=3, smirk=3, sadness=1, surprise=3.
    - Test 3: `/admin/avatar/import` for an OLVT folder returns `default_plugin_action_bindings` in the AvatarImportPlan response.
    - Test 4: `/admin/avatar/import/commit` writes `_avatar_overrides.yaml` with `default_plugin_action_bindings` and excludes wire-only fields.
    - Test 5: `build_rig_capabilities()` copies `AvatarOverrides.default_plugin_action_bindings` into `RigCapabilities.default_plugin_action_bindings`.
  </behavior>
  <action>
    Update `sidecar/src/sidecar/avatar/extractors/olvt.py`:
    - Remove the "emotionMap is intentionally ignored" comment.
    - Import `DefaultPluginActionBinding`.
    - Change the return contract to include bindings while preserving existing variants/events/warnings behavior.
    - Read `emotionMap` from the selected model and create one binding per item using `action_code=str(key)`, `expression_index=int(value)`, `expression_name=""`, `source="olvt_emotionMap"`, `plugin_name="default"`.

    Update `sidecar/src/sidecar/admin/avatar.py`:
    - For `AvatarType.OLVT`, unpack the OLVT extractor binding return and set `AvatarImportPlan.default_plugin_action_bindings=bindings`.
    - For non-OLVT extractors, preserve current behavior and set bindings to `[]`.
    - In commit, pass `default_plugin_action_bindings=plan.default_plugin_action_bindings` into `AvatarOverrides`.

    Update `sidecar/src/sidecar/avatar/rig_capabilities.py` so `build_rig_capabilities()` returns `default_plugin_action_bindings=overrides.default_plugin_action_bindings`.

    Update sidecar tests to assert the exact OLVT binding pairs, admin import/commit propagation, writer schema acceptance, and RigCapabilities copy behavior.
  </action>
  <verify>
    <automated>cd sidecar; uv run pytest tests/avatar/test_extract_olvt.py tests/avatar/test_admin_avatar.py tests/avatar/test_overrides_writer.py tests/avatar/test_rig_capabilities.py -q --tb=short; if ($LASTEXITCODE -ne 0) { throw "sidecar avatar binding tests failed with exit code $LASTEXITCODE" }; cd ..; $required = @(@{Path="sidecar/src/sidecar/avatar/extractors/olvt.py"; Pattern="DefaultPluginActionBinding"}, @{Path="sidecar/src/sidecar/avatar/extractors/olvt.py"; Pattern="emotionMap"}, @{Path="sidecar/src/sidecar/admin/avatar.py"; Pattern="default_plugin_action_bindings"}, @{Path="sidecar/src/sidecar/avatar/rig_capabilities.py"; Pattern="default_plugin_action_bindings"}); foreach ($item in $required) { if (!(Test-Path $item.Path)) { throw "Missing file: $($item.Path)" }; if (!(Select-String -Path $item.Path -Pattern $item.Pattern -SimpleMatch -Quiet)) { throw "Missing pattern '$($item.Pattern)' in $($item.Path)" } }; if (Select-String -Path sidecar/src/sidecar/avatar/extractors/olvt.py -Pattern "emotionMap is intentionally ignored" -SimpleMatch -Quiet) { throw "olvt.py still says emotionMap is intentionally ignored" }</automated>
  </verify>
  <done>
    Runtime sidecar paths preserve `emotionMap` bindings from OLVT import through commit and RigCapabilities.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dogfood and VTS evidence templates with asserting verification</name>
  <files>
    .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <action>
    Create `.planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md` with frontmatter `phase: 08-avatar-import-catalogs`, `evidence: dogfood-native-dialog`, `status: pending`, `date: ""`, and `operator: ""`. The body must instruct the operator to run `npm run dev`, open Settings, click `Edit avatar catalogs`, choose `Live2D/重音テト/`, review/rename/delete rows, save, and then validate `avatars/teto/_avatar_overrides.yaml` with jsonschema.

    Create `.planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md` with frontmatter `phase: 08-avatar-import-catalogs`, `evidence: vts-introspection-smoke`, `status: pending`, `date: ""`, and `operator: ""`. The body must include:
    - command: `cd sidecar && uv run python scripts/vts_introspect_smoke.py`
    - PASS evidence: exit code `0` and output containing `[SMOKE] PASS: pyvts 0.3.3 + VTS API "1.0" introspection shape verified`
    - BLOCKED evidence: exit code `3`, output containing `Is VTube Studio running?`, date/time attempted, attempted command, and reason VTS/Teto was unavailable
    - FAIL evidence: exit code `2`, full mismatch output, and whether a pyvts vendor patch is needed
  </action>
  <verify>
    <automated>$checks = @(@{Path=".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md"; Patterns=@("status: pending","npm run dev","Live2D/重音テト","avatars/teto/_avatar_overrides.yaml","jsonschema.validate")}, @{Path=".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"; Patterns=@("status: pending","uv run python scripts/vts_introspect_smoke.py","[SMOKE] PASS",'exit code `3`',"Is VTube Studio running?","output","reason")}); foreach ($check in $checks) { if (!(Test-Path $check.Path)) { throw "Missing evidence template: $($check.Path)" }; foreach ($pattern in $check.Patterns) { if (!(Select-String -Path $check.Path -Pattern $pattern -SimpleMatch -Quiet)) { throw "Missing pattern '$pattern' in $($check.Path)" } } }</automated>
  </verify>
  <done>
    Evidence templates exist with exact executable commands and fields required for later assertions.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run native-dialog Teto dogfood and VTS smoke, then persist evidence</name>
  <files>
    avatars/teto/_avatar_overrides.yaml,
    .planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md,
    .planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md
  </files>
  <action>
    Human/operator must complete the `08-DOGFOOD-UAT.md` checklist through the real Electron native folder dialog, selecting `Live2D/重音テト/`, saving the review screen, and ensuring the final artifact is committed at `avatars/teto/_avatar_overrides.yaml`.

    Human/operator must run the VTS smoke command when VTube Studio with Teto is available. If unavailable, update `08-VTS-SMOKE-UAT.md` to `status: blocked` and include date, attempted command, exit code `3`, output containing `Is VTube Studio running?`, and reason. If available, update it to `status: pass` with exit code `0` and PASS output.

    `08-DOGFOOD-UAT.md` must be `status: pass`. `08-VTS-SMOKE-UAT.md` may be `status: blocked` only with complete blocked evidence.
  </action>
  <verify>
    <automated>if (!(Test-Path "avatars/teto/_avatar_overrides.yaml")) { throw "Missing avatars/teto/_avatar_overrides.yaml" }; foreach ($pattern in @("variants:","events:","default_plugin_action_bindings:","source_rig_path:")) { if (!(Select-String -Path "avatars/teto/_avatar_overrides.yaml" -Pattern $pattern -SimpleMatch -Quiet)) { throw "Missing '$pattern' in avatars/teto/_avatar_overrides.yaml" } }; if (!(Select-String -Path ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md" -Pattern "status: pass" -SimpleMatch -Quiet)) { throw "Dogfood UAT is not status: pass" }; foreach ($pattern in @("selected folder","variants before Save","rows renamed","rows deleted","resulting path","validation output")) { if (!(Select-String -Path ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md" -Pattern $pattern -SimpleMatch -Quiet)) { throw "Dogfood UAT missing evidence field: $pattern" } }; $vts = Get-Content -Raw ".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"; if ($vts -match "status: pass") { foreach ($pattern in @('exit code `0`',"[SMOKE] PASS","uv run python scripts/vts_introspect_smoke.py","output")) { if ($vts -notlike "*$pattern*") { throw "VTS PASS evidence missing: $pattern" } } } elseif ($vts -match "status: blocked") { foreach ($pattern in @("date:","uv run python scripts/vts_introspect_smoke.py",'exit code `3`',"Is VTube Studio running?","output","reason")) { if ($vts -notlike "*$pattern*") { throw "VTS BLOCKED evidence missing: $pattern" } } } else { throw "VTS smoke evidence must be status: pass or status: blocked" }; cd sidecar; uv run python -c "from pathlib import Path; import yaml, jsonschema; from sidecar.avatar.overrides_writer import _SCHEMA; p=Path('../avatars/teto/_avatar_overrides.yaml'); data=yaml.safe_load(p.read_text(encoding='utf-8')); jsonschema.validate(data, _SCHEMA); assert 'default_plugin_action_bindings' in data; print('OK')"; if ($LASTEXITCODE -ne 0) { throw "avatar overrides schema validation failed with exit code $LASTEXITCODE" }</automated>
  </verify>
  <done>
    `avatars/teto/_avatar_overrides.yaml` exists and validates; dogfood evidence is PASS; VTS smoke evidence is PASS or explicitly BLOCKED with date, command, exit code 3, output, and reason.
  </done>
  <how-to-verify>
    Complete the actions recorded in `08-DOGFOOD-UAT.md` and `08-VTS-SMOKE-UAT.md`, then run the automated assertion block above.
  </how-to-verify>
  <resume-signal>Type "done" after the YAML artifact and both evidence files are present and updated.</resume-signal>
</task>

</tasks>

<verification>
Run:
```powershell
cd sidecar
uv run pytest tests/avatar/test_extract_olvt.py tests/avatar/test_admin_avatar.py tests/avatar/test_overrides_writer.py tests/avatar/test_rig_capabilities.py -q --tb=short
if ($LASTEXITCODE -ne 0) { throw "sidecar avatar integration tests failed" }
cd ..
if (!(Test-Path "avatars/teto/_avatar_overrides.yaml")) { throw "Missing dogfooded YAML" }
if (!(Select-String -Path ".planning/phases/08-avatar-import-catalogs/08-DOGFOOD-UAT.md" -Pattern "status: pass" -SimpleMatch -Quiet)) { throw "Dogfood UAT not pass" }
$vts = Get-Content -Raw ".planning/phases/08-avatar-import-catalogs/08-VTS-SMOKE-UAT.md"
if ($vts -match "status: blocked") {
  foreach ($pattern in @("date:","uv run python scripts/vts_introspect_smoke.py",'exit code `3`',"Is VTube Studio running?","output","reason")) {
    if ($vts -notlike "*$pattern*") { throw "Blocked VTS evidence missing $pattern" }
  }
} elseif ($vts -match "status: pass") {
  foreach ($pattern in @('exit code `0`',"[SMOKE] PASS","uv run python scripts/vts_introspect_smoke.py","output")) {
    if ($vts -notlike "*$pattern*") { throw "Pass VTS evidence missing $pattern" }
  }
} else {
  throw "VTS evidence must be pass or blocked"
}
```
</verification>

<success_criteria>
- IMP-05 is closed by implementation: OLVT `emotionMap` produces default-plugin action bindings and `actionMap` still produces variants.
- IMP-08 is closed by evidence: native-dialog Teto dogfood produces `avatars/teto/_avatar_overrides.yaml` and `08-DOGFOOD-UAT.md` records PASS.
- IMP-09/ARCH-02 are closed by runtime wiring: committed AvatarOverrides bindings reach RigCapabilities.
- IMP-10 is closed or explicitly blocked: `08-VTS-SMOKE-UAT.md` records PASS or BLOCKED with date, command, exit code `3`, output, and reason.
</success_criteria>

<output>
After completion, create `.planning/phases/08-avatar-import-catalogs/08-05-SUMMARY.md` documenting sidecar integration, dogfood evidence status, `avatars/teto/_avatar_overrides.yaml`, and the VTS smoke PASS/BLOCKED result.
</output>
