---
phase: 07-three-category-code-parsing-dispatch
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - sidecar/src/sidecar/parser/__init__.py
  - sidecar/src/sidecar/parser/reserved.py
  - sidecar/tests/parser/__init__.py
  - sidecar/tests/parser/test_reserved.py
autonomous: true
requirements:
  - PARSE-04
  - PARSE-07
must_haves:
  truths:
    - "Reserved LLM protocol names are rejected by a reusable boot validation function."
    - "Cross-category code collisions are rejected case-insensitively."
    - "PARSE-04 is enforced through reserved-name validation, not parse-time stripping."
  artifacts:
    - path: "sidecar/src/sidecar/parser/reserved.py"
      provides: "RESERVED_NAMES and validate_reserved_names"
      contains: "def validate_reserved_names"
  key_links:
    - from: "sidecar/src/sidecar/parser/reserved.py"
      to: "sidecar/tests/parser/test_reserved.py"
      via: "reserved-name and collision table tests"
      pattern: "validate_reserved_names"
---

<objective>
Add reserved-name and cross-category collision validation.

Purpose: Phase 7 CONTEXT D-A1 reframes PARSE-04 as boot-time protection; this
module is the reusable validation primitive that Plan 07-07 wires into boot.

Output: Parser validation module and unit tests.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reserved-name and cross-category validation module</name>
  <files>
    sidecar/src/sidecar/parser/__init__.py,
    sidecar/src/sidecar/parser/reserved.py,
    sidecar/tests/parser/__init__.py,
    sidecar/tests/parser/test_reserved.py
  </files>
  <read_first>
    - .planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md (D-D1 through D-D4)
    - .planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md (reserved-name final list and validation section)
    - packages/contracts/py/contracts/variant_entry.py
    - packages/contracts/py/contracts/event_entry.py
  </read_first>
  <behavior>
    - Test 1: `validate_reserved_names(plugin_action_codes={"think"}, variants=[], events=[])` raises `ReservedNameError` mentioning `think` and `plugin.action_codes`.
    - Test 2: plugin action code `Joy` colliding with variant code `joy` raises `CategoryCollisionError`.
    - Test 3: variant code `wave` colliding with event code `WAVE` raises `CategoryCollisionError`.
    - Test 4: clean disjoint sets pass.
    - Test 5: `RESERVED_NAMES` contains all 28 research entries.
  </behavior>
  <action>
Create `sidecar/src/sidecar/parser/__init__.py` exporting
`RESERVED_NAMES`, `ReservedNameError`, `CategoryCollisionError`, and
`validate_reserved_names`.

Create `sidecar/src/sidecar/parser/reserved.py` with the exact 28-name
`RESERVED_NAMES` set from 07-RESEARCH, including the PLG-06 floor names and
extended sweep names such as `antml:function_calls`, `tool_use`, `reasoning`,
`search_quality_reflection`, `system`, `user`, and `assistant`.

Add `ReservedNameError(ValueError)` and `CategoryCollisionError(ValueError)`.

Implement:
```python
def validate_reserved_names(
    plugin_action_codes: set[str] | list[str] | None = None,
    variants: list[VariantEntry] | None = None,
    events: list[EventEntry] | None = None,
) -> None:
```

Normalize every code with `.lower()`. Reject any normalized code in
`RESERVED_NAMES` with an error containing `reserved LLM protocol name`, the
offending code, and one of these source strings: `plugin.action_codes`,
`variants[].code`, or `events[].code`.

Reject any normalized code present in more than one category with an error
containing `cross-category code collision`, the code, and both source strings.
Pairwise collisions fail; do not require a three-way collision.

Do not add runtime hot-path checks in this module. Plan 07-07 calls it during
sidecar boot.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/parser/test_reserved.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `sidecar/src/sidecar/parser/reserved.py` contains `RESERVED_NAMES: frozenset[str]`.
    - `sidecar/src/sidecar/parser/reserved.py` contains `class ReservedNameError`.
    - `sidecar/src/sidecar/parser/reserved.py` contains `class CategoryCollisionError`.
    - `sidecar/src/sidecar/parser/reserved.py` contains `def validate_reserved_names`.
    - `sidecar/src/sidecar/parser/reserved.py` contains `cross-category code collision`.
    - `sidecar/tests/parser/test_reserved.py` contains `test_reserved_names_completeness`.
    - `sidecar/tests/parser/test_reserved.py` contains `"antml:function_calls"`.
    - `cd sidecar && uv run pytest tests/parser/test_reserved.py -x --no-header` exits 0.
  </acceptance_criteria>
  <done>Reserved-name and cross-category validation is implemented and unit-tested.</done>
</task>

</tasks>

<verification>
Run from repo root:
```powershell
cd sidecar
uv run pytest tests/parser/test_reserved.py -q
```
</verification>

<success_criteria>
- Reserved LLM protocol names fail loudly.
- Cross-category collisions fail loudly and case-insensitively.
- The module is ready for boot integration in Plan 07-07.
</success_criteria>

<output>
After completion, create `.planning/phases/07-three-category-code-parsing-dispatch/07-04-SUMMARY.md`.
</output>
