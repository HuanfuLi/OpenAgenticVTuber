---
phase: 07-three-category-code-parsing-dispatch
plan: 08
type: execute
wave: 5
depends_on:
  - 07-07
files_modified:
  - sidecar/src/sidecar/plugins/loader.py
  - sidecar/src/sidecar/orchestrator/prompts/live2d_expression_prompt.txt
  - sidecar/src/sidecar/orchestrator/orchestrator.py
  - sidecar/src/sidecar/ws/server.py
  - sidecar/tests/plugins/test_prompt_section.py
  - sidecar/tests/plugins/test_manifest_watcher.py
  - sidecar/tests/test_orchestrator_turn.py
  - sidecar/tests/test_sidecar_boot.py
  - .planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements:
  - PARSE-03
  - PARSE-05
  - PARSE-06
must_haves:
  truths:
    - "The runtime system prompt lists active plugin action codes, active avatar variant codes, and active avatar event codes using their real delimiters."
    - "The prompt no longer tells the LLM to avoid variant/event tags when those tags are supplied by the active avatar catalog."
    - "Deterministic tests prove a forced assistant sentence containing listed `[action] {variant} <event>` codes produces all three dispatch kinds without relying on stochastic LLM output."
    - "Live UAT event verification is blocked with a clear catalog-prerequisite message when the active avatar has `events: []`, instead of being reported as a dispatch failure."
  artifacts:
    - path: "sidecar/src/sidecar/plugins/loader.py"
      provides: "Combined dispatch prompt catalog builder"
      contains: "build_dispatch_codes_section"
    - path: "sidecar/src/sidecar/orchestrator/prompts/live2d_expression_prompt.txt"
      provides: "Prompt copy allowing listed action, variant, and event dispatch codes"
      contains: "[<insert_dispatch_codes_section>]"
    - path: "sidecar/src/sidecar/ws/server.py"
      provides: "Boot wiring from active plugin/avatar catalogs into the prompt"
      contains: "build_dispatch_codes_section"
    - path: "sidecar/tests/plugins/test_prompt_section.py"
      provides: "Prompt catalog unit coverage"
      contains: "test_build_dispatch_codes_section"
    - path: "sidecar/tests/test_orchestrator_turn.py"
      provides: "Deterministic prompt/system-prompt coverage"
      contains: "heart-eye"
    - path: ".planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md"
      provides: "Updated live UAT gating instructions for empty event catalogs"
      contains: "events: []"
  key_links:
    - from: "sidecar/src/sidecar/ws/server.py"
      to: "sidecar/src/sidecar/plugins/loader.py"
      via: "build_dispatch_codes_section(plugin_manifest, overrides)"
      pattern: "build_dispatch_codes_section"
    - from: "sidecar/src/sidecar/orchestrator/orchestrator.py"
      to: "sidecar/src/sidecar/orchestrator/prompts/live2d_expression_prompt.txt"
      via: "system prompt placeholder replacement"
      pattern: "insert_dispatch_codes_section"
    - from: "sidecar/src/sidecar/orchestrator/orchestrator.py"
      to: "sidecar/src/sidecar/orchestrator/transformers.py"
      via: "same active plugin/avatar catalogs appear in prompt and code_extractor"
      pattern: "plugin_action_codes.*avatar_overrides"
---

<objective>
Close the Phase 7 live UAT gap without replanning parser/routing.

Purpose: The parser and runtime dispatch paths already work when the assistant emits valid codes. The live failure happened because the boot prompt exposed only plugin `[action]` codes and explicitly withheld `{variant}` / `<event>` vocabulary, while the active Teto catalog had no events. This plan makes the active dispatch vocabulary visible to the LLM and makes empty event catalogs an explicit UAT prerequisite failure.

Output: Combined prompt catalog builder, prompt copy update, boot wiring, deterministic prompt/catalog tests, and revised UAT gating instructions.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md
@.planning/debug/phase-7-live-dispatch-codes-not-visible.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-VERIFICATION.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-VALIDATION.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-06-SUMMARY.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-07-SUMMARY.md

<interfaces>
Current relevant surfaces:
```python
# sidecar/src/sidecar/plugins/loader.py
def build_action_codes_section(manifest: PluginManifest) -> str:
    lines = [f"## Available Actions (plugin: {manifest.name} v{manifest.version})"]
    for action_code in sorted(manifest.action_codes, key=lambda item: item.code):
        lines.append(f"[{action_code.code}] - {action_code.description}")
    return "\n".join(lines)
```

```python
# sidecar/src/sidecar/orchestrator/orchestrator.py
def _build_system_prompt(persona_text: str, action_codes_section: str) -> str:
    expression_prompt = load_util("live2d_expression_prompt")
    expression_prompt = expression_prompt.replace(
        "[<insert_action_codes_section>]", action_codes_section
    )
    return persona_text + "\n\n" + expression_prompt
```

```python
# sidecar/src/sidecar/ws/server.py
action_codes_section = (
    build_action_codes_section(plugin_manifest) if plugin_manifest is not None else ""
)
app.state.orchestrator = Orchestrator(
    ...,
    action_codes_section=action_codes_section,
    plugin_action_codes=plugin_action_codes,
    avatar_overrides=overrides,
)
```

Current prompt problem:
```text
Use only the action codes listed below:
[<insert_action_codes_section>]
Do not add model variant or event tags unless they are explicitly provided by another instruction.
```

Current live catalog problem:
```yaml
# avatars/重音テト/_avatar_overrides.yaml
variants:
  - code: heart-eye
events: []
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and test a combined dispatch prompt catalog</name>
  <files>
    sidecar/src/sidecar/plugins/loader.py,
    sidecar/src/sidecar/orchestrator/prompts/live2d_expression_prompt.txt,
    sidecar/tests/plugins/test_prompt_section.py
  </files>
  <behavior>
    - Test 1: `build_dispatch_codes_section(plugin_manifest, overrides)` lists `[smirk]`, `{heart-eye}`, and `<wave>` with stable sorted order and category headings.
    - Test 2: when `overrides.events` is empty, the generated section explicitly says no avatar event codes are declared for the active avatar.
    - Test 3: the prompt template contains `[<insert_dispatch_codes_section>]`, refers to action, variant, and event dispatch codes, and no longer contains the current ban line `Do not add model variant or event tags unless they are explicitly provided by another instruction.`
  </behavior>
  <action>
Add `build_dispatch_codes_section(manifest: PluginManifest | None, overrides: AvatarOverrides | None) -> str` to `sidecar/src/sidecar/plugins/loader.py`. Keep `build_action_codes_section(manifest)` as a backward-compatible wrapper for tests or callsites that still need action-only text; do not delete it in this gap closure.

The new combined section must be deterministic and human-readable:
- Heading: `## Available Dispatch Codes`
- Plugin action subsection: `### Plugin Actions`, with one line per sorted plugin action as `[code] - description`
- Avatar variant subsection: `### Avatar Variants`, with one line per sorted variant as `{code} - source_name`
- Avatar event subsection: `### Avatar Events`, with one line per sorted event as `<code> - source_name-or-motion_file (duration: Ns)` when events exist
- Empty subsections must say no codes are declared, especially `No avatar event codes are declared for the active avatar.`

Sort by the actual code string in each subsection. Use `source_name` for variants. For events, prefer `source_name` if present on the object, otherwise `motion_file`, otherwise the code. Do not invent codes.

Update `sidecar/src/sidecar/orchestrator/prompts/live2d_expression_prompt.txt` so it describes all three supported delimiter categories:
- `[action]` = active plugin action
- `{variant}` = active avatar variant toggle
- `<event>` = active avatar motion/event hotkey

Replace `[<insert_action_codes_section>]` with `[<insert_dispatch_codes_section>]`. Remove the line that tells the model not to add model variant or event tags. Keep the existing "optional, use sparingly, do not invent unlisted codes" behavior.

Extend `sidecar/tests/plugins/test_prompt_section.py` with deterministic builder tests using a fake plugin manifest containing `smirk` and `neutral`, plus an `AvatarOverrides` fixture containing `VariantEntry(code="heart-eye", hotkey_id="hk-v", source_name="Heart Eye")` and `EventEntry(code="wave", hotkey_id="hk-e", motion_file="wave.motion3.json", duration_seconds=1.5)`.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/plugins/test_prompt_section.py -x --no-header</automated>
  </verify>
  <done>The prompt catalog builder exposes all available dispatch categories, and prompt-template tests prove listed variant/event tags are allowed while unlisted-code discipline remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire active catalogs into the boot prompt and make live UAT catalog readiness explicit</name>
  <files>
    sidecar/src/sidecar/orchestrator/orchestrator.py,
    sidecar/src/sidecar/ws/server.py,
    sidecar/tests/plugins/test_manifest_watcher.py,
    sidecar/tests/test_orchestrator_turn.py,
    sidecar/tests/test_sidecar_boot.py,
    .planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md
  </files>
  <behavior>
    - Test 1: an `Orchestrator` built with a dispatch catalog section freezes a system prompt containing `[smirk]`, `{heart-eye}`, and `<wave>`.
    - Test 2: boot constructs the prompt section with `build_dispatch_codes_section(plugin_manifest, overrides)` after both plugin and avatar catalogs are loaded, and before `Orchestrator(...)`.
    - Test 3: a deterministic fake gateway yielding `[smirk] {heart-eye} <wave> Hello.` with matching active catalogs produces action, variant, and event dispatches; this test must not depend on live LLM choice.
    - Test 4: when the active avatar catalog has `events: []`, UAT metadata/instructions mark event-specific live verification as blocked until an event-bearing catalog is selected or imported.
  </behavior>
  <action>
Update `sidecar/src/sidecar/orchestrator/orchestrator.py` to replace the new `[<insert_dispatch_codes_section>]` placeholder. Prefer renaming the internal helper parameter to `dispatch_codes_section`. Keep the public `Orchestrator(..., action_codes_section=...)` keyword as a compatibility alias unless you update every existing test/callsite in the same task; this gap closure should stay narrow.

Update `sidecar/src/sidecar/ws/server.py` to import `build_dispatch_codes_section` and build the prompt section from both `plugin_manifest` and loaded `overrides`:
```python
dispatch_codes_section = build_dispatch_codes_section(plugin_manifest, overrides)
```
Pass that value into the orchestrator prompt argument. This construction must happen after `load_avatar_overrides(avatar_dir)`, after plugin manifest load, and before `Orchestrator(...)`. Preserve the existing `plugin_action_codes` and `avatar_overrides` arguments used by `code_extractor`; the prompt and parser must consume the same boot-frozen catalogs.

Add an info log near boot catalog construction:
```text
[DISPATCH-CATALOG] active_avatar=<id> actions=<n> variants=<n> events=<n>
```
If `events` is empty, also log:
```text
[DISPATCH-CATALOG-BLOCKED] live event UAT requires an active avatar catalog with at least one event.
```
Do not fail boot just because `events` is empty; variants still work and many rigs may not declare motion events.

Update `sidecar/tests/plugins/test_manifest_watcher.py` so source-order assertions expect the new `build_dispatch_codes_section(plugin_manifest, overrides)` call before `Orchestrator(...)` and do not require the old action-only builder in boot.

Extend `sidecar/tests/test_orchestrator_turn.py` with a deterministic system-prompt freeze test that passes a combined section containing `[smirk]`, `{heart-eye}`, and `<wave>`, then asserts the gateway receives those exact lines in the frozen prompt.

Extend `sidecar/tests/test_sidecar_boot.py` with boot wiring coverage proving the combined builder receives both the plugin manifest and the loaded avatar overrides. Also assert the empty-event catalog log appears when overrides have `events=[]`.

Update `.planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md` so the live test prerequisites are explicit:
- use an active avatar whose `_avatar_overrides.yaml` contains the variant and event codes under test;
- for current imported Teto, set `AGENTICLLMVTUBER_ACTIVE_AVATAR=重音テト` for `{heart-eye}`;
- because current `avatars/重音テト/_avatar_overrides.yaml` has `events: []`, event dispatch / `[EVENT-COMPLETE]` verification is `blocked` until an event-bearing avatar catalog is selected or imported;
- absence of `<event>` output from the LLM after an empty-event catalog warning is not a parser/routing failure.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/test_orchestrator_turn.py tests/test_sidecar_boot.py tests/plugins/test_manifest_watcher.py tests/orchestrator/test_dispatch_routing.py -x --no-header</automated>
  </verify>
  <done>Runtime boot prompt uses the same active catalogs as `code_extractor`, deterministic tests cover prompt visibility and forced dispatch, and live UAT instructions distinguish empty catalog readiness from dispatch failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm live VTS behavior after prompt/catalog fix</name>
  <files>.planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md</files>
  <what-built>Automated tasks expose active action, variant, and event codes to the LLM prompt and add catalog-readiness diagnostics for live UAT.</what-built>
  <action>Perform the live VTS verification steps below after the automated tasks and their pytest commands pass. This is the only manual checkpoint because visible VTS rig state and real hotkey registration cannot be proven by CLI alone.</action>
  <how-to-verify>
    1. Start VTube Studio with the intended rig loaded.
    2. Start the app with an active avatar whose overrides match the rig. For current imported Teto variant testing, set `AGENTICLLMVTUBER_ACTIVE_AVATAR=重音テト`.
    3. Confirm startup logs include `[DISPATCH-CATALOG]` with nonzero `actions` and `variants`. If `events=0`, event dispatch UAT is blocked; do not mark `<event>` / `[EVENT-COMPLETE]` as failed until an event-bearing catalog is active.
    4. Ask the model for a short reply using a listed action and variant, for example `[smirk] {heart-eye}` when those codes appear in the prompt.
    5. Expected: logs include `[DISPATCH] kind=action name=smirk` and `[DISPATCH] kind=variant name=heart-eye`; the VTS rig visibly changes variant.
    6. If `[DISPATCH-CATALOG]` shows at least one event, ask for a listed `<event>` and confirm `[DISPATCH] kind=event` plus `[EVENT-COMPLETE]` after its duration.
  </how-to-verify>
  <verify>
    <manual>Follow the numbered live VTS steps and record pass/block/fail in `07-HUMAN-UAT.md`.</manual>
  </verify>
  <done>Human UAT distinguishes successful live variant dispatch from blocked event verification when the active avatar has no events, and records any remaining live VTS issue with concrete logs.</done>
  <resume-signal>Type "approved" if the live variant/event-ready behavior matches the expectations, or describe the observed log and visible behavior.</resume-signal>
</task>

</tasks>

<verification>
Run from repo root after automated tasks:
```powershell
cd sidecar
uv run pytest tests/plugins/test_prompt_section.py tests/test_orchestrator_turn.py tests/test_sidecar_boot.py tests/plugins/test_manifest_watcher.py tests/orchestrator/test_dispatch_routing.py -q
```

Then perform the live VTS checkpoint only for catalog categories that are actually declared by the active avatar.
</verification>

<success_criteria>
- The active boot prompt exposes plugin `[action]`, avatar `{variant}`, and avatar `<event>` codes when present.
- The prompt no longer discourages listed variant/event tags.
- Prompt visibility is covered by deterministic tests and does not rely on stochastic LLM choice.
- Current Teto's `events: []` state is represented as a blocked live event prerequisite, not a dispatch failure.
- Live VTS variant verification can proceed with `AGENTICLLMVTUBER_ACTIVE_AVATAR=重音テト` and `{heart-eye}`; live event verification proceeds only after an event-bearing active avatar catalog exists.
</success_criteria>

<output>
After completion, create `.planning/phases/07-three-category-code-parsing-dispatch/07-08-SUMMARY.md`.
</output>
