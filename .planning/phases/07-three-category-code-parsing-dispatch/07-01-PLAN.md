---
phase: 07-three-category-code-parsing-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/py/contracts/dispatch.py
  - packages/contracts/py/contracts/__init__.py
  - packages/contracts/py/contracts/event_entry.py
  - packages/contracts/py/contracts/audio_payload.py
  - packages/contracts/py/contracts/action_intent.py
  - packages/contracts/scripts/codegen.py
  - sidecar/schemas/avatar_overrides.schema.json
  - sidecar/src/sidecar/avatar/extractors/vts.py
  - sidecar/tests/avatar/test_extract_vts.py
  - sidecar/src/sidecar/orchestrator/output_types.py
autonomous: true
requirements:
  - PARSE-03
  - PARSE-06
must_haves:
  truths:
    - "Dispatch is a three-variant discriminated union with action, variant, and event records."
    - "Audio payloads and sentence outputs carry ordered dispatches, not ActionIntent actions."
    - "Event catalog entries expose hotkey_id while retaining duration_seconds for PARSE-06 conversion."
  artifacts:
    - path: "packages/contracts/py/contracts/dispatch.py"
      provides: "ActionCode, VariantToggle, EventFire, Dispatch"
      contains: "Field(discriminator=\"kind\")"
    - path: "packages/contracts/py/contracts/audio_payload.py"
      provides: "AudioPayloadMessage.dispatches"
      contains: "dispatches: List[Dispatch]"
    - path: "sidecar/src/sidecar/orchestrator/output_types.py"
      provides: "SentenceOutput.dispatches"
      contains: "dispatches"
    - path: "sidecar/tests/avatar/test_extract_vts.py"
      provides: "TriggerAnimation duration regression coverage"
      contains: "EventFire"
  key_links:
    - from: "packages/contracts/scripts/codegen.py"
      to: "packages/contracts/py/contracts/dispatch.py"
      via: "imports Dispatch and owner-file mapping"
      pattern: "Dispatch.*dispatch"
---

<objective>
Create the Python Dispatch contract and update the Python-side producers that
must stop using `ActionIntent`.

Purpose: Later parser and runtime plans need one stable dispatch shape before
they can route action, variant, and event records.

Output: Python contract source, codegen registration, avatar schema support,
VTS event hotkey extraction, and Python output model updates.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-VALIDATION.md
@.planning/phases/08-avatar-import-catalogs/08-01-SUMMARY.md
@.planning/phases/08-avatar-import-catalogs/08-02-SUMMARY.md

<interfaces>
Required dispatch contract:
```python
class ActionCode(BaseModel):
    kind: Literal["action"] = "action"
    name: str

class VariantToggle(BaseModel):
    kind: Literal["variant"] = "variant"
    name: str
    hotkey_id: str

class EventFire(BaseModel):
    kind: Literal["event"] = "event"
    name: str
    hotkey_id: str
    duration_ms: int

Dispatch = Annotated[
    Union[ActionCode, VariantToggle, EventFire],
    Field(discriminator="kind"),
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Python Dispatch contract and source model updates</name>
  <files>
    packages/contracts/py/contracts/dispatch.py,
    packages/contracts/py/contracts/__init__.py,
    packages/contracts/py/contracts/event_entry.py,
    packages/contracts/py/contracts/audio_payload.py,
    packages/contracts/py/contracts/action_intent.py,
    packages/contracts/scripts/codegen.py,
    sidecar/schemas/avatar_overrides.schema.json,
    sidecar/src/sidecar/avatar/extractors/vts.py,
    sidecar/tests/avatar/test_extract_vts.py,
    sidecar/src/sidecar/orchestrator/output_types.py
  </files>
  <read_first>
    - .planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md (D-A4: ActionIntent is deleted; [INTENT] becomes [DISPATCH])
    - .planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md (Discriminated Union Pattern and Event Entry hotkey discussion)
    - packages/contracts/py/contracts/action_intent.py
    - packages/contracts/py/contracts/audio_payload.py
    - packages/contracts/py/contracts/event_entry.py
    - packages/contracts/scripts/codegen.py
    - sidecar/src/sidecar/avatar/extractors/vts.py
    - sidecar/src/sidecar/avatar/motion3_meta.py
    - sidecar/tests/avatar/test_extract_vts.py
    - sidecar/src/sidecar/orchestrator/output_types.py
  </read_first>
  <behavior>
    - Test 1: `ActionCode(name="joy").model_dump()` returns `{"kind": "action", "name": "joy"}`.
    - Test 2: `VariantToggle(name="hold-mic", hotkey_id="hk-1")` exposes `kind`, `name`, and `hotkey_id`.
    - Test 3: `EventFire(name="wave", hotkey_id="hk-2", duration_ms=2833)` exposes `duration_ms`.
    - Test 4: `AudioPayloadMessage.dispatches` accepts a list containing all three dispatch variants.
    - Test 5: `extract_vts()` on a temp VTS folder with a `TriggerAnimation` hotkey whose `File` points to a `.motion3.json` containing `{"Meta":{"Duration":1.833,"Loop":false}}` returns one `EventEntry` with `duration_seconds == 1.833`, `motion_file` equal to the hotkey file, and the hotkey ID copied through.
    - Test 6: The same fixture constructs the Phase 7 event-dispatch duration value from the parsed entry and asserts `EventFire(name="wave", hotkey_id="hk-event", duration_ms=2833).duration_ms == 2833`, proving the real `Meta.Duration` flows into the `+1000ms` blend-pad completion path instead of the fallback.
  </behavior>
  <action>
Create `packages/contracts/py/contracts/dispatch.py` with the exact classes and
`Dispatch` union shown in the context block. Use `kind` as the discriminator per
D-A4.

Update `packages/contracts/py/contracts/__init__.py` to export
`ActionCode`, `VariantToggle`, `EventFire`, and `Dispatch`, and remove every
`ActionIntent` import/export.

Delete `packages/contracts/py/contracts/action_intent.py` after all Python
imports are replaced. Do not leave a compatibility alias; D-A4 explicitly
replaces the old contract with Dispatch.

Update `packages/contracts/py/contracts/event_entry.py` to add
`hotkey_id: str = ""` immediately after `code: str`. Keep
`duration_seconds: float`; Phase 7 converts it to `EventFire.duration_ms` in
the parser lookup.

Update `packages/contracts/py/contracts/audio_payload.py` to import
`Dispatch`, replace `actions: List[ActionIntent] = []` with
`dispatches: List[Dispatch] = []`, and update the docstring to say the field
carries ordered Phase 7 Dispatch records.

Update `sidecar/src/sidecar/orchestrator/output_types.py` to import
`Dispatch`, rename `SentenceOutput.actions` to `SentenceOutput.dispatches`, and
type it as `List[Dispatch] = field(default_factory=list)`.

Update `packages/contracts/scripts/codegen.py` to import the four dispatch
symbols, remove `ActionIntent` from `TARGETS` and `OWNER_FILE`, add
`(Dispatch, "dispatch", "dispatch", "Dispatch")`, and map
`ActionCode`, `VariantToggle`, and `EventFire` to owner `"dispatch"`.

Update `sidecar/schemas/avatar_overrides.schema.json` so event items allow
`hotkey_id`.

Update `sidecar/src/sidecar/avatar/extractors/vts.py` so VTS hotkeys branch by
`Action`: keep existing `ToggleExpression` -> `VariantEntry` behavior unchanged,
and add `TriggerAnimation` -> `EventEntry`.

For each `TriggerAnimation` hotkey, copy `HotkeyID` into `hotkey_id`, derive
`code` with `slug_from_hotkey_name(hotkey["Name"])`, copy `hotkey["File"]` into
`motion_file`, and read metadata with the existing
`sidecar.avatar.motion3_meta.read_motion3_meta(folder / motion_file)`.

Set `EventEntry.duration_seconds` to the parsed `motion3.json.Meta.Duration`
only when metadata is readable and valid under the Phase 7 policy:
`0 < duration <= 10.0`. Set `is_loop` from the parsed metadata in that valid
case. Use the explicit fallback `duration_seconds=10.0` and `is_loop=False`
only when metadata is missing, unreadable, invalid/non-numeric, nonpositive, or
oversized (`duration > 10.0`) per PARSE-06. Do not hardcode `10.0` for the
normal readable TriggerAnimation path.

Add or extend `sidecar/tests/avatar/test_extract_vts.py` with a temp VTS fixture
that creates a `.vtube.json` containing one `TriggerAnimation` hotkey with
`File: "Motions/Wave.motion3.json"` plus a matching `.motion3.json` whose
`Meta.Duration` is `1.833`. Assert `extract_vts()` returns an event with
`duration_seconds == pytest.approx(1.833)` and that the derived
`EventFire.duration_ms` value is exactly `2833` after applying the `+1000ms`
blend pad (`int(1.833 * 1000) + 1000`). Add a fallback case in the same test
file proving missing/unreadable/invalid/oversized metadata yields
`duration_seconds == 10.0`. Leave Cubism/OLVT event entries with default
`hotkey_id=""`; runtime dispatch logs and drops those in Plan 07-06.
  </action>
  <verify>
    <automated>uv run --project sidecar python -c "from contracts import ActionCode, VariantToggle, EventFire, Dispatch; print(ActionCode(name='joy').kind, VariantToggle(name='hold-mic', hotkey_id='hk').kind, EventFire(name='wave', hotkey_id='hk', duration_ms=2833).duration_ms)"</automated>
    <automated>cd sidecar && uv run pytest tests/avatar/test_extract_vts.py::test_trigger_animation_uses_motion3_duration -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/py/contracts/dispatch.py` contains `class ActionCode`.
    - `packages/contracts/py/contracts/dispatch.py` contains `class VariantToggle`.
    - `packages/contracts/py/contracts/dispatch.py` contains `class EventFire`.
    - `packages/contracts/py/contracts/dispatch.py` contains `Field(discriminator="kind")`.
    - `packages/contracts/py/contracts/audio_payload.py` contains `dispatches: List[Dispatch]`.
    - `packages/contracts/py/contracts/event_entry.py` contains `hotkey_id: str = ""`.
    - `sidecar/schemas/avatar_overrides.schema.json` contains `hotkey_id`.
    - `sidecar/src/sidecar/avatar/extractors/vts.py` contains `TriggerAnimation`.
    - `sidecar/src/sidecar/avatar/extractors/vts.py` imports `read_motion3_meta`.
    - `sidecar/src/sidecar/avatar/extractors/vts.py` does not set `duration_seconds=10.0` on the normal readable `TriggerAnimation` path.
    - `sidecar/tests/avatar/test_extract_vts.py::test_trigger_animation_uses_motion3_duration` asserts a fixture with `Meta.Duration == 1.833` produces `EventEntry.duration_seconds == pytest.approx(1.833)` and `EventFire.duration_ms == 2833` after the `+1000ms` blend-pad conversion.
    - `sidecar/tests/avatar/test_extract_vts.py` covers the fallback policy: missing, unreadable, invalid/non-numeric, nonpositive, or `> 10.0` duration metadata uses `duration_seconds == 10.0`.
    - `packages/contracts/scripts/codegen.py` contains `(Dispatch, "dispatch", "dispatch", "Dispatch")`.
    - `rg "ActionIntent" packages/contracts/py sidecar/src/sidecar/orchestrator/output_types.py` returns no matches.
  </acceptance_criteria>
  <done>Python Dispatch source contracts replace ActionIntent, VTS TriggerAnimation event hotkeys carry real motion durations when available with explicit 10.0s fallback otherwise, and Python output models use `dispatches`.</done>
</task>

</tasks>

<verification>
Run from repo root:
```powershell
uv run --project sidecar python -c "from contracts import ActionCode, VariantToggle, EventFire; print('dispatch-ok')"
cd sidecar && uv run pytest tests/avatar/test_extract_vts.py::test_trigger_animation_uses_motion3_duration -x --no-header
```
</verification>

<success_criteria>
- `Dispatch` exists as the Python source-of-truth discriminated union.
- Python audio and sentence output paths expose `dispatches`.
- Event catalog entries can carry `hotkey_id` and VTS TriggerAnimation entries use real `motion3.json.Meta.Duration` when valid, falling back to 10.0s only for missing/unreadable/invalid/oversized metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/07-three-category-code-parsing-dispatch/07-01-SUMMARY.md`.
</output>
