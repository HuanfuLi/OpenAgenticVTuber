---
phase: 20.2
slug: cpu-local-stt-enhancement
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-14
updated: 2026-05-14
---

# Phase 20.2 - Validation Strategy

Per-phase validation contract reconstructed after execution from Phase 20.2 plans,
summaries, UAT, verification evidence, and current test infrastructure.

## Test Infrastructure

| Property | Value |
|----------|-------|
| Framework | Vitest, pytest, TypeScript, Electron build, contract codegen |
| Config file | workspace `package.json`; `sidecar/pyproject.toml` |
| Quick run command | `npm --workspace apps/renderer run test -- --run ChatVoiceInput ChatStreaming voice-capture` |
| Full suite command | `npm --workspace apps/renderer run test -- --run voice-capture voice-input-store ChatVoiceInput ChatStreaming ws-store-audio ws-audio-player ConversationHistory useStreamingMessages`; `npm --workspace apps/electron-main run test -- --run conversation-store ipc-voice-input`; `uv run --project sidecar python -m pytest sidecar/tests/admin/test_audio_voice_input_endpoint.py sidecar/tests/admin/test_audio_stt_local.py sidecar/tests/stt/test_faster_whisper_provider.py sidecar/tests/test_orchestrator_turn.py sidecar/tests/ws/test_stop_turn.py -q` |
| Contract command | `node packages/contracts/scripts/run-codegen.cjs` plus generated-file idempotence check |
| Preview cleanup command | `rg "previewing|previewTranscript|previewSequenceId|applyPreviewResult|setTransientPreview|clearTransientPreview|mode: 'preview'|mode: \"preview\"|VOICE_PREVIEW" apps packages sidecar` |
| Estimated runtime | ~80 seconds for focused validation |

## Sampling Rate

- After voice capture or Chat state changes: run the quick renderer command.
- After cross-tier protocol/history changes: run renderer, electron IPC/store, and sidecar focused suites.
- Before verification: run renderer typecheck, Electron build, contract codegen idempotence, and preview cleanup search.
- Max feedback latency: ~90 seconds for focused validation.

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 20.2-01 | 20.2-01 | 1 | STT-03, STT-06, VIN-02, VIN-04, PERF-01 | preview-latency / model-downgrade / boot-heavy-load | Voice input is final-only; faster-whisper keeps selected model quality, uses bounded CPU threads, reuses cached providers, and warms only after explicit actions. | unit/integration | `npm --workspace apps/renderer run test -- --run voice-capture voice-input-store ChatVoiceInput`; `uv run --project sidecar python -m pytest sidecar/tests/admin/test_audio_voice_input_endpoint.py sidecar/tests/admin/test_audio_stt_local.py sidecar/tests/stt/test_faster_whisper_provider.py -q` | yes | green |
| 20.2-02 | 20.2-02 | 2 | VIN-04, VIN-05, PERF-01 | uninterruptible-turn / audio-continues-after-stop | `stop-turn` cancels active sidecar work, clears TTS/audio queues, drops partial assistant output, and keeps the user message for recovery. | unit/integration/UI | `npm --workspace apps/renderer run test -- --run ChatStreaming ws-store-audio ws-audio-player`; `uv run --project sidecar python -m pytest sidecar/tests/test_orchestrator_turn.py sidecar/tests/ws/test_stop_turn.py -q` | yes | green |
| 20.2-03 | 20.2-03 | 3 | VIN-02, VIN-04, VIN-05 | duplicate-regenerate / stale-memory | Editing a sent user message truncates later history, dispatches edited text through normal `text-input`, and makes renderer history authoritative for sidecar memory resync. | unit/integration/UI | `npm --workspace apps/electron-main run test -- --run conversation-store ipc-voice-input`; `npm --workspace apps/renderer run test -- --run ConversationHistory ChatStreaming ChatVoiceInput`; `uv run --project sidecar python -m pytest sidecar/tests/test_orchestrator_turn.py -q` | yes | green |
| 20.2-04 | 20.2-04 | 4 | STT-03, STT-06, VIN-02, VIN-04, VIN-05, PERF-01 | cross-feature-regression / stale-preview-code | Combined final-only STT, Stop, audio cleanup, edit/regenerate, conversation truncation, and CPU latency behavior are verified together. | integration/search/docs | Full renderer/electron/sidecar focused suite plus preview cleanup search | yes | green |
| 20.2-05 | 20.2-05 | 5 | VIN-02, VIN-04, VIN-05 | stopped-message-orphan / history-order-drift | Stopped-message restart metadata is keyed by message id; stopped messages remain anchored after later history and can regenerate without duplicate bubbles. | UI regression | `npm --workspace apps/renderer run test -- --run ChatStreaming useStreamingMessages ChatVoiceInput` | yes | green |

Status: green = command passed during this audit or in Phase 20.2 verification evidence.

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

- No new test files were generated during this audit.
- Existing renderer tests cover final-only PTT, no preview bubbles, queued final transcripts, Stop UI, renderer audio stop, stopped-message edit/regenerate, persisted edit/regenerate, and no duplicate user bubbles.
- Existing electron tests cover conversation truncation and IPC voice-input behavior.
- Existing sidecar tests cover stop-turn cancellation/idempotency, TTS clear behavior, memory resync after truncated renderer history, faster-whisper CPU thread diagnostics, local provider warmup/cache reuse, and final-only voice-input endpoint behavior.
- Contract codegen checks cover final-only voice-input request/result modes and typed `stop-turn` WebSocket messages.

## Manual-Only Verifications

All Phase 20.2 requirement coverage has automated verification. Live UAT remains documented for confidence, not as a Nyquist blocker:

| Behavior | Requirement | Why Not Required For Nyquist | Test Instructions |
|----------|-------------|------------------------------|-------------------|
| Live faster-whisper CPU PTT latency | STT-03, STT-06, VIN-02, PERF-01 | Automated tests cover final-only requests, provider reuse, bounded CPU threads, and warmup; perceived latency depends on local hardware/model cache. | Use faster-whisper `small` on CPU, hold/release PTT, confirm one final request per utterance and no visible reload after warmup. |
| Stop during real streaming/TTS playback | VIN-04, VIN-05 | Automated tests cover WebSocket stop, orchestrator cancellation, TTS clear, renderer audio stop, and late event idempotency. | Send a prompt, click Stop during Thinking, streaming text, and TTS playback; confirm partial assistant output/audio stop while user text remains. |
| Edit stopped voice transcript in the live app | VIN-02, VIN-04, VIN-05 | Automated regression covers the original blocker, including delayed stopped-message edit after later history. UAT retest passed on 2026-05-13. | Stop a bad response, edit the kept user message after later chat activity, save, and confirm edited text dispatches without duplicate user bubbles. |

## Validation Audit 2026-05-14

| Metric | Count |
|--------|-------|
| Requirement coverage gaps found | 0 |
| New tests generated | 0 |
| Existing targeted suites rerun | 5 |
| Generated-contract drift corrected | 0 |
| Escalated manual-only requirement gaps | 0 |

Audit command results:

- PASS: `npm --workspace apps/renderer run test -- --run voice-capture voice-input-store ChatVoiceInput ChatStreaming ws-store-audio ws-audio-player ConversationHistory useStreamingMessages` - 78 passed.
- PASS: `npm --workspace apps/renderer run typecheck`.
- PASS: `npm --workspace apps/electron-main run test -- --run conversation-store ipc-voice-input` - 11 passed.
- PASS: `uv run --project sidecar python -m pytest sidecar/tests/admin/test_audio_voice_input_endpoint.py sidecar/tests/admin/test_audio_stt_local.py sidecar/tests/stt/test_faster_whisper_provider.py sidecar/tests/test_orchestrator_turn.py sidecar/tests/ws/test_stop_turn.py -q` - 55 passed.
- PASS: `npm --workspace apps/electron-main run build`.
- PASS: contract codegen generated-file hash check was idempotent.
- PASS: active-code preview cleanup search returned no matches.
- INFO: `git diff --check` currently reports trailing whitespace in unrelated Phase 20/22 planning files, not in Phase 20.2 validation changes.

## Validation Sign-Off

- [x] All tasks have automated verify commands.
- [x] Sampling continuity: no three consecutive tasks without automated verification.
- [x] Wave 0 is not needed because existing infrastructure covers all phase requirements.
- [x] No watch-mode flags.
- [x] Feedback latency under 90 seconds for focused validation.
- [x] `nyquist_compliant: true` set in frontmatter.

Approval: approved 2026-05-14
