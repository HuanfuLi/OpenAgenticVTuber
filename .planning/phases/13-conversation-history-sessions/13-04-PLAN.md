---
phase: 13-conversation-history-sessions
plan: 04
type: execute
wave: 4
depends_on:
  - 13-02
gap_closure: true
files_modified:
  - apps/renderer/src/chrome/HistorySheet.tsx
  - apps/renderer/src/index.css
  - apps/renderer/tests/HistorySheet.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
must_haves:
  truths:
    - "History rows should follow standard ChatGPT-style title-first session list behavior."
    - "The visible History row should not display the first or latest assistant response as prominent preview text."
    - "Search/filtering may still use transcript-derived text internally, but visible row content should be title plus compact metadata."
  artifacts:
    - path: "apps/renderer/src/chrome/HistorySheet.tsx"
      provides: "Title-first History rows without visible assistant-response preview"
      contains: "history-row-title"
    - path: "apps/renderer/tests/HistorySheet.test.tsx"
      provides: "Regression coverage that assistant preview text is searchable only if retained, not visibly rendered as row content"
      contains: "queryByText"
---

<objective>
Close the Phase 13 UAT gap where History rows visibly show both the derived user-message title and an assistant-response preview.

Purpose: make the History sheet feel like a standard conversation-session list: the row title is the primary user-facing identifier, and assistant response text is not displayed as row preview content. Preserve useful search/filtering if practical, but do not let preview text become prominent visible row content.
</objective>

<context>
@.planning/phases/13-conversation-history-sessions/13-UAT.md
@.planning/phases/13-conversation-history-sessions/13-02-PLAN.md
@.planning/phases/13-conversation-history-sessions/13-02-SUMMARY.md

@apps/renderer/src/chrome/HistorySheet.tsx
@apps/renderer/src/index.css
@apps/renderer/tests/HistorySheet.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make History rows title-first without visible assistant previews</name>
  <files>
    - apps/renderer/src/chrome/HistorySheet.tsx
    - apps/renderer/src/index.css
  </files>
  <behavior>
    - A normal History row visibly shows the session title and compact metadata only.
    - Assistant response text from `session.preview` is not displayed as a visible row subtitle.
    - Empty sessions still show an understandable compact state through metadata or title, not a fake assistant preview.
    - Existing create/select/rename/delete/search behavior remains intact.
  </behavior>
  <action>
    Remove the visible `.history-row-preview` content from `HistorySheet` row rendering, or replace it with non-transcript metadata that cannot be confused with conversation content.

    Keep row density and clickable target quality. Update CSS to remove unused preview styling if it becomes dead code.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- HistorySheet.test.tsx</automated>
  </verify>
  <done>
    History rows no longer visibly display assistant-response preview text.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve useful search while hiding preview content</name>
  <files>
    - apps/renderer/src/chrome/HistorySheet.tsx
    - apps/renderer/tests/HistorySheet.test.tsx
  </files>
  <behavior>
    - Search still filters by title.
    - If preview text remains part of the filter index, matching preview text can find the row without visibly rendering that preview in the row.
    - Tests assert assistant preview text is absent from visible History row content.
  </behavior>
  <action>
    Keep the existing title+preview search index if desired, but add explicit tests that the preview text is not visible in normal row rendering. If hidden preview search creates confusing behavior, narrow search to title-only for now.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- HistorySheet.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    The UAT gap is covered by tests and the standard History-list presentation is restored.
  </done>
</task>

</tasks>

<verification>
Run these checks after all tasks:

```powershell
npm --workspace apps/renderer run test -- HistorySheet.test.tsx
npm --workspace apps/renderer run typecheck
```
</verification>

<success_criteria>
- Phase 13 UAT gap #2 is closed.
- History rows use title-first session-list presentation.
- Assistant response text is not visibly rendered as row preview content.
- Search/filter behavior remains deterministic and covered by tests.
</success_criteria>
