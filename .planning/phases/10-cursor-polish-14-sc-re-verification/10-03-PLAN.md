---
phase: 10-cursor-polish-14-sc-re-verification
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on:
  - 10-02
files_modified:
  - plugins/default/__init__.py
  - sidecar/src/sidecar/compositor/plugin_adapter.py
  - sidecar/tests/plugins/test_default_plugin.py
  - sidecar/tests/compositor/test_plugin_adapter.py
  - sidecar/tests/test_orchestrator_turn.py
  - .planning/skeleton-verification.md
  - .planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
autonomous: false
requirements:
  - VFY-03
  - VFY-04
must_haves:
  truths:
    - "SC2-SMIRK-RENDERING is closed only when `[smirk]` produces a visible Teto face change in live UAT, not merely a `[DISPATCH]` log."
    - "`ActionCode(name='smirk')` routed through `PluginAdapter.enqueue_action_code` activates the active default plugin; do not rely only on plugin-side raw sentence parsing."
    - "The default smirk composition writes VTS-visible tracking input params that survive `clamp_and_validate` with active Teto capabilities."
    - "The fix preserves the Phase 7 strict action vocabulary: `joy` remains invalid/ignored for active Teto; `[smirk]` remains valid."
    - "ARCH-06 remains intact: no plugin or adapter imports pyvts and exactly one source file owns VTS writer identity."
  artifacts:
    - path: "plugins/default/__init__.py"
      provides: "DefaultPlugin action-code dispatch activation and visible smirk composition"
      contains: "def on_action_code"
    - path: "sidecar/tests/compositor/test_plugin_adapter.py"
      provides: "Regression that enqueue_action_code(ActionCode(name='smirk')) produces nonzero timed frames"
      contains: "ActionCode(name=\"smirk\")"
    - path: "sidecar/tests/plugins/test_default_plugin.py"
      provides: "Regression that smirk composition emits Teto-visible VTS input params after on_action_code"
      contains: "on_action_code"
    - path: ".planning/skeleton-verification.md"
      provides: "SC #2 verdict updated after live UAT rerun"
      contains: "SC2-SMIRK-RENDERING"
  key_links:
    - from: "sidecar/src/sidecar/orchestrator/orchestrator.py"
      to: "sidecar/src/sidecar/compositor/plugin_adapter.py"
      via: "_route_dispatches -> enqueue_action_code(ActionCode)"
      pattern: "enqueue_action_code"
    - from: "sidecar/src/sidecar/compositor/plugin_adapter.py"
      to: "plugins/default/__init__.py"
      via: "on_action_code callback on active plugin"
      pattern: "on_action_code"
---

<objective>
Close the `SC2-SMIRK-RENDERING` gap recorded during Phase 10 operator UAT. The parser and orchestrator dispatch path already work: logs showed `[DISPATCH] kind=action name=smirk`, but Teto's face did not visibly show smirk. This plan fixes the downstream motion path so `[smirk]` visibly changes the active Teto rig and updates `.planning/skeleton-verification.md` with the rerun verdict.

Purpose: make the Phase 10 SC #2 criterion honest and demonstrable: a valid `[smirk]` action must produce a visible smooth blend on the avatar, not just a parsed action-code event.
</objective>

<context>
@.planning/skeleton-verification.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
@plugins/default/plugin.yaml
@plugins/default/__init__.py
@sidecar/src/sidecar/compositor/plugin_adapter.py
@sidecar/src/sidecar/orchestrator/orchestrator.py
@sidecar/src/sidecar/compositor/clamp.py
@avatars/重音テト/_avatar_overrides.yaml
@Live2D/重音テト/重音テト.vtube.json

<observed_gap>
Operator UAT on 2026-05-09:
- `[smirk]` was emitted and parsed.
- Logs showed `[DISPATCH] kind=action name=smirk`.
- TTS completed normally.
- Teto's face did not visibly show smirk.

Current likely failure surfaces:
1. `DefaultPlugin` parses raw sentence text but does not implement `on_action_code`, so the explicit ActionCode dispatch path may not activate the plugin reliably.
2. The fallback smirk composition currently uses very small head inputs (`FaceAngleZ`, `FaceAngleY`) and may be visually too subtle or filtered by the active rig/capability surface.
3. Active Teto has no `default_plugin_action_bindings`; the fallback composition must be visibly sufficient on its own.
</observed_gap>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing dispatch-path regression for smirk action codes</name>
  <files>
    - sidecar/tests/plugins/test_default_plugin.py
    - sidecar/tests/compositor/test_plugin_adapter.py
  </files>
  <action>
    Add RED tests proving that a routed `ActionCode(name="smirk")` activates the default plugin and produces nonzero, VTS-visible output without depending on raw sentence text containing `[smirk]`.

    Required test coverage:
    - `DefaultPlugin.on_action_code(ActionCode(name="smirk"))` exists and sets an active action.
    - After `on_action_code`, `render_frame(now)` emits nonzero smirk params during the ramp window.
    - `PluginAdapter.enqueue_action_code(ActionCode(name="smirk"))` followed by `adapter.tick(now)` emits nonzero smirk params through the same path the orchestrator uses.
    - `ActionCode(name="joy")` remains ignored or unsupported for active Teto/default plugin semantics; do not resurrect `joy`.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/plugins/test_default_plugin.py tests/compositor/test_plugin_adapter.py -q</automated>
    Expected before implementation: new smirk action-code activation test fails because `DefaultPlugin` lacks `on_action_code`.
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement visible smirk activation through the dispatch callback</name>
  <files>
    - plugins/default/__init__.py
    - sidecar/src/sidecar/compositor/plugin_adapter.py
    - sidecar/tests/plugins/test_default_plugin.py
    - sidecar/tests/compositor/test_plugin_adapter.py
  </files>
  <action>
    Implement the narrow fix:
    - Add `DefaultPlugin.on_action_code(self, action: ActionCode) -> None`.
    - For supported actions, call the same `_activate(action.name.lower())` path used by `_extract_action_codes`.
    - For unsupported actions, no-op and log/debug if useful; do not raise from the compositor hot path.
    - Tune the fallback smirk composition to include at least one face/mouth/eye VTS input that is visibly meaningful on Teto and survives `clamp_and_validate`.

    Guardrails:
    - Do not call pyvts, trigger VTS hotkeys, or add a second writer.
    - Do not move model-owned variants such as `{heart-eye}` into `[action]`.
    - Do not re-add `joy` to the active action vocabulary.
    - Keep output as `ParamFrame` through the existing PluginAdapter/Compositor path.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/plugins/test_default_plugin.py tests/compositor/test_plugin_adapter.py tests/test_orchestrator_turn.py tests/orchestrator/test_dispatch_routing.py -q</automated>
    Expected: all pass; routed ActionCode smirk activates visible timed frames; dispatch audio payload semantics remain unchanged.
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live SC #2 rerun and verification artifact update</name>
  <files>
    - .planning/skeleton-verification.md
    - .planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
  </files>
  <action>
    Rerun only SC #2 live:
    1. Close the HUD and clear locks.
    2. Start VTube Studio with Teto loaded and sidecar authenticated.
    3. Send: `Reply with exactly one sentence about a cat sneaking into a bakery, and emit [smirk] somewhere in your reply.`
    4. Confirm logs still show `[DISPATCH] kind=action name=smirk`.
    5. Visually score the smirk blend:
       - entry gradual/faded
       - visible during the sentence
       - decay back to neutral is gradual

    Update `.planning/skeleton-verification.md`:
    - If 3/3 checks pass: SC #2 -> PASS and mark `SC2-SMIRK-RENDERING` resolved.
    - If 1-2/3: SC #2 -> PARTIAL with exact remaining issue.
    - If 0/3: keep FAIL and record new evidence.
  </action>
  <verify>
    <manual>Operator confirms SC #2 result and no `___PENDING___` markers remain in `.planning/skeleton-verification.md`.</manual>
  </verify>
</task>

</tasks>

<verification>
- Focused tests for default plugin and plugin adapter pass.
- Orchestrator dispatch routing tests pass.
- ARCH-06 single-writer grep remains exactly one pyvts import/callsite owner.
- Live SC #2 rerun changes the verdict based on observed avatar behavior, not logs alone.
</verification>

<success_criteria>
- [ ] `ActionCode(name="smirk")` activates `DefaultPlugin` through `on_action_code`.
- [ ] `PluginAdapter.enqueue_action_code(ActionCode(name="smirk"))` produces nonzero timed frames.
- [ ] Smirk fallback composition is visible on active Teto or the remaining limitation is explicitly recorded.
- [ ] `joy` remains absent/invalid for active Teto.
- [ ] `.planning/skeleton-verification.md` SC #2 verdict is updated from the rerun.
- [ ] `SC2-SMIRK-RENDERING` is either resolved or narrowed with fresh evidence.
</success_criteria>

<output>
Create `.planning/phases/10-cursor-polish-14-sc-re-verification/10-03-SUMMARY.md` with:
- root cause found for SC2-SMIRK-RENDERING
- code/test changes
- test commands and pass counts
- live SC #2 rerun verdict
- whether the gap is resolved or still partial/fail
</output>
