---
phase: 10-cursor-polish-14-sc-re-verification
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on:
  - 10-03
files_modified:
  - sidecar/src/sidecar/compositor/cursor_driver.py
  - sidecar/src/sidecar/compositor/param_id_resolver.py
  - sidecar/tests/compositor/test_cursor_driver.py
  - sidecar/tests/compositor/test_cursor_driver_namespace.py
  - sidecar/tests/compositor/test_cursor_driver_eye_tracking.py
  - .planning/skeleton-verification.md
  - .planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
autonomous: false
requirements:
  - VFY-01
  - VFY-02
  - VFY-03
  - VFY-04
must_haves:
  truths:
    - "SC5-EYE-TRACKING is closed only when live UAT shows both head and eye tracking; head-only tracking remains PARTIAL."
    - "Whole-screen cursor tracking remains accepted behavior; do not restore the in-VTS-window gate."
    - "CursorDriver preserves dead-zone behavior and synthetic primary-monitor fallback."
    - "Cursor eye output is symmetrical and explicit enough for VTS/Teto: horizontal and vertical eye intent must not be represented by only one left-eye X and one right-eye Y key unless live probing proves that is correct."
    - "ARCH-06 remains intact: cursor fixes do not import pyvts or create a second writer."
  artifacts:
    - path: "sidecar/src/sidecar/compositor/cursor_driver.py"
      provides: "CursorDriver producing visible head+eye tracking values"
      contains: "EyeLeft"
    - path: "sidecar/tests/compositor/test_cursor_driver_eye_tracking.py"
      provides: "Regression for full cursor eye-output surface"
      contains: "EyeRightX"
    - path: ".planning/skeleton-verification.md"
      provides: "SC #5 verdict updated after live UAT rerun"
      contains: "SC5-EYE-TRACKING"
  key_links:
    - from: "Live2D/重音テト/重音テト.vtube.json"
      to: "sidecar/src/sidecar/compositor/cursor_driver.py"
      via: "Teto ParameterSettings eye input mapping informs cursor eye output"
      pattern: "EyeLeftX"
    - from: "sidecar/src/sidecar/compositor/cursor_driver.py"
      to: "sidecar/src/sidecar/compositor/clamp.py"
      via: "VTS tracking input ranges and writable union preserve eye params"
      pattern: "VTS_TRACKING_INPUT_PARAM_IDS"
---

<objective>
Close the `SC5-EYE-TRACKING` gap recorded during Phase 10 operator UAT. Plan 10-01 made head tracking visible and intentionally enabled whole-screen cursor tracking, but live UAT still showed head-only tracking: eyes did not visibly follow the cursor. This plan investigates the VTS/Teto eye input surface, fixes the cursor eye output routing/tuning, and reruns SC #5.

Purpose: make the cursor criterion match its original requirement: visible avatar eye and head tracking. Whole-screen cursor tracking is not a failure; the gap is specifically missing visible eye motion.
</objective>

<context>
@.planning/skeleton-verification.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md
@sidecar/src/sidecar/compositor/cursor_driver.py
@sidecar/src/sidecar/compositor/param_id_resolver.py
@sidecar/src/sidecar/compositor/clamp.py
@sidecar/tests/compositor/test_cursor_driver.py
@Live2D/重音テト/重音テト.vtube.json
@Live2D/重音テト/重音テト.cdi3.json

<observed_gap>
Operator UAT on 2026-05-09:
- Head tracks cursor.
- Cursor continues tracking outside the VTS canvas; this is accepted and likely preferable.
- Eyes do not visibly track, matching a pre-existing OpenLLMVtuber issue where only head follows cursor.

Current code path:
- `CursorDriver` computes `ParamEyeBallX` and `ParamEyeBallY`.
- `resolve_param_id` maps those to `EyeLeftX` and `EyeRightY`.
- Teto `.vtube.json` maps `EyeLeftX -> ParamEyeBallX` and `EyeRightY -> ParamEyeBallY`.
- Live result is still head-only, so the generic resolver is insufficient for visible eye tracking.
</observed_gap>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add eye-surface regression and live-probe helper</name>
  <files>
    - sidecar/tests/compositor/test_cursor_driver_eye_tracking.py (CREATE)
    - sidecar/tests/compositor/test_cursor_driver.py
  </files>
  <action>
    Add RED/focused tests that encode the gap:
    - Cursor output for right/left/up/down positions includes explicit horizontal and vertical eye keys, not just `EyeLeftX` plus `EyeRightY`.
    - Dead-zone output zeroes every eye key that the cursor driver owns.
    - Synthetic-canvas fallback produces the same eye-output surface as VTS-rect projection.

    Add a lightweight developer probe if useful, but keep it test-only unless a repo pattern already exists. Do not add a second VTS writer. The preferred verification path is still through the existing compositor/writer.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/compositor/test_cursor_driver_eye_tracking.py -q</automated>
    Expected before implementation: fails because the current cursor output only includes `EyeLeftX` and `EyeRightY`.
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix cursor eye routing/tuning while preserving head tracking and whole-screen behavior</name>
  <files>
    - sidecar/src/sidecar/compositor/cursor_driver.py
    - sidecar/src/sidecar/compositor/param_id_resolver.py
    - sidecar/tests/compositor/test_cursor_driver.py
    - sidecar/tests/compositor/test_cursor_driver_namespace.py
    - sidecar/tests/compositor/test_cursor_driver_eye_tracking.py
  </files>
  <action>
    Investigate and implement the smallest cursor-eye fix that can plausibly move Teto's eyes:
    - Read `Live2D/重音テト/重音テト.vtube.json` ParameterSettings for eye inputs and ranges.
    - Test whether cursor should emit the full VTS eye-input surface: `EyeLeftX`, `EyeRightX`, `EyeLeftY`, `EyeRightY`.
    - If VTS only honors one horizontal and one vertical input for this Teto rig, document that and tune values strongly enough to be visible.
    - If live probing shows direct `ParamEyeBallX/Y` additive writes are required for this rig, isolate that behind a rig-aware resolver/override; do not make arbitrary direct Cubism writes global for every avatar.

    Guardrails:
    - Keep `FaceAngleX/Y` head tracking working.
    - Keep outside-window/whole-screen projection.
    - Keep dead-zone zero output.
    - Preserve `VTS_TRACKING_INPUT_PARAM_IDS` membership for default cursor output unless a rig-specific override is explicitly documented and tested.
    - Do not introduce pyvts imports outside `pyvts_writer.py`.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/compositor/test_cursor_driver.py tests/compositor/test_cursor_driver_namespace.py tests/compositor/test_cursor_driver_eye_tracking.py tests/vts/test_window_detect.py tests/test_arch06_single_writer.py -q</automated>
    Expected: all pass; cursor tests assert head and full eye-output behavior.
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live SC #5 rerun and verification artifact update</name>
  <files>
    - .planning/skeleton-verification.md
    - .planning/phases/10-cursor-polish-14-sc-re-verification/deferred-items.md
  </files>
  <action>
    Rerun only SC #5 live:
    1. Start VTube Studio with Teto loaded and sidecar authenticated.
    2. Move cursor slowly left/right and up/down over the VTS window.
    3. Move cursor outside the VTS window over the primary monitor.
    4. Stop at an off-axis point.
    5. Score:
       - head tracking visible
       - eye tracking visible
       - outside-window tracking remains visible and steady

    Update `.planning/skeleton-verification.md`:
    - 3/3 -> SC #5 PASS and mark `SC5-EYE-TRACKING` resolved.
    - 1-2/3 -> SC #5 PARTIAL with exact residual issue.
    - 0/3 -> SC #5 FAIL with diagnosis.

    Recompute the Phase 10 milestone close decision after combining SC #2 and SC #5 reruns.
  </action>
  <verify>
    <manual>Operator confirms SC #5 result and `.planning/skeleton-verification.md` has zero `___PENDING___` markers.</manual>
  </verify>
</task>

</tasks>

<verification>
- Focused cursor/head/eye tests pass.
- Window-detection fallback tests still pass.
- ARCH-06 single-writer tests and grep remain green.
- Live SC #5 rerun records whether head+eye tracking is actually visible.
- `skeleton-verification.md` and `deferred-items.md` reflect resolved or remaining gap state.
</verification>

<success_criteria>
- [ ] Cursor head tracking remains visible.
- [ ] Cursor eye tracking is visibly present in live UAT, or the residual failure is narrowed with evidence.
- [ ] Whole-screen/outside-VTS tracking remains accepted and does not regress.
- [ ] Cursor dead-zone remains stable and avoids stationary jitter.
- [ ] No second pyvts writer or direct plugin/driver pyvts import is introduced.
- [ ] `.planning/skeleton-verification.md` SC #5 and milestone close decision are updated.
</success_criteria>

<output>
Create `.planning/phases/10-cursor-polish-14-sc-re-verification/10-04-SUMMARY.md` with:
- eye-tracking root cause
- VTS/Teto eye input mapping chosen
- code/test changes
- test commands and pass counts
- live SC #5 rerun verdict
- final Phase 10 close status after SC #2 and SC #5 gap closure
</output>
