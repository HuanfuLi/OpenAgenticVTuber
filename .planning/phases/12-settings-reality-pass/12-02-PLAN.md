---
phase: 12-settings-reality-pass
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on:
  - 12-01
files_modified:
  - apps/renderer/src/screens/Settings/Settings.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/tests/Settings.test.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-06
must_haves:
  truths:
    - "Avatar Settings must never render a contradictory degraded state such as Avatar ID unknown plus copy saying the current ID is known."
    - "When the current avatar ID is available but metadata is not, the UI must show the ID and explain that catalog metadata is unavailable."
    - "Edit current must not be permanently disabled after a transient sidecar/current-plan miss; clicking it retries current-plan loading and either opens Avatar Import review or shows an actionable unavailable message."
    - "When sidecar status becomes ready after Settings mounted, Avatars should retry current metadata so a startup race does not leave Teto degraded forever."
    - "Diagnostics Log level must explain what error/warn/info/debug mean in user-facing language."
  artifacts:
    - path: "apps/renderer/src/screens/Settings/Settings.tsx"
      provides: "Resilient Avatar current-plan loading and descriptive log-level UX"
      contains: "AvatarsSection"
    - path: "apps/renderer/src/lib/copy.ts"
      provides: "Avatar degraded-state and log-level description copy"
      contains: "DIAG_LOG_LEVEL_DESCRIPTIONS"
    - path: "apps/renderer/tests/Settings.test.tsx"
      provides: "Regression tests for UAT gaps"
      contains: "routes Edit current"
---

<objective>
Close Phase 12 UAT gaps from `12-UAT.md`.

Purpose: make the Avatars Settings section robust against sidecar startup/current-plan timing and make Diagnostics log levels understandable. This plan fixes only the two UAT issues; it does not add saved conversation history, Memory, or broader logging behavior.
</objective>

<context>
@.planning/phases/12-settings-reality-pass/12-UAT.md
@.planning/phases/12-settings-reality-pass/12-01-SUMMARY.md
@apps/renderer/src/screens/Settings/Settings.tsx
@apps/renderer/src/lib/copy.ts
@apps/renderer/tests/Settings.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gap-regression tests</name>
  <files>
    - apps/renderer/tests/Settings.test.tsx
  </files>
  <behavior>
    - Test that if current-plan metadata is initially unavailable but the current avatar ID is available, Avatars shows that ID, not `unknown`, and does not use contradictory copy.
    - Test that `Edit current` retries current-plan loading after an initial miss and opens Avatar Import when the retry succeeds.
    - Test that log-level UI shows user-facing descriptions for all four levels or at least the selected level in a way that names what the selected level includes.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    Failing tests capture both UAT gaps before implementation changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Avatar current-state loading and edit retry</name>
  <files>
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/src/lib/copy.ts
  </files>
  <behavior>
    - Current avatar ID loading is independent from current-plan metadata loading.
    - If current-plan metadata is unavailable, Avatars shows the current ID when known and uses copy that does not claim more than the UI can prove.
    - `Edit current` is available when a current ID is known. It retries `getCurrentAvatarPlan()` on click and opens Avatar Import review if metadata is returned.
    - Settings retries current-plan metadata after sidecar status becomes green so startup timing does not strand the UI in degraded state.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    User starting with imported Teto does not see `unknown` as the avatar ID, and transient metadata misses can recover without relaunching.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add descriptive log-level UX</name>
  <files>
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/tests/Settings.test.tsx
  </files>
  <behavior>
    - Log level select still persists `error/warn/info/debug`.
    - The UI explains what each level means, either as a compact list or as a selected-level description panel.
    - Copy is concise and operational: users can tell that error is quietest, debug is most verbose, and warn/info sit between them.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    Diagnostics log level is understandable without external explanation and existing Phase 12 tests remain green.
  </done>
</task>

</tasks>

<verification>
Run:

```powershell
npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx
npm --workspace apps/renderer run typecheck
```

Manual UAT re-check:
- Start the app with Teto imported.
- Open Settings > Avatars. Confirm Avatar ID is not `unknown`; if metadata is still unavailable, copy is truthful and non-contradictory.
- Confirm `Edit current` retries and opens Avatar Import review when current metadata is available.
- Open Settings > Diagnostics. Confirm log level descriptions make `error/warn/info/debug` understandable.
</verification>

<success_criteria>
- Phase 12 UAT Test 2 gap has a passing automated regression and a plausible manual retry path.
- Phase 12 UAT Test 5 gap has a passing automated regression and clearer user-facing copy.
- No saved conversation history, Memory implementation, or unrelated mock-boundary cleanup is introduced.
- Focused renderer tests and typecheck pass.
</success_criteria>
