---
phase: 12-settings-reality-pass
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on:
  - 12-03
files_modified:
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/tests/Settings.test.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
must_haves:
  truths:
    - "Known-ID degraded avatar copy must be understandable without knowing what catalog metadata means."
    - "The copy must distinguish a known avatar ID from a loaded editable saved catalog."
    - "The copy must give a concrete next action: wait for sidecar/status to finish, retry Edit current, or use Import/replace if no saved catalog exists."
  artifacts:
    - path: "apps/renderer/src/lib/copy.ts"
      provides: "Plain-language known-ID degraded avatar copy"
      contains: "AVATARS_DEGRADED"
    - path: "apps/renderer/tests/Settings.test.tsx"
      provides: "Regression that confusing catalog metadata copy is not rendered"
      contains: "degraded avatar state"
---

<objective>
Close the remaining Phase 12 re-UAT wording gap.

Purpose: keep the avatar behavior from 12-03 unchanged, but replace confusing `Catalog metadata is unavailable...` copy with plain-language guidance that explains what is happening and what the user should do.
</objective>

<context>
@.planning/phases/12-settings-reality-pass/12-UAT.md
@.planning/phases/12-settings-reality-pass/12-03-SUMMARY.md
@apps/renderer/src/lib/copy.ts
@apps/renderer/src/screens/Settings/Settings.tsx
@apps/renderer/tests/Settings.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED copy regression</name>
  <files>
    - apps/renderer/tests/Settings.test.tsx
  </files>
  <behavior>
    - In the known-ID/no-plan degraded avatar test, assert the old confusing phrase `Catalog metadata is unavailable` is not rendered.
    - Assert the replacement copy names the saved editable avatar catalog in plain language.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    The current copy fails before implementation changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace known-ID degraded copy</name>
  <files>
    - apps/renderer/src/lib/copy.ts
  </files>
  <behavior>
    - Replace `AVATARS_DEGRADED` with copy similar to: `Avatar ID is known, but the saved editable catalog has not loaded yet. Wait for the sidecar status to finish loading, then try Edit current again. If this avatar was never imported, use Import/replace.`
    - Keep `AVATARS_DEGRADED_UNKNOWN` separate for the no-ID/no-catalog state.
    - Do not change avatar loading, resolver, or routing behavior.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    The degraded known-ID state is understandable without external explanation.
  </done>
</task>

</tasks>

<verification>
Run:

```powershell
npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx
npm --workspace apps/renderer run typecheck
```

Manual UAT re-check:
- Open Settings > Avatars in the known-ID/no-catalog-details state.
- Confirm the message explains that the avatar ID is known but the saved editable catalog has not loaded yet.
- Confirm the message no longer uses the phrase `Catalog metadata is unavailable`.
</verification>

<success_criteria>
- Phase 12 UAT Test 2 has no remaining unclear warning-copy issue.
- No behavior changes outside Settings copy/tests.
- Focused renderer tests and typecheck pass.
</success_criteria>
