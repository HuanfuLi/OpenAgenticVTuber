---
phase: 12-settings-reality-pass
plan: 03
type: execute
wave: 1
gap_closure: true
depends_on:
  - 12-02
files_modified:
  - apps/electron-main/src/ipc.ts
  - apps/electron-main/src/sidecar.ts
  - apps/electron-main/src/window-store.ts
  - apps/renderer/src/screens/Settings/Settings.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/tests/Settings.test.tsx
  - sidecar/tests/avatar/test_reimport.py
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-07
must_haves:
  truths:
    - "Settings > Avatars must not enable Edit current when no current avatar ID is known."
    - "The current avatar ID used by Settings, Avatar Import re-edit, and sidecar boot must resolve to a persisted avatar override catalog, not a stale hardcoded fallback."
    - "Edit current must load the saved _avatar_overrides.yaml rows for the active avatar after save/restart, including user-edited variant/event codes."
    - "After saving Avatar Import edits, returning to Settings and clicking Edit current again must reopen the editable saved catalog instead of showing Current avatar catalog is not available to edit."
    - "Settings > About must show the current v2.1 milestone/version label instead of 0.1.0-skeleton."
  artifacts:
    - path: "apps/electron-main/src/ipc.ts"
      provides: "Current-avatar ID and plan loading that validates persisted override catalogs"
      contains: "avatar:getCurrentId, avatar:getCurrentPlan, avatar:commitOverrides"
    - path: "apps/electron-main/src/sidecar.ts"
      provides: "Sidecar active avatar env var derived from the same resolved current avatar ID"
      contains: "AGENTICLLMVTUBER_ACTIVE_AVATAR"
    - path: "apps/renderer/src/screens/Settings/Settings.tsx"
      provides: "Correct Edit current disabled/retry state"
      contains: "AvatarsSection"
    - path: "apps/renderer/src/lib/copy.ts"
      provides: "Current About version/milestone copy"
      contains: "ABOUT_VERSION_VAL"
    - path: "apps/renderer/tests/Settings.test.tsx"
      provides: "Renderer regressions for unknown ID disabled state, re-edit routing, and About version copy"
      contains: "Avatars Settings"
---

<objective>
Close the remaining Phase 12 UAT gaps from `12-UAT.md`.

Purpose: make Settings > Avatars re-edit the real active persisted catalog reliably across startup/save/restart, and remove stale skeleton copy from Settings > About. This plan does not add saved chat history, Memory, new avatar extractor formats, or broader version-management infrastructure.
</objective>

<context>
@.planning/phases/12-settings-reality-pass/12-UAT.md
@.planning/phases/12-settings-reality-pass/12-02-SUMMARY.md
@apps/renderer/src/screens/Settings/Settings.tsx
@apps/renderer/src/screens/AvatarImport/AvatarImport.tsx
@apps/renderer/src/lib/copy.ts
@apps/electron-main/src/ipc.ts
@apps/electron-main/src/sidecar.ts
@apps/electron-main/src/window-store.ts
@sidecar/src/sidecar/admin/avatar.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED regressions for current avatar edit failures</name>
  <files>
    - apps/renderer/tests/Settings.test.tsx
    - sidecar/tests/avatar/test_reimport.py
  </files>
  <behavior>
    - Add a renderer test where `getCurrentAvatarId()` returns an empty string and `getCurrentAvatarPlan()` returns null; Avatars shows the unknown/unavailable state and `Edit current` is disabled.
    - Add a renderer test where a current plan contains user-edited variant/event rows; `Edit current` routes to Avatar Import with that exact persisted plan, not a fresh empty/default table.
    - Add a renderer test for the post-save/re-edit recovery path: an initial plan miss followed by a saved current plan should route on retry, and should not leave only `Current avatar catalog is not available to edit.`
    - Add/extend sidecar reimport coverage so `/admin/avatar/import/current` returns the persisted `_avatar_overrides.yaml` variants/events and commit preserves those edited rows.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
    <automated>cd sidecar; uv run pytest tests/avatar/test_reimport.py tests/avatar/test_admin_avatar.py</automated>
  </verify>
  <done>
    Failing tests reproduce the user-reported unknown-ID enabled button and saved-edit loss/re-edit miss before implementation changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve current avatar ID from persisted catalogs</name>
  <files>
    - apps/electron-main/src/ipc.ts
    - apps/electron-main/src/sidecar.ts
    - apps/electron-main/src/window-store.ts
  </files>
  <behavior>
    - Replace the blind `currentAvatarId || 'teto'` fallback with a resolver that validates the stored ID has `avatars/{id}/_avatar_overrides.yaml`.
    - If the stored ID is missing, stale, or points to a folder without overrides, choose a single available persisted catalog under `avatars/*/_avatar_overrides.yaml` and persist that ID.
    - If no persisted catalog exists, return an empty/unknown ID to the renderer and do not pretend `teto` is editable.
    - Use the same resolver for `avatar:getCurrentId`, `avatar:getCurrentPlan`, `avatar:commitOverrides` post-save persistence, and `AGENTICLLMVTUBER_ACTIVE_AVATAR` during sidecar spawn.
    - Keep the resolver filesystem-bound and deterministic; do not query VTube Studio for this Phase 12 gap.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    A repo with only `avatars/重音テト/_avatar_overrides.yaml` resolves the active avatar as `重音テト`, not stale `teto`, and a repo with no overrides reports no editable current avatar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix Settings Avatars edit availability and retry semantics</name>
  <files>
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/tests/Settings.test.tsx
  </files>
  <behavior>
    - Disable `Edit current` when no current avatar ID is known.
    - Enable `Edit current` only when the current ID is known and the UI can retry metadata, but show an actionable unavailable notice if the retry still cannot load a persisted catalog.
    - After a current-plan load succeeds, cache and route that plan so Avatar Import receives saved variants/events from persisted overrides.
    - Clear stale notices on successful retry and when sidecar/current-plan state refreshes.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    Startup no-ID state has a disabled Edit current button, and valid-ID re-edit opens the saved catalog consistently after save/restart.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update About version/milestone copy</name>
  <files>
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/tests/Settings.test.tsx
  </files>
  <behavior>
    - Replace `ABOUT_VERSION_VAL: '0.1.0-skeleton'` with current milestone copy, e.g. `v2.1 Mock/Reality Cleanup`.
    - Add a Settings regression asserting About does not render `0.1.0-skeleton` and does render the current milestone label.
    - Keep package.json version fields unchanged unless the implementation already has a central app-version source; this plan only fixes user-facing Settings copy.
  </behavior>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    Settings > About no longer shows the stale skeleton label and reflects the current milestone.
  </done>
</task>

</tasks>

<verification>
Run:

```powershell
npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx
npm --workspace apps/renderer run typecheck
cd sidecar; uv run pytest tests/avatar/test_reimport.py tests/avatar/test_admin_avatar.py
```

Manual UAT re-check:
- Start the app with no editable current avatar resolved. Settings > Avatars should show unavailable/unknown state and `Edit current` disabled.
- Start the app with the existing imported Teto catalog under `avatars/重音テト`. Settings > Avatars should show `重音テト` and current catalog counts.
- Click `Edit current`; Avatar Import should show saved edited variant/event rows from `_avatar_overrides.yaml`.
- Save edits, return to Settings, and click `Edit current` again. It should reopen the saved catalog, not show `Current avatar catalog is not available to edit.`
- Open Settings > About. Version should show the current v2.1 milestone label.
</verification>

<success_criteria>
- Phase 12 UAT Test 2 has passing regressions for unknown-ID disabled state and persisted current-catalog re-edit.
- Phase 12 UAT Test 6 has a passing regression for current About version/milestone copy.
- No conversation history, memory, VTS runtime querying, or new avatar extractor scope is introduced.
- Focused renderer tests, renderer typecheck, and sidecar avatar admin tests pass.
</success_criteria>
