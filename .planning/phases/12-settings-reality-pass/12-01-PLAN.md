---
phase: 12-settings-reality-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/renderer/src/screens/Settings/Settings.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/src/state/app-store.tsx
  - apps/electron-main/preload/index.ts
  - apps/electron-main/preload/index.d.ts
  - apps/electron-main/src/ipc.ts
  - apps/electron-main/src/window-store.ts
  - apps/electron-main/src/sidecar.ts
  - apps/renderer/tests/Settings.test.tsx
  - apps/renderer/tests/AppStoreStatus.test.tsx
  - apps/renderer/tests/StatusIcon.test.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06
  - SET-07
must_haves:
  truths:
    - "D-01/D-02: Settings has one combined Avatars section, not separate stale Avatars / Avatar catalogs / Per-avatar settings sections; it exposes both Edit current and Import/replace"
    - "D-03/D-04: Avatars shows active identity plus catalog counts when current metadata loads, and a truthful degraded state when it does not; it never silently assumes Teto"
    - "D-05/D-06: VTube Studio remains a compact Settings block mirroring live VTS status while the upper-right Status popover remains the primary status surface"
    - "D-07/D-08: VTS troubleshooting actions include Restart sidecar and Reset/re-auth VTS token, behind a troubleshooting disclosure"
    - "D-09/D-10: Conversation Settings is a truth summary only: single in-memory thread, clears on relaunch, no saved-history/session controls"
    - "D-11: Memory Settings is visible but disabled and explicitly says memory ships with v4.0 agentic + memory"
    - "D-12/D-13/D-14: Log level is a real persisted renderer/main-process preference with error/warn/info/debug choices"
    - "SET-07: Settings copy references current milestone intent: v3.0 for STT/TTS where relevant, v4.0 for agentic system plus memory; no target Phase 12 section says Coming in milestone-2"
  artifacts:
    - path: "apps/renderer/src/screens/Settings/Settings.tsx"
      provides: "Real Settings sections for Avatars, VTube Studio, Conversation, Memory, and functional Log level"
      contains: "VTube Studio"
    - path: "apps/renderer/src/lib/copy.ts"
      provides: "Centralized truthful Settings copy and log-level labels"
      contains: "v4.0"
    - path: "apps/electron-main/preload/index.ts"
      provides: "Renderer bridge methods for current avatar, VTS token reset, and log-level preference"
      contains: "resetVtsAuth"
    - path: "apps/electron-main/src/ipc.ts"
      provides: "Main-process handlers for current avatar lookup, VTS token reset/re-auth, and log-level preference"
      contains: "vts:resetAuth"
    - path: "apps/electron-main/src/window-store.ts"
      provides: "Persistent log-level preference using the existing electron-store pattern"
      contains: "logLevel"
    - path: "apps/renderer/tests/Settings.test.tsx"
      provides: "Regression tests for Phase 12 Settings reality requirements"
      contains: "v4.0"
  key_links:
    - from: "apps/renderer/src/screens/Settings/Settings.tsx"
      to: "apps/electron-main/preload/index.ts"
      via: "window.api current avatar / VTS troubleshooting / log-level methods"
      pattern: "window.api"
    - from: "apps/electron-main/src/ipc.ts"
      to: "sidecar/src/sidecar/admin/avatar.py"
      via: "GET /admin/avatar/import/current?avatar_id=${currentAvatarId}"
      pattern: "/admin/avatar/import/current"
    - from: "apps/electron-main/src/ipc.ts"
      to: "sidecar/.vts_token.txt"
      via: "reset/re-auth action deletes the stored token then restarts the sidecar"
      pattern: ".vts_token.txt"
---

<objective>
Replace stale Settings placeholders with real or truthfully disabled Settings sections for Phase 12.

Purpose: satisfy SET-01 through SET-07 and translate every Phase 12 CONTEXT decision into an executable implementation. The resulting Settings screen should show real avatar catalog state/actions, compact real VTS status plus troubleshooting, truthful current Conversation behavior, disabled Memory v4.0 copy, and a real renderer/main-process log-level preference. It must not implement saved conversation history or Memory.

Output: one coherent Settings pass across renderer copy/components, Electron preload/IPC/store APIs, and focused regression tests.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-settings-reality-pass/12-CONTEXT.md
@.planning/phases/12-settings-reality-pass/12-RESEARCH.md

@apps/renderer/src/screens/Settings/Settings.tsx
@apps/renderer/src/lib/copy.ts
@apps/renderer/src/state/app-store.tsx
@apps/renderer/src/chrome/StatusIcon.tsx
@apps/renderer/src/screens/AvatarImport/AvatarImport.tsx
@apps/electron-main/preload/index.ts
@apps/electron-main/preload/index.d.ts
@apps/electron-main/src/ipc.ts
@apps/electron-main/src/window-store.ts
@apps/electron-main/src/sidecar.ts
@sidecar/src/sidecar/admin/avatar.py
@sidecar/src/sidecar/admin/status.py
@sidecar/src/sidecar/vts/pyvts_writer.py

<interfaces>
Existing renderer store surface:
```ts
const {
  status, refreshStatus, restartSidecar,
  chatMessages, setChatMessages,
  logsDrawer, setLogsDrawer,
  setView
} = useStore()
```

Existing VTS status bridge:
```ts
window.api.getVtsStatus(): Promise<{
  state: 'authenticated' | 'auth_pending' | 'not_authenticated' |
    'sidecar_unconfigured' | 'vts_window_not_found' | 'unavailable'
  detail: string
  authenticated: boolean
  windowDetected: boolean
}>
window.api.restartSidecar(): Promise<void>
```

Existing avatar endpoint:
```http
GET /admin/avatar/import/current?avatar_id=<id>
```
returns `AvatarImportPlan` with `avatar_id`, `avatar_name`, `source_rig_path`,
`variants`, `events`, `voice`, and `existing_overrides`.

Existing current avatar storage:
```ts
store.get('currentAvatarId') // default 'teto'
store.set('currentAvatarId', plan.avatar_id || 'teto') // after commit
```

New bridge methods this plan should add or equivalent:
```ts
getCurrentAvatarId(): Promise<string>
getCurrentAvatarPlan(): Promise<AvatarImportPlan | null>
resetVtsAuth(): Promise<void>
getLogLevel(): Promise<'error' | 'warn' | 'info' | 'debug'>
saveLogLevel(level: 'error' | 'warn' | 'info' | 'debug'): Promise<void>
```

Do not expose `sidecar/.vts_token.txt` to renderer. Token reset is a high-level Electron-main action.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Electron bridge support for current avatar, VTS re-auth, and log level</name>
  <files>
    - apps/electron-main/src/window-store.ts
    - apps/electron-main/src/ipc.ts
    - apps/electron-main/preload/index.ts
    - apps/electron-main/preload/index.d.ts
    - apps/electron-main/src/sidecar.ts
  </files>
  <behavior>
    - Renderer can request current avatar id and current avatar plan without knowing Electron store internals.
    - If sidecar/current-avatar metadata is unavailable, current avatar plan returns `null` or throws a handled error; renderer can still get current avatar id.
    - Renderer can invoke `resetVtsAuth()`, which deletes the VTS auth token if present and restarts the sidecar.
    - Renderer can read and save a persisted log-level preference with allowed values `error`, `warn`, `info`, `debug`, defaulting to `info`.
  </behavior>
  <action>
    Extend `window-store.ts` with `logLevel` in the schema/defaults and helpers `getLogLevel()` / `saveLogLevel(level)`. Validate allowed log-level values before persisting.

    Extend `ipc.ts` with handlers:
    - `avatar:getCurrentId` returning `store.get('currentAvatarId') || 'teto'`
    - `avatar:getCurrentPlan` fetching `${getSidecarHttpUrl()}/admin/avatar/import/current?avatar_id=${encodeURIComponent(currentAvatarId)}` and returning `null` on 404/unavailable so Settings can degrade truthfully
    - `vts:resetAuth` deleting the token file and calling `restartSidecar()`
    - `log:getLevel` / `log:saveLevel`

    Place token-path resolution in `sidecar.ts` or a tiny Electron-main helper so `ipc.ts` does not duplicate repo-root logic. Match the sidecar default token path used by `PyvtsSafeWriter`: `<repo>/sidecar/.vts_token.txt`.

    Extend `preload/index.ts` and `preload/index.d.ts` types with the new methods. Use names that are explicit and stable; if names differ from the suggested interface, update tests and Settings consistently.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    Renderer has typed bridge access to current avatar metadata, VTS re-auth, and persisted log-level preference without direct filesystem or pyvts access.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace stale Settings placeholders with truthful sections</name>
  <files>
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/src/state/app-store.tsx
  </files>
  <behavior>
    - One combined `Avatars` section replaces the old avatar catalog + avatar/per-avatar placeholders.
    - `Avatars` shows active id/name, source path, variant count, event count, and voice when current metadata loads.
    - `Avatars` shows truthful degraded state if metadata cannot load, never assuming Teto unless Electron store says current id is `teto`.
    - `Avatars` exposes both `Edit current` and `Import/replace`.
    - Compact `VTube Studio` section mirrors `status.vts` / `status.vtsDetail`, keeps upper-right Status as primary, and has a `Troubleshooting` disclosure with `Restart sidecar` and `Reset/re-auth VTS token`.
    - `Conversation` section states single in-memory thread and reset-on-relaunch behavior. It does not offer saved sessions/history controls.
    - `Memory` section is visible but disabled and says Memory ships with v4.0 agentic + memory.
    - Diagnostics log-level select is enabled, persists `error/warn/info/debug`, and no longer says `Coming in milestone-2.`
    - Target Phase 12 sections do not render stale milestone-2 copy.
  </behavior>
  <action>
    Replace `PLACEHOLDERS` rendering for nums 2, 3, 4, 7, and 8 with explicit section components:
    - `AvatarsSection`
    - `VTubeStudioSection`
    - `ConversationSection`
    - `MemorySection`

    Keep the existing long-scroll section style and anchor pills. Update anchors to point to the new explicit section ids.

    Avatar actions:
    - `Edit current`: load current avatar plan through the new bridge, then open the existing AvatarImport review experience pre-populated. If the current `AvatarImport` component has no route-state support, extend it minimally to auto-load/edit current via a prop or store field.
    - `Import/replace`: preserve the existing folder-picker import path.

    VTS actions:
    - `Restart sidecar`: call `useStore().restartSidecar()`.
    - `Reset/re-auth VTS token`: call `window.api.resetVtsAuth()`, then `refreshStatus()`.
    - Put both under a native `<details>` or equivalent small disclosure.

    Log level:
    - Add log-level state in Settings or AppStore.
    - Hydrate from `window.api.getLogLevel()`.
    - Save through `window.api.saveLogLevel(level)` on select change.
    - Keep the first implementation scoped to renderer/main-process UI logging. If practical, gate `console.debug/info/warn/error` or main forwarded logs by the preference; at minimum, the preference must be real, persisted, and exercised by tests.

    Update `COPY.SETTINGS` with specific strings. Remove or stop using stale `PLACEHOLDERS` entries for Phase 12 sections. Preserve future sections not in Phase 12 unless SET-07 requires updated wording, e.g. voice/STT says v3.0 and agent/memory says v4.0.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx</automated>
  </verify>
  <done>
    Settings renders truthful Phase 12 sections and log-level control with no stale milestone-2 copy in the targeted sections.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage and run focused verification</name>
  <files>
    - apps/renderer/tests/Settings.test.tsx
    - apps/renderer/tests/AppStoreStatus.test.tsx
    - apps/renderer/tests/StatusIcon.test.tsx
  </files>
  <behavior>
    - Tests prove `Avatars` combined section shows counts and both actions.
    - Tests prove avatar metadata failure shows degraded copy and does not hardcode Teto unless current id is Teto.
    - Tests prove VTS section mirrors live status and troubleshooting buttons call restart/reset APIs only from the disclosure.
    - Tests prove Conversation is truth-only and saved-history controls are absent.
    - Tests prove Memory is disabled and references v4.0 agentic + memory.
    - Tests prove Log level select persists `error/warn/info/debug`.
    - Tests prove relevant stale copy is absent: no `Coming in milestone-2` in target sections, no fake conversation-history controls, and no old avatar/per-avatar placeholders.
  </behavior>
  <action>
    Update `Settings.test.tsx` with focused Phase 12 tests. Reuse the existing `window.api` mock pattern and add mocks for new bridge methods.

    Update `AppStoreStatus.test.tsx` only if the log-level preference or VTS troubleshooting integration changes store behavior. Keep Phase 11 status tests green.

    Update `StatusIcon.test.tsx` only if status refresh API names change. The Status popover remains the primary VTS status surface and should still refresh through real APIs.

    Run focused renderer tests and typecheck. Run sidecar status/avatar endpoint tests if any Electron bridge changes rely on endpoint behavior.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
    <automated>python -m pytest sidecar/tests/admin/test_status_endpoint.py sidecar/tests/avatar/test_admin_avatar.py -q</automated>
  </verify>
  <done>
    Focused tests and typecheck pass, and Phase 12 Settings decisions are guarded against regression.
  </done>
</task>

</tasks>

<verification>
Run these checks after all tasks:

```powershell
npm --workspace apps/renderer run test -- Settings.test.tsx AppStoreStatus.test.tsx StatusIcon.test.tsx
npm --workspace apps/renderer run typecheck
python -m pytest sidecar/tests/admin/test_status_endpoint.py sidecar/tests/avatar/test_admin_avatar.py -q
```

Manual/UAT spot check:
- Open Settings.
- Confirm one `Avatars` section shows current avatar identity/counts or a truthful degraded state.
- Confirm `Edit current` opens the current avatar catalog review, while `Import/replace` starts folder import.
- Confirm `VTube Studio` is compact, mirrors live status, and shows restart/re-auth only in troubleshooting.
- Confirm Conversation says single in-memory thread/reset-on-relaunch, Memory says v4.0 disabled, and Log level persists.
</verification>

<success_criteria>
- SET-01 through SET-07 are covered in one executable plan.
- Every trackable Phase 12 CONTEXT decision D-01 through D-14 appears in plan truths/tasks.
- Settings no longer renders stale placeholders for Avatars, Per-avatar settings, VTube Studio, Conversation, Memory, or Log level.
- VTS control remains high-level through Electron main; renderer never touches pyvts or token files directly.
- Saved conversation history and Memory implementation remain deferred.
- Focused renderer and sidecar tests pass or failures are documented in the execution summary.
</success_criteria>
