---
phase: 04-action-compositor-vts-bridge-body-sway-investigation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecar/src/sidecar/compositor/cursor_driver.py
  - sidecar/src/sidecar/vts/window_detect.py
  - sidecar/tests/compositor/test_cursor_driver.py
  - sidecar/tests/vts/test_window_detect.py
autonomous: true
gap_closure: true
requirements:
  - AVT-10
must_haves:
  truths:
    - "Sidecar polls the OS cursor and VTube Studio window bounds without renderer involvement on the cursor hot path"
    - "Cursor movement inside the detected VTS window produces head and eye tracking params with the existing 80px face-center dead zone"
    - "Cursor exit or unavailable VTS bounds eases avatar gaze/head back to center over the existing 800ms cubic curve"
  artifacts:
    - path: "sidecar/src/sidecar/vts/window_detect.py"
      provides: "Win32 VTS window discovery, bounds refresh, and cursor-position polling"
      contains: "GetWindowRect"
    - path: "sidecar/src/sidecar/compositor/cursor_driver.py"
      provides: "AVT-10 sidecar cursor driver using Win32 cursor/window samples"
      contains: "get_cursor_and_rect"
    - path: "sidecar/tests/compositor/test_cursor_driver.py"
      provides: "Regression tests for cursor math, dead zone, and ease-back"
      contains: "CursorDriver"
    - path: "sidecar/tests/vts/test_window_detect.py"
      provides: "Regression tests for Win32 window detection caching/fallback"
      contains: "find_vts_hwnd"
  key_links:
    - from: "sidecar/src/sidecar/compositor/cursor_driver.py"
      to: "sidecar/src/sidecar/vts/window_detect.py"
      via: "get_cursor_and_rect() called from CursorDriver.tick()"
      pattern: "get_cursor_and_rect\\("
    - from: "sidecar/src/sidecar/compositor/compositor.py"
      to: "sidecar/src/sidecar/compositor/cursor_driver.py"
      via: "cursor tick output merged into ParamFrame.add_params"
      pattern: "_cursor\\.tick"
---

<objective>
Close the AVT-10 verification gap by documenting and tightening the accepted equivalent cursor contract from locked decisions D-09 and D-11: sidecar Win32 window-bounds detection plus cursor polling is authoritative.

Purpose: The verification report used stale renderer-overlay wording, but `04-CONTEXT.md` explicitly dropped renderer-side cursor/window-bounds detection. This plan must not add renderer overlay code, renderer WS messages, or renderer-authoritative cursor events. It proves the sidecar-only cursor path satisfies Phase 4's observable behavior.
Output: Focused sidecar cursor/window-detection tests and code comments/docstrings that make the D-09/D-11 contract explicit.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-CONTEXT.md
@.planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-VERIFICATION.md
@.planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-VALIDATION.md
@.planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-03-SUMMARY.md
@sidecar/src/sidecar/compositor/cursor_driver.py
@sidecar/src/sidecar/vts/window_detect.py
@sidecar/tests/compositor/test_cursor_driver.py
@sidecar/tests/vts/test_window_detect.py

<interfaces>
Locked cursor decisions from 04-CONTEXT.md:
```text
D-09: VTS window-bounds detection = sidecar Win32 polling at ~250ms.
No renderer involvement on the hot path.

D-11 / deferred: Renderer-side cursor + window-bounds detection explicitly dropped.
```

Current cursor-driver contract:
```python
def get_cursor_and_rect() -> tuple[tuple[int, int], tuple[int, int, int, int] | None]: ...

class CursorDriver:
    def tick(self, now: float) -> dict[str, float]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock AVT-10 sidecar-only cursor contract in tests</name>
  <files>sidecar/tests/compositor/test_cursor_driver.py, sidecar/tests/vts/test_window_detect.py</files>
  <read_first>
    - .planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-CONTEXT.md D-09 and deferred renderer-side cursor decision
    - sidecar/tests/compositor/test_cursor_driver.py
    - sidecar/tests/vts/test_window_detect.py
    - sidecar/src/sidecar/compositor/cursor_driver.py
    - sidecar/src/sidecar/vts/window_detect.py
  </read_first>
  <behavior>
    - CursorDriver tests patch `sidecar.compositor.cursor_driver.get_cursor_and_rect` directly; no renderer event, WebSocket message, or ActionIntent reaction input is required for AVT-10.
    - Inside-window cursor samples drive non-empty `ParamAngleX`, `ParamAngleY`, `ParamEyeBallX`, and `ParamEyeBallY` output unless inside the 80px dead zone.
    - Outside-window or missing-window samples start cubic ease-back and expire to `{}` after `EASE_BACK_DURATION_S`.
    - Window-detection tests prove cached VTS HWND reuse and force-reprobe behavior without requiring a real VTS process.
  </behavior>
  <action>
Extend the existing sidecar tests rather than adding renderer tests.

In `sidecar/tests/compositor/test_cursor_driver.py`:
- Add `test_driver_uses_sidecar_win32_sample_contract_without_renderer_event()`. Monkeypatch `get_cursor_and_rect` to return `((300, 200), (100, 100, 300, 300))`; assert `CursorDriver().tick(0.0)` returns right-edge head/eye deflection. The test name and assertion message should mention "sidecar Win32 sample contract".
- Add or tighten an existing dead-zone test so `(210, 210)` against `(100, 100, 300, 300)` returns zeros for all four cursor params, not an empty dict.
- Add an exit/ease-back test that enters once, then monkeypatches `get_cursor_and_rect` to return `((50, 50), (100, 100, 200, 200))`; assert the intermediate tick is non-empty and `tick(EASE_BACK_DURATION_S + epsilon)` returns `{}`.

In `sidecar/tests/vts/test_window_detect.py`:
- Add a cached-HWND test that sets `_cached_hwnd = 999` and `_last_enum_at = time.monotonic()`; assert `find_vts_hwnd()` returns 999 without calling EnumWindows.
- Add a force-reprobe test that sets `_cached_hwnd = 999`, monkeypatches EnumWindows to report a new matching HWND, calls `find_vts_hwnd(force_reprobe=True)`, and asserts the new HWND is returned.

Do not create or modify any renderer files. Do not add `cursor-reaction`, `CursorReactionMessage`, `AvatarCursorOverlay`, or `apply_reaction_intent`; those contradict D-09/D-11.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/compositor/test_cursor_driver.py tests/vts/test_window_detect.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg "sidecar Win32 sample contract|force_reprobe" sidecar/tests/compositor/test_cursor_driver.py sidecar/tests/vts/test_window_detect.py` returns matches
    - `rg "cursor-reaction|CursorReactionMessage|AvatarCursorOverlay|apply_reaction_intent" sidecar/tests/compositor/test_cursor_driver.py sidecar/tests/vts/test_window_detect.py` returns no matches
    - `cd sidecar && uv run pytest tests/compositor/test_cursor_driver.py tests/vts/test_window_detect.py -x -v` passes
  </acceptance_criteria>
  <done>AVT-10 tests encode the accepted D-09/D-11 sidecar Win32 cursor contract and exclude renderer-overlay semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Document the accepted AVT-10 equivalent in sidecar code</name>
  <files>sidecar/src/sidecar/compositor/cursor_driver.py, sidecar/src/sidecar/vts/window_detect.py</files>
  <read_first>
    - sidecar/src/sidecar/compositor/cursor_driver.py
    - sidecar/src/sidecar/vts/window_detect.py
    - .planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-CONTEXT.md D-09/D-11
  </read_first>
  <action>
Update module docstrings/comments only where they clarify the contract:
- In `cursor_driver.py`, make the top docstring state that AVT-10 is implemented by sidecar Win32 cursor/window polling per D-09, with no renderer hot-path involvement.
- In `window_detect.py`, add a short docstring or comment near `find_vts_hwnd()` / `get_cursor_and_rect()` explaining that VTS bounds are re-polled in the sidecar so moving/resizing the VTS window remains authoritative.

If tests from Task 1 reveal a bug in the existing sidecar cursor code, fix only that bug in these two files. Do not touch `sidecar/src/sidecar/ws/server.py`; this plan has no sidecar lifespan or WS ownership, avoiding the Wave 1 file conflict with 04-05.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/compositor/test_cursor_driver.py tests/vts/test_window_detect.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg "D-09|Win32|No renderer|sidecar" sidecar/src/sidecar/compositor/cursor_driver.py sidecar/src/sidecar/vts/window_detect.py` returns matches documenting the sidecar-only contract
    - `rg "cursor-reaction|CursorReactionMessage|AvatarCursorOverlay|apply_reaction_intent|renderer overlay" sidecar/src/sidecar/compositor/cursor_driver.py sidecar/src/sidecar/vts/window_detect.py` returns no matches
    - `cd sidecar && uv run pytest tests/compositor/test_cursor_driver.py tests/vts/test_window_detect.py -x -v` passes
  </acceptance_criteria>
  <done>Sidecar cursor implementation documents the accepted AVT-10 equivalent and remains independent of renderer/WS cursor traffic.</done>
</task>

</tasks>

<verification>
Run:
```powershell
cd sidecar
uv run pytest tests/compositor/test_cursor_driver.py tests/vts/test_window_detect.py -x -v
cd ..
rg "cursor-reaction|CursorReactionMessage|AvatarCursorOverlay|apply_reaction_intent" sidecar/src/sidecar/compositor/cursor_driver.py sidecar/src/sidecar/vts/window_detect.py sidecar/tests/compositor/test_cursor_driver.py sidecar/tests/vts/test_window_detect.py
```
The pytest command must pass. The `rg` command must produce no matches in the listed code/test files.
</verification>

<success_criteria>
AVT-10 is closed when sidecar Win32 window-bounds detection plus cursor polling is tested and documented as the accepted D-09/D-11 cursor contract, the cursor driver still produces visible head/eye tracking plus cubic ease-back, and no renderer overlay, renderer WS message, or renderer-authoritative cursor event path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-compositor-vts-bridge-body-sway-investigation/04-06-SUMMARY.md` with changed files, test commands/results, and a note that stale renderer-overlay verification wording was superseded by the locked D-09/D-11 sidecar Win32 cursor contract.
</output>
