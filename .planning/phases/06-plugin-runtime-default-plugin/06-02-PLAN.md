---
phase: 06-plugin-runtime-default-plugin
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - sidecar/src/sidecar/plugins/supervisor.py
  - sidecar/src/sidecar/compositor/plugin_adapter.py
  - sidecar/src/sidecar/compositor/compositor.py
  - sidecar/src/sidecar/compositor/intent_driver.py
  - sidecar/src/sidecar/ws/server.py
  - sidecar/src/sidecar/orchestrator/orchestrator.py
  - sidecar/src/sidecar/orchestrator/transformers.py
  - sidecar/src/sidecar/compositor/body_sway/
  - plugins/default/body_sway/
  - sidecar/scripts/plumbing_harness.py
  - sidecar/tests/plugins/test_supervisor.py
  - sidecar/tests/compositor/test_plugin_adapter.py
  - sidecar/tests/compositor/test_compositor.py
  - sidecar/tests/test_orchestrator_turn.py
  - sidecar/tests/architecture/test_pyvts_writer_singleton.py
  - sidecar/tests/scripts/test_plumbing_harness.py
autonomous: true
requirements:
  - PLG-01
  - PLG-04
  - PLG-05
  - PLG-08
  - PLG-09
  - PLG-10
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ARCH-10
  - ARCH-12

must_haves:
  truths:
    - "Sidecar boots through plugin loader and supervisor; invalid plugin falls back to NullPlugin without crashing."
    - "Supervisor applies 5s on_load timeout and 3 failures per 60s circuit breaker."
    - "PluginAdapter exposes TickDriver behavior: latest-frame-wins over-rate, hold-last-frame under-rate, then stale decay to empty."
    - "Compositor merge order is idle -> speech -> plugin -> cursor -> lock/filter/clamp -> writer."
    - "IntentDriver is deleted and no production import path references it."
    - "body_sway package is moved to plugins/default/body_sway and only head_only remains selectable."
    - "Exactly one import pyvts remains under sidecar/src, in the safe writer file."
    - "Orchestrator prompt is built from plugin manifest action-code section at boot and is stable across turns."
  artifacts:
    - path: "sidecar/src/sidecar/plugins/supervisor.py"
      provides: "PluginSupervisor, NullPlugin"
      contains: "class NullPlugin"
    - path: "sidecar/src/sidecar/compositor/plugin_adapter.py"
      provides: "PluginAdapter TickDriver"
      contains: "class PluginAdapter"
    - path: "sidecar/src/sidecar/compositor/compositor.py"
      provides: "plugin_adapter merge slot"
      contains: "plugin_driver"
    - path: "sidecar/scripts/plumbing_harness.py"
      provides: "lipsync and idle baseline harness"
      contains: "def main"
---

<objective>
Perform the runtime plumbing surgery: supervised plugin lifecycle, compositor
adapter, orchestrator prompt rewiring, server boot rewiring, `IntentDriver`
removal, single-pyvts-writer guard, and the Phase 6 plumbing harness.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-plugin-runtime-default-plugin/06-CONTEXT.md
@.planning/phases/06-plugin-runtime-default-plugin/06-RESEARCH.md
@.planning/phases/06-plugin-runtime-default-plugin/06-VALIDATION.md
@.planning/phases/06-plugin-runtime-default-plugin/06-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Supervisor and PluginAdapter</name>
  <files>
    sidecar/src/sidecar/plugins/supervisor.py,
    sidecar/src/sidecar/compositor/plugin_adapter.py,
    sidecar/tests/plugins/test_supervisor.py,
    sidecar/tests/compositor/test_plugin_adapter.py
  </files>
  <read_first>
    - sidecar/src/sidecar/plugins/api.py
    - sidecar/src/sidecar/plugins/loader.py
    - sidecar/src/sidecar/compositor/compositor.py
    - packages/contracts/py/contracts/param_frame.py
    - .planning/phases/06-plugin-runtime-default-plugin/06-RESEARCH.md (failure modes table)
  </read_first>
  <action>
Create `supervisor.py` with:
- `NullPlugin(BodyMotionPlugin)` returning empty `ParamFrame()` output.
- `PluginSupervisor` with constants `ON_LOAD_TIMEOUT_SECONDS = 5.0`,
  `FAILURE_WINDOW_SECONDS = 60.0`, `MAX_FAILURES = 3`.
- `load_or_null(plugin, capabilities, overrides)` that runs sync `on_load` via
  `asyncio.to_thread(...)` and `asyncio.wait_for(..., 5.0)`.
- circuit breaker state for async generator failures.
- `close()` that calls `on_unload()` and tolerates cleanup exceptions.

Create `plugin_adapter.py` with:
- `PluginAdapter.tick(now) -> ParamFrame`
- `submit_frame(frame, now)` latest-frame-wins coalescing.
- stale decay: if latest plugin frame is older than 1.0s, return empty frame.
- `enqueue_sentence(sentence)` API to feed plugin input.

Tests must cover `__init__`/`on_load` failure fallback, 5s timeout using a small
test timeout override, generator failure circuit breaker, latest-frame-wins,
hold-last-frame, and stale decay.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/plugins/test_supervisor.py tests/compositor/test_plugin_adapter.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "class NullPlugin" sidecar/src/sidecar/plugins/supervisor.py` matches.
    - `grep -E "ON_LOAD_TIMEOUT_SECONDS = 5\\.0" sidecar/src/sidecar/plugins/supervisor.py` matches.
    - `grep -E "MAX_FAILURES = 3" sidecar/src/sidecar/plugins/supervisor.py` matches.
    - `grep -E "class PluginAdapter" sidecar/src/sidecar/compositor/plugin_adapter.py` matches.
    - `grep -E "def tick\\(self, now: float\\)" sidecar/src/sidecar/compositor/plugin_adapter.py` matches.
    - `cd sidecar && uv run pytest tests/plugins/test_supervisor.py tests/compositor/test_plugin_adapter.py -x --no-header` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire compositor merge order and remove IntentDriver</name>
  <files>
    sidecar/src/sidecar/compositor/compositor.py,
    sidecar/src/sidecar/compositor/intent_driver.py,
    sidecar/src/sidecar/compositor/body_sway/,
    plugins/default/body_sway/,
    sidecar/tests/compositor/test_compositor.py,
    sidecar/tests/architecture/test_pyvts_writer_singleton.py
  </files>
  <read_first>
    - sidecar/src/sidecar/compositor/compositor.py
    - sidecar/src/sidecar/compositor/intent_driver.py
    - sidecar/src/sidecar/compositor/body_sway/registry.py
    - sidecar/src/sidecar/compositor/clamp.py
    - sidecar/src/sidecar/compositor/lock_filter.py
    - sidecar/src/sidecar/vts/pyvts_writer.py
  </read_first>
  <action>
Modify `Compositor.__init__` to accept `plugin_driver: TickDriver` instead of
`intent_driver: IntentTickDriver`.

Rewrite `_tick` merge order:
1. idle add params
2. speech params, with `MouthOpen` as set `(value, 1.0)`
3. plugin `ParamFrame.add_params` and `ParamFrame.set_params`
4. cursor add params
5. `clamp_and_validate(frame, capabilities)` before `writer.inject_params`

Add `capabilities: RigCapabilities` to the constructor so the clamp has the
writable param surface.

Move `sidecar/src/sidecar/compositor/body_sway/` to
`plugins/default/body_sway/`. Preserve `proxy_param.py` and
`exp3_modulation.py` as source artifacts, but rewrite `registry.py` so:

```python
def available_strategy_names(*_args, **_kwargs) -> tuple[str, ...]:
    return ("head_only",)

def build_strategy(name: str, *args, **kwargs):
    if name != "head_only":
        raise ValueError("Only head_only is selectable in the default plugin")
    ...
```

Delete `sidecar/src/sidecar/compositor/intent_driver.py` and remove all
production imports. Keep tests adjusted to the new plugin path.

Create `sidecar/tests/architecture/test_pyvts_writer_singleton.py` asserting
`rg "import pyvts" sidecar/src` finds exactly one file:
`sidecar/src/sidecar/vts/pyvts_writer.py`.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/compositor/test_compositor.py tests/architecture/test_pyvts_writer_singleton.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path sidecar/src/sidecar/compositor/intent_driver.py` returns False.
    - `rg "IntentDriver" sidecar/src` returns no production matches.
    - `Test-Path plugins/default/body_sway/head_only.py` returns True.
    - `grep -F "Only head_only is selectable" plugins/default/body_sway/registry.py` matches.
    - `grep -E "plugin_driver" sidecar/src/sidecar/compositor/compositor.py` matches.
    - `grep -E "clamp_and_validate" sidecar/src/sidecar/compositor/compositor.py` matches.
    - `cd sidecar && uv run pytest tests/compositor/test_compositor.py tests/architecture/test_pyvts_writer_singleton.py -x --no-header` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire server boot and orchestrator prompt path</name>
  <files>
    sidecar/src/sidecar/ws/server.py,
    sidecar/src/sidecar/orchestrator/orchestrator.py,
    sidecar/src/sidecar/orchestrator/transformers.py,
    sidecar/tests/test_orchestrator_turn.py,
    sidecar/tests/test_phase4_bootstrap.py
  </files>
  <read_first>
    - sidecar/src/sidecar/ws/server.py
    - sidecar/src/sidecar/orchestrator/orchestrator.py
    - sidecar/src/sidecar/orchestrator/transformers.py
    - sidecar/src/sidecar/plugins/loader.py
    - sidecar/src/sidecar/plugins/supervisor.py
    - sidecar/src/sidecar/compositor/plugin_adapter.py
    - sidecar/src/sidecar/avatar/rig_capabilities.py
    - sidecar/src/sidecar/avatar/overrides.py
  </read_first>
  <action>
In `ws/server.py`:
- remove `IntentDriver` import and construction.
- discover and load active plugin, defaulting to `"default"`.
- read optional env var `AGENTICLLMVTUBER_ACTIVE_PLUGIN`; if absent use `"default"`.
- read optional env var `AGENTICLLMVTUBER_USER_DATA`; if absent use empty user plugin directory.
- call `PluginSupervisor.load_or_null(...)`.
- construct `PluginAdapter`.
- pass `plugin_driver=plugin_adapter` and `capabilities=capabilities` into `Compositor`.
- store `app.state.plugin_manifest`, `app.state.plugin_adapter`, and `app.state.plugin_supervisor`.

In `orchestrator.py`:
- remove `AvatarCapabilities` import.
- replace `_build_system_prompt(persona_text, capabilities)` with
  `_build_system_prompt(persona_text, action_codes_section: str)`.
- replace `[<insert_action_codes_section>]` with the prebuilt section.
- keep `_system_prompt` frozen at constructor time.
- pass plugin input sentences to `plugin_adapter.enqueue_sentence(...)` after
  sentence segmentation but before/alongside display/TTS emission.

In `transformers.py`:
- decouple `actions_extractor` from `AvatarCapabilities`; if still needed before
  Phase 7, make it accept `valid_expression_names: set[str]` and default empty.
- do not emit expression `ActionIntent` for plugin action codes; plugin sees the
  raw sentence text separately.

Tests must assert two orchestrators built from the same manifest have identical
`_system_prompt` bytes and switching manifest content changes the prompt only
after a new orchestrator is constructed.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/test_orchestrator_turn.py tests/test_phase4_bootstrap.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "IntentDriver" sidecar/src/sidecar/ws/server.py` returns no matches.
    - `grep -F "AGENTICLLMVTUBER_ACTIVE_PLUGIN" sidecar/src/sidecar/ws/server.py` matches.
    - `grep -F "PluginAdapter" sidecar/src/sidecar/ws/server.py` matches.
    - `grep -F "_build_system_prompt(persona_text: str, action_codes_section: str)" sidecar/src/sidecar/orchestrator/orchestrator.py` matches.
    - `grep -F "[<insert_action_codes_section>]" sidecar/src/sidecar/orchestrator/orchestrator.py` matches.
    - `grep -F "AvatarCapabilities" sidecar/src/sidecar/orchestrator/orchestrator.py` returns no matches.
    - `cd sidecar && uv run pytest tests/test_orchestrator_turn.py tests/test_phase4_bootstrap.py -x --no-header` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Plumbing harness for lipsync and idle baselines</name>
  <files>
    sidecar/scripts/plumbing_harness.py,
    sidecar/tests/scripts/test_plumbing_harness.py,
    .planning/baselines/v2.0/
  </files>
  <read_first>
    - .planning/phases/06-plugin-runtime-default-plugin/06-CONTEXT.md (D-A2.2)
    - sidecar/src/sidecar/compositor/idle_driver.py
    - sidecar/src/sidecar/compositor/speech_driver.py
    - sidecar/scripts/plot_speech_evidence.py
  </read_first>
  <action>
Create `sidecar/scripts/plumbing_harness.py` with CLI:

```powershell
uv run python sidecar/scripts/plumbing_harness.py --mode lipsync --out .planning/baselines/v2.0/lipsync.json
uv run python sidecar/scripts/plumbing_harness.py --mode idle --out .planning/baselines/v2.0/idle.json
```

`--mode lipsync` writes JSON with keys:
`mode`, `sample_count`, `pearson_r`, `threshold`, `passed`.
Threshold is `0.7`.

`--mode idle` writes JSON with keys:
`mode`, `duration_seconds`, `variance_sum`, `variance_ceiling`, `passed`.
`variance_sum` must be greater than `0.0` and less than `0.5`.

This harness is synthetic/unit-level; it does not need VTube Studio. It samples
the current idle/speech drivers directly. Tests assert schema and threshold
behavior using temp output files.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/scripts/test_plumbing_harness.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def main" sidecar/scripts/plumbing_harness.py` matches.
    - `grep -F "pearson_r" sidecar/scripts/plumbing_harness.py` matches.
    - `grep -F "variance_sum" sidecar/scripts/plumbing_harness.py` matches.
    - `cd sidecar && uv run pytest tests/scripts/test_plumbing_harness.py -x --no-header` exits 0.
    - `cd sidecar && uv run python scripts/plumbing_harness.py --mode idle --out ../.planning/baselines/v2.0/idle.json` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
```powershell
cd sidecar
uv run pytest tests/plugins tests/compositor tests/test_orchestrator_turn.py tests/test_phase4_bootstrap.py tests/architecture/test_pyvts_writer_singleton.py tests/scripts/test_plumbing_harness.py -q
npm run check:contracts
npm --workspace apps/renderer run typecheck
```
</verification>

<success_criteria>
- Supervisor and adapter tests pass.
- Compositor no longer depends on `IntentDriver`.
- Boot path loads plugin runtime and still constructs sidecar app.
- Orchestrator system prompt uses plugin manifest action-code section.
- Single pyvts writer invariant is tested.
- Plumbing harness exists for the automatable Phase 10 replay subset.
</success_criteria>

<output>
After completion, create `06-02-SUMMARY.md` documenting the boot sequence,
deleted/moved files, prompt freeze behavior, and harness outputs.
</output>
