---
phase: 06-plugin-runtime-default-plugin
plan: 07
type: execute
wave: 6
depends_on: [06-06]
files_modified:
  - sidecar/src/sidecar/ws/server.py
  - sidecar/src/sidecar/compositor/speech_driver.py
  - sidecar/src/sidecar/vts/__init__.py
  - sidecar/src/sidecar/vts/speech_mouth_driver.py        # DELETE
  - sidecar/src/sidecar/vts/parameter_writer.py           # DELETE
  - sidecar/tests/vts/test_speech_mouth_driver.py         # DELETE if present
  - sidecar/tests/test_phase4_bootstrap.py
  - sidecar/tests/compositor/test_speech_driver.py
  - sidecar/tests/test_arch06_single_writer.py            # NEW (CI hardening)
autonomous: true
gap_closure: true
status: completed
completed: 2026-05-08T18:09:47-04:00
requirements: [ARCH-05, ARCH-06, TTS-04]
must_haves:
  truths:
    - "There is exactly ONE class in sidecar/src/sidecar/vts/ that owns a VTS plugin identity (plugin_name + auth_token), and exactly ONE coroutine sends parameter writes to VTS via pyvts. PyvtsSafeWriter is that class."
    - "MouthOpen flows through the compositor merge order: SpeechDriver tick -> compositor set_acc -> clamp_and_validate -> PyvtsSafeWriter.inject_params. There is NO second writer or second VTS plugin identity in the running sidecar."
    - "ws/server.py lifespan no longer creates SpeechMouthDriver, mouth_speech_queue, mouth_task, or PyVTSParameterWriter. The mouth_writer build helper (_build_mouth_writer) is removed."
    - "compositor SpeechDriver is constructed with emit_mouth=True (the field is removed entirely OR the default is flipped); SpeechDriver.tick(now) returns MouthOpen as part of its dict alongside body_sway params."
    - "TTSGateway feeds a single speech queue (compositor_speech_queue). The dual-queue split (compositor_speech_queue + mouth_speech_queue) is removed."
    - "A new CI test (sidecar/tests/test_arch06_single_writer.py) FAILS if anyone reintroduces a second VTS-plugin-identity wrapper. The assertion goes beyond `import pyvts` count == 1: it AST-greps for occurrences of requestSetParameterValue / requestInjectParameterData / 'plugin_name' kwarg on PyvtsSafeWriter construction, and asserts each appears in at most one source file (pyvts_writer.py)."
    - "Existing tests still pass: test_phase4_bootstrap, all compositor tests, all plugin tests, all sidecar lifespan tests. The deleted test_speech_mouth_driver suite is replaced by the compositor SpeechDriver mouth-output assertion."
    - "Boot smoke (sidecar lifespan up + warmup + connect_and_authenticate(writer)) shows ONE handshake, ONE [READY], no second auth flow."
  artifacts:
    - path: "sidecar/src/sidecar/ws/server.py"
      provides: "lifespan no longer constructs SpeechMouthDriver / mouth_writer / mouth_speech_queue"
      contains_not: "SpeechMouthDriver|PyVTSParameterWriter|_build_mouth_writer|mouth_speech_queue|mouth_task"
    - path: "sidecar/src/sidecar/compositor/speech_driver.py"
      provides: "MouthOpen emitted as part of SpeechDriver.tick(); emit_mouth flag removed"
      contains_not: "emit_mouth"
    - path: "sidecar/tests/test_arch06_single_writer.py"
      provides: "CI assertion that no second VTS plugin identity wrapper exists in vts/"
      contains:
        - "requestSetParameterValue"
        - "requestInjectParameterData"
        - "plugin_name"
  key_links:
    - from: "sidecar/src/sidecar/compositor/speech_driver.py"
      to: "sidecar/src/sidecar/compositor/compositor.py"
      via: "SpeechDriver.tick(now) dict includes MouthOpen; compositor handles MOUTH_PARAM as set_acc[MOUTH_PARAM] = (value, 1.0)"
      pattern: "if key == MOUTH_PARAM:"
    - from: "sidecar/src/sidecar/compositor/compositor.py"
      to: "sidecar/src/sidecar/vts/pyvts_writer.py"
      via: "Compositor._tick -> writer.inject_params(frame); writer is PyvtsSafeWriter (single-instance)"
      pattern: "await self._writer.inject_params"
    - from: "sidecar/src/sidecar/ws/server.py"
      to: "sidecar/src/sidecar/vts/pyvts_writer.py"
      via: "ONE PyvtsSafeWriter() construction in lifespan; ONE connect_and_authenticate(writer) handshake task"
      pattern: "writer = PyvtsSafeWriter\\(\\)"
deletes:
  - sidecar/src/sidecar/vts/speech_mouth_driver.py
  - sidecar/src/sidecar/vts/parameter_writer.py
  - sidecar/tests/vts/test_speech_mouth_driver.py
---

<objective>
Close Phase 6 post-verification finding F-1 + F-2 (06-VERIFICATION.md): consolidate the split mouth writer into the single-writer architecture that ARCH-05 + ARCH-06 already specify.

Purpose: Operator UAT (2026-05-08 PM) reported lipsync absent. Investigation found that MouthOpen flows through a SECOND VTS plugin identity ("AgenticLLMVTuber Phase3 Mouth Driver") via PyVTSParameterWriter — a class that wraps PyvtsSafeWriter indirectly so it bypasses the ARCH-06 grep. The split is a Milestone-1 Phase 3 (TTS-04) artifact that 06-02 plumbing surgery was supposed to consolidate but did not. Recent fix(06) commits ("authenticate mouth writer through safe handshake", "restore VTS tracking params and token path") were patching the split path rather than removing it. This plan removes the split.

ARCH-05 (REQUIREMENTS.md:67) is the authoritative spec: "IdleDriver -> SpeechDriver (lipsync only) -> PluginAdapter -> CursorDriver -> ... -> pyvts.inject_params". Lipsync is the SpeechDriver's job INSIDE the compositor merge, not a parallel writer.

ARCH-06 (REQUIREMENTS.md:68) says "Every entry point flows through PyvtsSafeWriter." The current PyVTSParameterWriter is a second writer that wraps PyvtsSafeWriter to bypass the literal grep — it satisfies the letter of the CI test but violates the architectural intent.

Output: Single VTS plugin identity. Single writer. Lipsync routed through compositor. Stronger CI assertion. Boot shows one handshake, not two.
</objective>

<scope_constraints>

**IN SCOPE:**
- Delete `vts/speech_mouth_driver.py` + `vts/parameter_writer.py` + their tests
- Flip compositor `SpeechDriver` to emit MouthOpen (remove `emit_mouth` flag entirely; default is "always emit")
- Remove `mouth_speech_queue` / `mouth_task` / `_build_mouth_writer` from `ws/server.py` lifespan
- Wire `TTSGateway` to feed `compositor_speech_queue` only (single queue)
- Add `tests/test_arch06_single_writer.py` with assertions stronger than the existing `import pyvts` grep
- Update `vts/__init__.py` to drop the deleted exports
- Update existing tests that referenced the deleted modules

**OUT OF SCOPE (do NOT do these):**
- DO NOT change PROJECT.md, REQUIREMENTS.md, or any architectural document. ARCH-05/06 are correct as written; this plan makes the implementation match.
- DO NOT introduce a "MotionCaptureFrame" contract or any new contract. ParamFrame is the existing frame contract and is sufficient.
- DO NOT pivot to live2d-py or evaluate it. live2d-py is a renderer-replacement candidate, not a fix for a writer-consolidation bug.
- DO NOT change RigCapabilities or its writable_param_ids. The Live2D-model-param vs VTS-tracking-input namespace question is already handled by `clamp.py:27` (writable union with VTS_TRACKING_INPUT_PARAM_IDS) and `param_id_resolver.py`.
- DO NOT investigate F-3 (head_only single-axis visibility) in this plan. It is a runtime quality issue masked by F-1's auth instability. After F-1 is fixed and lipsync is back, F-3 becomes a runtime-log-driven investigation in a separate task or Phase 10 ceremony.
- DO NOT touch `compositor/lock_filter.py` system-primitive override list. ARCH-12 (MouthOpen as system primitive overriding HUD locks) stays as-is — it just now operates on a frame that includes the SpeechDriver's MouthOpen contribution rather than a parallel-written value.
- DO NOT add a new VTS plugin identity for any reason. ONE pyvts WebSocket connection, ONE auth flow.

**Deviation handling:** If you discover the consolidation requires a contracts change (it should not), STOP and surface the issue — do not improvise. If you find tests asserting the dual-writer pattern, those tests were locking in the bug; rewrite or delete them, do not preserve the dual-writer behavior to make them pass.

</scope_constraints>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/06-plugin-runtime-default-plugin/06-VERIFICATION.md
</execution_context>

<verification>
Before reporting success:

1. **Filesystem check:**
   ```bash
   test ! -f sidecar/src/sidecar/vts/speech_mouth_driver.py && \
   test ! -f sidecar/src/sidecar/vts/parameter_writer.py && \
   echo "OK: split writer files deleted"
   ```

2. **Grep ARCH-06 hardening:**
   ```bash
   grep -rn "requestSetParameterValue\|requestInjectParameterData" sidecar/src --include="*.py" | grep -v "/tests/" | wc -l
   # Must equal 1 (PyvtsSafeWriter only)
   grep -rn "plugin_name" sidecar/src --include="*.py" | grep -v "/tests/" | wc -l
   # Must equal 1 (PyvtsSafeWriter only — the auth identity field)
   ```

3. **Grep emit_mouth removed:**
   ```bash
   grep -rn "emit_mouth" sidecar/src --include="*.py" | wc -l
   # Must equal 0
   ```

4. **Run full sidecar test suite:**
   ```bash
   cd sidecar && uv run pytest
   # All tests pass; new test_arch06_single_writer.py passes
   ```

5. **Boot smoke check (manual, but record evidence):**
   ```bash
   # Start sidecar, observe logs:
   # - exactly ONE [VTS] connect_and_authenticate
   # - exactly ONE [READY]
   # - NO log lines mentioning "Mouth Driver" plugin identity
   ```

6. **Operator UAT preview (manual):**
   - With VTS running and Teto loaded, send a multi-sentence prompt
   - Confirm MouthOpen visibly tracks RMS (lipsync restored)
   - Confirm head/body sway is multi-axis (or clearly identify F-3 root cause now that auth-instability mask is removed)

</verification>

<commit_guidance>
Suggested commit sequence (one commit per logical step keeps revert easy):

1. `feat(06-07): flip compositor SpeechDriver emit_mouth=True default and remove flag` — minimal diff isolating the compositor change
2. `feat(06-07): consolidate ws/server.py lifespan to single VTS writer/queue` — server.py changes
3. `chore(06-07): delete vts/speech_mouth_driver.py + vts/parameter_writer.py and their tests` — deletion commit
4. `test(06-07): add ARCH-06 single-writer CI assertion stronger than 'import pyvts' grep` — CI hardening
5. `docs(06-07): mark plan complete + 06-VERIFICATION re_verification_2 pass` — close-out

Do NOT squash these into one commit; the per-step diffs are valuable for forensics if F-3 turns out to depend on subtle behavior change.
</commit_guidance>
