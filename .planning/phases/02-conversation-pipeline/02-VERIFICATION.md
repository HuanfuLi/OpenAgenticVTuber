---
phase: 02-conversation-pipeline
verified: 2026-05-06T23:35:00Z
status: human_needed
score: 3/5 must-haves verified programmatically; 2/5 require human verification (live LM Studio)
re_verification:
  is_re_verification: false
human_verification:
  - test: "SC #1 — three-sentence story streams as ONE growing bubble"
    expected: "With LM Studio running and a configured provider, the user types 'tell me a 3-sentence story' and three sentences appear sequentially in the chat panel, accumulating into ONE growing assistant bubble (not three separate bubbles). Banner stays clear; input re-enables on chain-end."
    why_human: "Requires live LM Studio with a real model loaded; the renderer reducer test (test_full_canonical_sequence_single_turn_produces_ONE_growing_bubble) and the orchestrator end-to-end test (test_emits_canonical_envelope_sequence with FakeGateway emitting multi-sentence chunks) both pass programmatically — but only an operator with LM Studio running can confirm the live network round-trip."
  - test: "SC #2 — `[joy]` strips bracket from chat AND fires `[INTENT]` log line"
    expected: "Type 'hello [joy] world' (or any prompt likely to elicit `[joy]`); chat shows `hello world` (no brackets); Logs drawer (toggled on in Settings) shows green-prefixed `[INTENT] kind=expression name=joy strength=1.0 avatar=teto` line."
    why_human: "Requires live LLM emitting `[joy]` token — orchestrator unit tests assert the bracket-strip + intent log line at decorator-chain level (test_full_decorator_chain_strips_brackets, test_intent_emitted_in_audio_actions); LogsDrawer DOM test asserts prefix coloring; but verifying the LLM actually emits `[joy]` against the avatar.yaml tag vocabulary is a live-system concern."
  - test: "SC #4 — no `<think>...</think>` ever leaks into chat"
    expected: "With LM Studio running a compliant reasoning model (latest DeepSeek-R1 distill that honors `enable_thinking:false`, or Qwen3-Reasoning), no `<think>...</think>` content appears in the main chat stream or in extracted ActionIntents during a multi-sentence reply."
    why_human: "Requires live LM Studio + reasoning-class model loaded. The sidecar Q1 smoke test (test_extra_body_disables_think_block) is OPERATOR-RUN with SIDECAR_SMOKE_LMSTUDIO=1; PROVENANCE.md currently records SKIP. Operator must run this once before Phase 5 verification per the plan-time gate."
  - test: "SC #2 with the actual Teto rig — operator action required on avatar.yaml"
    expected: "Before SC #2 can be reliably verified end-to-end, the operator must launch VTS with the Teto rig loaded, open Settings → Expressions / Hotkeys, and replace the placeholder names in avatars/teto/avatar.yaml with the real rig's expression/hotkey names so the LLM emits names that match what the rig actually has."
    why_human: "Per the avatar.yaml header comment ('OPERATOR ACTION REQUIRED before SC #2 verification'); programmatic tests exercise the loader correctness with placeholder data. Affects SC #2 reliability, not Phase 2 code correctness."
---

# Phase 2: Conversation Pipeline Verification Report

**Phase Goal:** Establish the local conversation pipeline (LLM streaming gateway, sentence segmentation, ActionIntent extraction, append-only memory). User can hold a real text-in / text-out conversation with their first avatar; agentic-LLM-VTuber feels alive in chat for the first time.

**Verified:** 2026-05-06T23:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| #   | Truth (ROADMAP SC)                                                                                                | Status                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | "tell me a 3-sentence story" → 3 sentences arrive on the renderer streamed (one per envelope)                     | ? UNCERTAIN — code path PROVEN, live untested | Sidecar: `test_emits_canonical_envelope_sequence` PROVES `Orchestrator.turn` emits chain-start → full-text(Thinking…) → audio×N → force-new-message → chain-end with FakeGateway streaming `"Hello [joy] world. Second sentence."`. Renderer: `test_full_canonical_sequence_single_turn_produces_ONE_growing_bubble` PROVES the reducer accumulates 3 sentences into ONE bubble (length=2, contains all three sentence fragments). Live LM-Studio test deferred to human verification. |
| 2   | Avatar response containing `[joy]` strips bracket from chat AND fires INTENT log line `[INTENT] [joy] (rule#1)`   | ? UNCERTAIN — code path PROVEN, live untested | `test_full_decorator_chain_strips_brackets` PROVES display has no `[`/`]` and intents `[hold-mic, joy]` are produced. `test_intent_emitted_in_audio_actions` PROVES the audio envelope's `actions` field carries ActionIntent(kind=expression, name=joy). LogsDrawer test (`logs-drawer-intent.test.tsx`) PROVES `[INTENT]` lines render with `var(--success)` prefix span. Live test (LLM emits `[joy]` against real Teto rig) deferred to human verification.                          |
| 3   | Adversarial: bracket split across LLM stream chunks (`[jo` then `y]`) still extracted correctly                   | ✓ VERIFIED (programmatic)               | `test_split_bracket_token_deltas` (sentence_divider layer) PROVES `["[", "jo", "y]", " hello", " world.", ...]` reassembles into a sentence containing the literal `"[joy]"`. `test_full_decorator_chain_strips_brackets` PROVES the full chain on the same adversarial fixture: brackets disappear from display_text AND ActionIntents `[hold-mic, joy]` are produced. SC #3 BLOCKER closed. Source: `sidecar/tests/test_sentence_divider.py:20`, `test_actions_extractor.py:95`. |
| 4   | No `<think>...</think>` ever leaks into chat (any reasoning model with API-side disable flag)                     | ? UNCERTAIN — code path PROVEN, live untested | `test_lm_studio_kwargs_have_extra_body`, `test_anthropic_kwargs_reasoning_none`, `test_openai_kwargs_reasoning_minimal`, `test_gemini_no_reasoning_kwarg`, `test_custom_openai_kwargs_have_extra_body` all PASS — gateway emits the correct provider-specific reasoning-disable kwargs at the API call boundary. `test_gateway_smoke.py` (Q1 smoke) is OPT-IN via SIDECAR_SMOKE_LMSTUDIO=1; PROVENANCE.md currently records SKIP. Live verification with a compliant reasoning model deferred to human.            |
| 5   | Closing app + relaunching produces a fresh conversation thread (in-memory only)                                  | ✓ VERIFIED (programmatic)               | Direct introspection: `Orchestrator.__init__` initializes `self._memory: list[dict] = []` and `self._head_idx: int = 0`. Verified at runtime: `uv run python -c "...Orchestrator(...); print(f'len={len(o._memory)}, head={o._head_idx}')"` → `len=0, head=0`. `test_memory_append_only_after_turn` confirms only `_memory.append` mutates state, never `pop/del/insert/remove/clear`. No persistence layer exists (no SQLite, no Chroma, no FTS5 — verified by absence in `sidecar/`). Process restart implicitly clears state. |

**Score:** 2/5 fully verified programmatically; 3/5 have all programmatic evidence and require live LM Studio for behavioral confirmation.

### Required Artifacts

| Artifact                                                              | Expected                                                            | Status     | Details                                                          |
| --------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------- |
| `sidecar/src/sidecar/llm/gateway.py`                                  | LLMGateway streaming wrapper, reasoning-disable kwargs, 120s timeout, cache_control | ✓ VERIFIED | 113 lines; contains `litellm.acompletion`, `enable_thinking` (×2), `cache_control` (×4), `timeout=120` (×1). `LLMGateway.stream()` is an async generator that yields str deltas. |
| `sidecar/src/sidecar/orchestrator/orchestrator.py`                    | Append-only `_memory` + `_head_idx`; system_prompt frozen at boot   | ✓ VERIFIED | 230 lines. KV-cache discipline grep: `self._memory.(pop\|__delitem__\|insert\|remove\|clear)` returns 0; `self._memory[*] =` slice-assignment returns 0. Only `self._memory.append` writes. `_system_prompt` built once in `__init__`; `test_system_prompt_built_once` PASSES. |
| `sidecar/src/sidecar/orchestrator/transformers.py`                    | OLVT 4-decorator chain port + actions_extractor (AvatarCapabilities + ActionIntent) | ✓ VERIFIED | 292 lines. All 4 decorators present: `sentence_divider`, `actions_extractor`, `display_processor`, `tts_filter`. `_extract_intents` bracket-walker keyed on capabilities expressions+hotkeys (D-13 expression-priority). `display_processor` adds skeleton-side `filter_brackets` call (SC #3 deviation, documented). |
| `sidecar/src/sidecar/orchestrator/sentence_divider.py`                | OLVT-port SentenceDivider with pysbd + comma-splitter                | ✓ VERIFIED | 614 lines (verbatim port from OLVT 608 LOC; only docstring header adapted). |
| `sidecar/src/sidecar/orchestrator/tts_preprocessor.py`                | OLVT-port tts_filter()                                               | ✓ VERIFIED | 184 lines (port from OLVT 196 LOC; TranslateInterface dropped, TTSPreprocessorConfig inlined). |
| `sidecar/src/sidecar/ws/handlers.py`                                  | text-input drives orchestrator.turn                                  | ✓ VERIFIED | `handle_text_input` reads `app.state.orchestrator`; if None emits ErrorMessage envelope; else `await orchestrator.turn(text, ws)`. Phase 1 echo body removed. |
| `sidecar/src/sidecar/ws/server.py`                                    | FastAPI lifespan loads ProviderConfig, builds Orchestrator, fires warmup ping | ✓ VERIFIED | `_load_provider_config_from_env` reads AGENTICLLMVTUBER_LLM_CONFIG_JSON; `_warmup_ping` fires before `app.state.orchestrator` set; graceful degradation when config absent. |
| `sidecar/src/sidecar/ws/emit.py`                                      | Typed WS emission helpers                                            | ✓ VERIFIED | All helpers present: `emit_audio_payload`, `emit_chain_start`, `emit_chain_end`, `emit_full_text`, `emit_force_new_message`, `emit_error`, `emit_log`. |
| `sidecar/src/sidecar/avatar/capabilities.py`                          | AvatarCapabilities loader with loud-fail YAML schema validation      | ✓ VERIFIED | Pydantic Expression/Hotkey/Parameter/Voice/AvatarCapabilities; `load_capabilities()` raises ValidationError on schema drift; `tag_vocabulary()` returns bracketed-name string. |
| `packages/contracts/py/contracts/audio_payload.py`                    | OLVT-canonical AudioPayloadMessage with `type='audio'` + `audio` field | ✓ VERIFIED | `type: Literal["audio"]` + `audio: Optional[str]` + `volumes`, `slice_length`, `display_text`, `actions: List[ActionIntent]`, `sentence_id`, `forwarded`. |
| `packages/contracts/py/contracts/action_intent.py`                    | Pydantic ActionIntent (PROJECT_DESIGN §6 shape)                      | ✓ VERIFIED | `kind: Literal["expression", "action", "reaction"]`, name, strength, duration_ms, avatar_id. |
| `packages/contracts/py/contracts/ws_message.py`                       | WSMessage union extended with audio + control + full-text + force-new-message + error + log | ✓ VERIFIED | All 9 variants present; Annotated-Union `discriminator="type"` preserved; Phase 1 variants unchanged. |
| `packages/contracts/ts/audio-payload.ts`                              | TS hand-mirror with `type: 'audio'`                                  | ✓ VERIFIED | Mirrors Pydantic shape; DisplayTextField + AudioPayloadMessage interfaces. |
| `packages/contracts/ts/action-intent.ts`                              | TS hand-mirror                                                       | ✓ VERIFIED | Mirrors Pydantic ActionIntent. |
| `packages/contracts/ts/ws-message.ts`                                 | Extended union + 6 new type guards                                   | ✓ VERIFIED | `isAudioPayload`, `isControl`, `isFullText`, `isForceNewMessage`, `isError`, `isLog` all present alongside Phase 1 guards. |
| `apps/renderer/src/screens/Chat/useStreamingMessages.ts`              | OLVT growing-bubble + force-new-message seal port                    | ✓ VERIFIED | 229 lines. `appendUserMessage`, `setThinking`, `appendAssistantSentence`, `setForceNewMessage`, `setBanner`, hooks. THINKING wholesale-replacement on first audio (IP-6); seal flag consumed in `setThinking` (skeleton-side adaptation; documented). |
| `apps/renderer/src/ws/store.ts`                                       | Phase 2 WS dispatcher routing 7 envelopes                            | ✓ VERIFIED | type-guarded routing for control(start/end), full-text (no-op), audio (appendAssistantSentence), force-new-message (setForceNewMessage), error (setBanner CONTEXT_OVERFLOW/STREAM_ERROR), log (logSinks), display-text (Phase 1 fallback). |
| `apps/renderer/src/screens/Chat/Chat.tsx`                             | Consumes useStreamingMessages; sticky-scroll + banner                | ✓ VERIFIED | `useStreamingBanner` + `useStreamingMessages` hooks; sticky-scroll via `scrollHeight - 40`; THINKING italic `var(--muted-foreground)`; banner `role="alert"` shape. |
| `apps/renderer/src/chrome/LogsDrawer.tsx`                             | [INTENT] prefix styled `var(--success)`                              | ✓ VERIFIED | Lines 109-127: `line.startsWith('[INTENT]')` → green prefix span; non-INTENT lines render plain. |
| `apps/renderer/src/chrome/AppShell.tsx`                               | subscribeWSLog wired alongside window.api.onSidecarLog               | ✓ VERIFIED | Both Phase 1 stdout (`window.api.onSidecarLog`) and Phase 2 WS log envelopes (`subscribeWSLog`) feed the 200-line-capped logLines state. |
| `avatars/teto/avatar.yaml`                                            | Teto capabilities placeholder                                        | ⚠️ PLACEHOLDER (intentional) | Schema-valid placeholder per D-09; OPERATOR ACTION REQUIRED before SC #2 reliability — operator must replace placeholder names with the real Teto rig's expression/hotkey names. Documented in file header. |
| `avatars/teto/personality.md`                                         | Teto persona placeholder                                             | ✓ VERIFIED | Includes "no XML/HTML-style think tags" instruction (Pitfall 1 fallback hardening). |
| `.planning/phases/02-conversation-pipeline/02-CONTEXT-AMENDMENT.md`   | OLVT-canonical envelope decision (D-02 revision)                     | ✓ VERIFIED | Documents D-02 amendment (`type:"audio"` not `"audio-payload"`; `audio` not `audio_b64`); D-12 clarification (`actions: list[ActionIntent]`). |
| `sidecar/tests/test_sentence_divider.py`                              | SC #3 split-bracket fixture                                          | ✓ VERIFIED | `test_split_bracket_token_deltas` PASSES with the literal `["[", "jo", "y]", " hello", " world.", ...]` adversarial fixture. |
| `sidecar/tests/test_actions_extractor.py`                             | SC #3 full-chain bracket-strip + intent extraction                   | ✓ VERIFIED | `test_full_decorator_chain_strips_brackets` PASSES with `[`,`jo`,`y]`,` hello`,` `,`[hold`,`-`,`mic`,`]`,` world.` and asserts brackets absent + intents `[hold-mic, joy]` produced. |
| `sidecar/tests/test_orchestrator_turn.py`                             | End-to-end orchestrator turn + KV-cache discipline                   | ✓ VERIFIED | 18 tests covering envelope sequence, append-only memory, KV-cache discipline grep, intent surfacing in audio.actions, context-overflow retry success/failure, generic exception, system-prompt freeze, log-line emission, lifespan helpers, WS dispatch. |

### Key Link Verification

| From                                             | To                                          | Via                                                    | Status     | Details                                                                                                                                                                                                       |
| ------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sidecar/src/sidecar/llm/gateway.py`             | `litellm.acompletion`                       | direct import + provider-specific kwargs branching      | ✓ WIRED    | Line 110: `async for chunk in await litellm.acompletion(**kwargs)`; provider routing in `_build_kwargs`; cache_control on system slot.                                                                        |
| `sidecar/src/sidecar/avatar/capabilities.py`     | `avatars/teto/avatar.yaml`                  | Path read at sidecar boot                              | ✓ WIRED    | `load_capabilities(avatar_dir)` reads `avatar_dir / "avatar.yaml"`; lifespan in `server.py` calls `load_capabilities(avatars/teto)`.                                                                          |
| `sidecar/src/sidecar/orchestrator/orchestrator.py` | `LLMGateway.stream`                       | `self._gateway.stream(send_window, system_prompt)`     | ✓ WIRED    | `_run_pipeline` constructs the decorator chain wrapping `gateway.stream(send_window, system_prompt)`; orchestrator owns gateway via `__init__`.                                                                |
| `sidecar/src/sidecar/orchestrator/orchestrator.py` | `AvatarCapabilities`                      | `load_capabilities()` at boot via lifespan             | ✓ WIRED    | `server.py` lifespan calls `load_capabilities(teto_dir)` then `Orchestrator(capabilities=capabilities, ...)`. `_build_system_prompt` consumes capabilities for `tag_vocabulary()` injection.                       |
| `sidecar/src/sidecar/orchestrator/transformers.py` | `contracts.ActionIntent`                  | `actions_extractor` constructs ActionIntent per matched tag | ✓ WIRED    | Lines 148, 152: `ActionIntent(kind="expression"\|"action", name=name, avatar_id="teto")`. Expression-first per D-13.                                                                                            |
| `sidecar/src/sidecar/orchestrator/orchestrator.py` | `AudioPayloadMessage`                     | per-sentence `emit_audio_payload(ws, AudioPayloadMessage(...))` | ✓ WIRED    | `_emit_sentence` builds AudioPayloadMessage with display_text, actions, sentence_id, forwarded=False; `emit_audio_payload` sends `model_dump()` over WS.                                                       |
| `sidecar/src/sidecar/ws/handlers.py`             | `Orchestrator.turn`                         | `await app.state.orchestrator.turn(text, ws)`         | ✓ WIRED    | `handle_text_input` line 36: `await orchestrator.turn(text, ws)`. Graceful degradation emits ErrorMessage when `app.state.orchestrator is None`.                                                              |
| `apps/renderer/src/ws/store.ts`                  | `useStreamingMessages.appendAssistantSentence` | dispatch on `isAudioPayload(msg)`                  | ✓ WIRED    | Line 62: `appendAssistantSentence(msg.display_text.text, msg.sentence_id)` on `isAudioPayload`.                                                                                                                |
| `apps/renderer/src/ws/store.ts`                  | `useStreamingMessages.setForceNewMessage`   | dispatch on `isForceNewMessage(msg)`                  | ✓ WIRED    | Line 66: `setForceNewMessage()` on `isForceNewMessage`.                                                                                                                                                       |
| `apps/renderer/src/ws/store.ts`                  | `useStreamingMessages.setThinking + setInputDisabled` | dispatch on `control{conversation-chain-start}`     | ✓ WIRED    | Lines 47-49: `setThinking(true)` + `setInputDisabled(true)` on chain-start.                                                                                                                                   |
| `apps/renderer/src/ws/store.ts`                  | `useStreamingMessages.setInputDisabled(false)` | dispatch on `control{conversation-chain-end}`        | ✓ WIRED    | Lines 50-52: `setInputDisabled(false)` on chain-end.                                                                                                                                                          |
| `apps/renderer/src/chrome/AppShell.tsx`          | log envelope (`isLog`)                      | `subscribeWSLog` log-channel sink                      | ✓ WIRED    | AppShell.tsx line 46: `const offWS = subscribeWSLog(append)`; `store.ts` line 79: `for (const cb of logSinks) cb(msg.message)` on `isLog`.                                                                    |
| `apps/renderer/src/screens/Chat/Chat.tsx`        | `useStreamingMessages` + `useStreamingBanner` | hooks consumed for messages + banner state          | ✓ WIRED    | Chat.tsx imports both hooks; renders `useStreamingMessages` as bubbles; renders `useStreamingBanner` with `role="alert"`.                                                                                       |

### Data-Flow Trace (Level 4)

| Artifact                                          | Data Variable           | Source                                                                                                                                                                                                                                          | Produces Real Data | Status        |
| ------------------------------------------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------- |
| `apps/renderer/src/screens/Chat/Chat.tsx`         | `messages` (StreamingMessage[]) | `useStreamingMessages()` hook → module-singleton state in `useStreamingMessages.ts` → mutated by `appendAssistantSentence` → called from `store.ts` WS dispatcher on `isAudioPayload` → AudioPayloadMessage from orchestrator's `_emit_sentence` → SentenceOutput from decorator chain → `LLMGateway.stream()` → `litellm.acompletion(stream=True)` | ✓ Real (with running sidecar + LLM) | ✓ FLOWING    |
| `apps/renderer/src/screens/Chat/Chat.tsx`         | `streamBanner`          | `useStreamingBanner()` hook → state set by `setBanner` → called from `store.ts` on `isError` envelope → orchestrator's `emit_error` on STREAM_ERROR / CONTEXT_OVERFLOW path                                                                       | ✓ Real             | ✓ FLOWING    |
| `apps/renderer/src/chrome/LogsDrawer.tsx`         | `logLines` (string[])   | AppShell logLines state ← BOTH `window.api.onSidecarLog` (Phase 1 stdout) AND `subscribeWSLog` (Phase 2 LogMessage envelopes) ← orchestrator's `logger.info("[INTENT] ...")` / `[STUB-TTS]` lines                                                | ✓ Real             | ✓ FLOWING    |
| `sidecar/src/sidecar/orchestrator/orchestrator.py` | `_memory` (list[dict])  | `self._memory.append(...)` in `turn()` (user message, then assistant accum); APPEND-ONLY                                                                                                                                                          | ✓ Real             | ✓ FLOWING    |
| `sidecar/src/sidecar/orchestrator/orchestrator.py` | `send_window`           | `_compute_send_window()` → slices `self._memory[self._head_idx:]` with token-budget pruning via `litellm.token_counter`; D-16 retry on context overflow advances `_head_idx`                                                                       | ✓ Real             | ✓ FLOWING    |

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                                                                                                                                                                                                                                                                  | Result                          | Status     |
| ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ---------- |
| Sidecar test suite passes (49 tests + 2 expected SKIP)                    | `cd sidecar && uv run pytest tests/ -q`                                                                                                                                                                                                                                                | `49 passed, 2 skipped in 30.48s` | ✓ PASS     |
| Renderer test suite passes (14 tests)                                     | `cd apps/renderer && npm test -- --run`                                                                                                                                                                                                                                                | `Test Files 2 passed (2), Tests 14 passed (14)` | ✓ PASS     |
| Renderer typecheck                                                        | `cd apps/renderer && npx tsc --noEmit`                                                                                                                                                                                                                                                 | exit 0                          | ✓ PASS     |
| KV-cache discipline grep — no `_memory.pop\|del\|insert\|remove\|clear`   | `grep -rn 'self\._memory\.(pop\|__delitem__\|insert\|remove\|clear)' sidecar/src/sidecar/orchestrator/`                                                                                                                                                                                | 0 matches                       | ✓ PASS     |
| KV-cache discipline grep — no in-place slice assignment                   | `grep -rn 'self\._memory\[[^]]*\]\s*=' sidecar/src/sidecar/orchestrator/`                                                                                                                                                                                                              | 0 matches                       | ✓ PASS     |
| LLM-04 fresh-orchestrator invariant                                       | `uv run python -c "...Orchestrator(...); print(len(o._memory), o._head_idx)"`                                                                                                                                                                                                          | `0, 0`                          | ✓ PASS     |
| Gateway has reasoning-disable kwargs                                      | `grep -c enable_thinking sidecar/src/sidecar/llm/gateway.py`                                                                                                                                                                                                                           | 2                               | ✓ PASS     |
| Gateway has cache_control on system slot                                  | `grep -c cache_control sidecar/src/sidecar/llm/gateway.py`                                                                                                                                                                                                                             | 4                               | ✓ PASS     |
| Gateway has 120s timeout                                                  | `grep -c 'timeout.*120' sidecar/src/sidecar/llm/gateway.py`                                                                                                                                                                                                                            | 1                               | ✓ PASS     |
| REQUIREMENTS.md no longer says "side channel"                             | `grep -c 'side channel' .planning/REQUIREMENTS.md`                                                                                                                                                                                                                                     | 0                               | ✓ PASS     |
| ROADMAP.md SC #4 amendment landed                                         | `grep -c 'compliant reasoning model' .planning/ROADMAP.md`                                                                                                                                                                                                                             | 1                               | ✓ PASS     |
| OLVT-canonical envelope: no `audio_b64` in contracts source code          | `grep -rn 'audio_b64' packages/contracts/`                                                                                                                                                                                                                                             | only in "NOT audio_b64" doc comments | ✓ PASS     |
| LogsDrawer renders [INTENT] in --success                                  | `grep -c 'var(--success)' apps/renderer/src/chrome/LogsDrawer.tsx`                                                                                                                                                                                                                     | 1                               | ✓ PASS     |
| Sticky-scroll 40px slack                                                  | `grep -c 'scrollHeight - 40' apps/renderer/src/screens/Chat/Chat.tsx`                                                                                                                                                                                                                  | 1                               | ✓ PASS     |
| Q1 LM Studio extra_body smoke test                                        | `cd sidecar && SIDECAR_SMOKE_LMSTUDIO=1 uv run pytest tests/test_gateway_smoke.py`                                                                                                                                                                                                     | n/a — operator-gated, recorded SKIP in PROVENANCE.md | ? SKIP (deferred to operator before Phase 5) |

### Requirements Coverage

| Requirement | Source Plan(s) | Description                                                                                                              | Status        | Evidence                                                                                                                                                                                                                                                              |
| ----------- | -------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LLM-01      | 02-01          | LiteLLM single client; LM Studio + custom OpenAI-compatible providers; ≥120s timeout                                     | ✓ SATISFIED  | `LLMGateway` wraps `litellm.acompletion`; 5 providers routed (lm_studio, custom_openai, openai, anthropic, gemini); `timeout=120` verified. `test_llm_gateway.py` (10 tests) exercises kwargs shape per provider.                                                       |
| LLM-02      | 02-02, 02-03   | OLVT-style decorator chain with buffer-then-extract on completed sentences only; renderer streamed                       | ✓ SATISFIED  | 4-decorator chain ported (sentence_divider 614 LOC, tts_preprocessor 184 LOC, transformers 291 LOC); `test_split_bracket_token_deltas` proves buffer-then-extract; `test_full_canonical_sequence_single_turn_produces_ONE_growing_bubble` proves renderer streaming. |
| LLM-03      | 02-02, 02-03   | Provider-specific reasoning-disable kwargs; NO parser-strip, NO out-of-band capture; verified by Phase 5 SC-01           | ✓ SATISFIED at code level; ? NEEDS HUMAN at live verification | Code level: 4 provider branches in gateway with correct kwargs (verified by 5 unit tests). Wording amendment: REQUIREMENTS.md uses "out-of-band reasoning capture" instead of "side channel"; ROADMAP SC #4 reworded to verify against compliant reasoning model. Live test deferred per the Phase 5 contract. |
| LLM-04      | 02-02          | Single in-memory thread; no persistence, no FTS5, no thread sidebar                                                       | ✓ SATISFIED  | `Orchestrator.__init__` initializes `_memory=[]` + `_head_idx=0`; verified at runtime; KV-cache discipline grep proves append-only invariant; no SQLite/Chroma/FTS5 in `sidecar/`.                                                                                       |

No orphaned requirements: ROADMAP/REQUIREMENTS map LLM-01..04 to Phase 2; all four are claimed by ≥1 plan and verified.

### Anti-Patterns Found

| File                                                              | Line       | Pattern                                                                | Severity | Impact                                                                                                                                                              |
| ----------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sidecar/src/sidecar/ws/server.py`                                | 35-36      | `TODO Phase 5 / electron-main side: add the env-var write`             | ℹ️ Info  | `AGENTICLLMVTUBER_LLM_CONFIG_JSON` env-var write from electron-main is NOT YET implemented. Sidecar emits config-error envelope when missing — graceful degradation, NOT a Phase 2 blocker. Tracked for Phase 5. |
| `avatars/teto/avatar.yaml`                                        | header     | "OPERATOR ACTION REQUIRED before SC #2 verification"                   | ⚠️ Warning | Placeholder names (joy, surprise, sadness, anger, neutral, disgust, fear, smirk + cry, baguette) need to be replaced with the real Teto rig's expression/hotkey names. Affects SC #2 reliability with a live LLM, NOT Phase 2 code correctness. |
| `sidecar/src/sidecar/orchestrator/PROVENANCE.md`                  | Q1 section | Q1 smoke result recorded as SKIP                                        | ⚠️ Warning | LM Studio not running during execution; the test infrastructure is committed; operator MUST re-run before Phase 5 verification. NOT a Phase 2 blocker — the test exists and skips cleanly. |
| `apps/renderer/src/screens/Chat/Chat.tsx` and `useStreamingMessages.ts` | various | hardcoded UI copy moved to COPY constants                              | ✓ N/A    | All copy is centralized in `apps/renderer/src/lib/copy.ts` (CHAT.THINKING, CHAT.STREAM_ERROR, CHAT.CONTEXT_OVERFLOW, LOGS.INTENT_PREFIX) — not an anti-pattern. |
| `sidecar/src/sidecar/orchestrator/transformers.py`                | 194-203   | "dead code; OLVT-port-faithfulness keeps it" comment for `<think>` handling | ℹ️ Info  | The think-tag-handling branch in display_processor is dead code given D-10's API-side disable, but the OLVT-port-faithfulness rationale is documented. Acceptable — plays defense-in-depth if a non-compliant model leaks `<think>`. |

No blocker anti-patterns found. The TODO at server.py:35 is a documented Phase-5 follow-up (electron-main env-var write), not a Phase-2 deliverable.

### Human Verification Required

Three behavioral truths require live LM Studio + a real model loaded; the code paths are programmatically proven, but live system confirmation is the operator's burden:

1. **SC #1 — three-sentence story streams as ONE growing bubble**
   - Test: With LM Studio running and a configured provider, type "tell me a 3-sentence story" → ONE growing assistant bubble accumulating three sentences.
   - Expected: Banner stays clear; input re-enables on chain-end; no duplicate bubbles.
   - Why human: Requires live LM Studio with a real model loaded. Programmatic tests at orchestrator + reducer level both PASS but live network round-trip is operator-only.

2. **SC #2 — `[joy]` strips bracket from chat AND fires `[INTENT]` log line**
   - Test: With LM Studio running, prompt the LLM to use `[joy]`; toggle Logs drawer; observe chat (no brackets) + green-prefixed `[INTENT] kind=expression name=joy ...`.
   - Expected: Chat panel shows bracket-free text; LogsDrawer shows the green `[INTENT]` prefix.
   - Why human: Requires live LLM emitting `[joy]` against the avatar.yaml tag vocabulary. Operator should also replace placeholder names in `avatars/teto/avatar.yaml` with the real Teto rig's names before this test for reliability.

3. **SC #4 — no `<think>...</think>` ever leaks into chat**
   - Test: With LM Studio running a compliant reasoning model (latest DeepSeek-R1 distill or Qwen3-Reasoning), confirm no `<think>` content appears in chat or in extracted ActionIntents.
   - Expected: Clean chat; reasoning suppressed at the API boundary.
   - Why human: Requires live LM Studio + reasoning-class model. The Q1 smoke test (`SIDECAR_SMOKE_LMSTUDIO=1`) is operator-gated and currently recorded SKIP in PROVENANCE.md. Operator must re-run this once before Phase 5 verification.

### Gaps Summary

No code-level gaps. All artifacts exist, are substantive, are wired, and have data flowing end-to-end. KV-cache discipline is enforced both at code level (no `_memory.pop/del/insert/remove/clear/slice-assign`) and via a regression test (`test_memory_pop_violation_absent`). The OLVT 4-decorator chain is ported verbatim where possible with three intentional, documented adaptations (display-side bracket-strip, ActionIntent[] in place of OLVT Actions, sentence_id Phase-2 extension). The renderer streaming reducer ports OLVT's growing-bubble + force-new-message seal verbatim with one documented skeleton-side adaptation (setThinking consumes the seal flag because the Thinking bubble IS the new turn's bubble). The wording amendments (ROADMAP SC #4 + REQUIREMENTS LLM-03) align with D-10's API-disable strategy.

The three "uncertain" truths (SC #1, #2, #4) all have full programmatic code-path proofs; the gating dependency is live LM Studio. SC #3 (split-bracket adversarial) and SC #5 (fresh-thread on relaunch) are fully verified programmatically — the BLOCKER and the in-memory-only invariant are closed.

---

_Verified: 2026-05-06T23:35:00Z_
_Verifier: Claude (gsd-verifier)_
