---
phase: 13-conversation-history-sessions
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/renderer/src/screens/Chat/Chat.tsx
  - apps/renderer/src/screens/Chat/useStreamingMessages.ts
  - apps/renderer/src/ws/store.ts
  - apps/renderer/src/chrome/HistorySheet.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/src/index.css
  - apps/renderer/tests/Chat.test.tsx
  - apps/renderer/tests/useStreamingMessages.test.ts
  - apps/renderer/tests/HistorySheet.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-05
must_haves:
  truths:
    - "D-01/D-02: HistorySheet is a full real manager with create, switch, rename, delete, and search/filtering; it no longer renders PLACEHOLDER_THREADS"
    - "D-03/D-18: Detailed session management stays in HistorySheet, not Settings"
    - "D-04/D-05/D-06/D-07: New chats start as New chat, auto-title from first user message on complete-turn commit, manual rename is preserved, and no LLM title-generation call is added"
    - "D-08/D-09/D-10/D-11: Normal completed sessions persist locally by default until delete-single or clear-all removes them"
    - "D-12/D-13/D-14/D-15: Chat commits only complete user/assistant turns on conversation-chain-end, skips failed/interrupted turns, and preserves the existing LLM/TTS/VTS streaming pipeline behavior"
    - "HIST-05: Chat history stores transcript messages only; no memory/retrieval/per-avatar memory is introduced"
  artifacts:
    - path: "apps/renderer/src/chrome/HistorySheet.tsx"
      provides: "Real History sheet manager for sessions, search/filtering, rename, delete, create, and switch"
      contains: "useConversationHistory"
    - path: "apps/renderer/src/screens/Chat/Chat.tsx"
      provides: "Chat rendering that hydrates persisted active-session messages and keeps in-flight streaming messages separate"
      contains: "persisted"
    - path: "apps/renderer/src/ws/store.ts"
      provides: "WS chain-end/error routing that supports complete-turn persistence without changing envelope semantics"
      contains: "conversation-chain-end"
    - path: "apps/renderer/tests/HistorySheet.test.tsx"
      provides: "Regression coverage for real session management and search/filtering"
      contains: "rename"
    - path: "apps/renderer/tests/Chat.test.tsx"
      provides: "Regression coverage for complete-turn persistence and failed-turn skip"
      contains: "commitConversationTurn"
  key_links:
    - from: "apps/renderer/src/ws/store.ts"
      to: "apps/renderer/src/screens/Chat/useStreamingMessages.ts"
      via: "chain-end obtains completed turn candidate"
      pattern: "conversation-chain-end"
    - from: "apps/renderer/src/screens/Chat/Chat.tsx"
      to: "apps/renderer/src/state/conversation-history.tsx"
      via: "active persisted messages + pending turn methods"
      pattern: "useConversationHistory"
---

<objective>
Wire real conversation sessions into the Chat and History user experience.

Purpose: replace placeholder history with a usable session manager and persist only completed user/assistant turns while keeping the current streaming, TTS, and VTS response pipeline intact. The user should be able to create, switch, rename, delete, and search sessions from the normal History sheet, and completed transcripts should rehydrate into Chat.

Output: real History sheet manager, Chat hydration from active session, complete-turn persistence on chain-end, failed-turn skip behavior, updated copy/styles, and focused Chat/History tests.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-conversation-history-sessions/13-CONTEXT.md
@.planning/phases/13-conversation-history-sessions/13-RESEARCH.md
@.planning/phases/13-conversation-history-sessions/13-01-PLAN.md

@apps/renderer/src/state/conversation-history.tsx
@apps/renderer/src/chrome/HistorySheet.tsx
@apps/renderer/src/screens/Chat/Chat.tsx
@apps/renderer/src/screens/Chat/useStreamingMessages.ts
@apps/renderer/src/ws/store.ts
@apps/renderer/src/lib/copy.ts
@apps/renderer/src/index.css
@apps/renderer/tests/Chat.test.tsx
@apps/renderer/tests/useStreamingMessages.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit completed-turn boundary to streaming chat</name>
  <files>
    - apps/renderer/src/screens/Chat/useStreamingMessages.ts
    - apps/renderer/src/ws/store.ts
    - apps/renderer/tests/useStreamingMessages.test.ts
  </files>
  <behavior>
    - `conversation-chain-end` can produce a completed-turn candidate containing the latest user text and assistant text.
    - Error envelopes clear or invalidate the pending turn so failed/interrupted turns are not persisted.
    - The reducer continues to render the existing Thinking/audio/force-new behavior exactly as before.
    - Existing tests for merge, force-new, thinking replacement, speaking, and input-disabled behavior remain green.
    - No storage side effect is triggered by ordinary React renders.
  </behavior>
  <action>
    Extend `useStreamingMessages.ts` with a production helper for completed-turn extraction. Do not rely on the test-only `_internalState()` name in production code; either rename it to a general snapshot helper or add a separate exported function.

    Suggested approach:
    - Track a pending turn from `appendUserMessage(text)`.
    - On audio payloads, accumulate assistant text in the existing assistant bubble.
    - On `setBanner(kind)` or WS `error`, mark the pending turn failed.
    - On `conversation-chain-end`, let `ws/store.ts` ask for one completed turn candidate and mark it consumed after successful commit.

    Keep `force-new-message` semantics unchanged. Force-new still seals the assistant bubble for display; persistence commits the user/assistant pair only once per completed turn.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- useStreamingMessages.test.ts</automated>
  </verify>
  <done>
    Streaming state exposes a safe one-shot complete-turn candidate and skips failed turns without changing visible streaming behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hydrate Chat from the active session and commit complete turns</name>
  <files>
    - apps/renderer/src/screens/Chat/Chat.tsx
    - apps/renderer/src/ws/store.ts
    - apps/renderer/src/state/conversation-history.tsx
    - apps/renderer/tests/Chat.test.tsx
  </files>
  <behavior>
    - Chat renders persisted active-session messages before in-flight streaming messages.
    - Sending a message still immediately renders the user bubble and sends the existing `{ type: 'text-input', text }` envelope.
    - Completed turns are committed to the active session on `conversation-chain-end`.
    - Failed/error turns are not committed.
    - The existing LLM streaming, TTS, VTS lipsync, speaking label, and input-disabled behavior are preserved.
    - Switching sessions during an in-flight turn is prevented or disabled so a turn cannot be committed to the wrong session.
  </behavior>
  <action>
    Integrate the conversation-history state from Plan 13-01 into Chat.

    Replace the current `chatMessages` persisted-like merge with:
    - active session persisted messages from `useConversationHistory()`
    - current in-flight streaming messages from `useStreamingMessages()`

    Avoid double-rendering after a commit. Once a turn commits and the active session refreshes, clear or reconcile consumed streaming messages so the same user/assistant pair does not appear twice.

    Update the WS dispatcher to call the conversation-history commit path only on `conversation-chain-end`. If the dispatcher cannot access React context directly, introduce a small module-level callback registration from the conversation-history provider and clean it up on unmount.

    Preserve the dev-only scripted-conversation injection as dev-only if still needed, but do not use `chatMessages` as the normal production transcript path.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- Chat.test.tsx useStreamingMessages.test.ts</automated>
  </verify>
  <done>
    Chat shows restored active-session transcript, streams live replies as before, and persists exactly one completed turn on chain-end.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace placeholder History sheet with real session manager</name>
  <files>
    - apps/renderer/src/chrome/HistorySheet.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/src/index.css
    - apps/renderer/tests/HistorySheet.test.tsx
  </files>
  <behavior>
    - History sheet lists real sessions grouped by date or equivalent ChatGPT-style buckets.
    - Search/filter input filters by title and preview text.
    - New Chat creates and selects a new empty session titled `New chat`.
    - Selecting a session switches the active session and closes or keeps the sheet in a predictable way.
    - Rename supports manual title override.
    - Delete removes one session and leaves a valid active session.
    - Row actions are accessible by button labels; destructive delete has confirmation or a clear guard.
    - Session switching, rename, and delete controls are disabled during an in-flight turn if needed to protect complete-turn persistence.
    - `PLACEHOLDER_THREADS` is no longer imported by normal History sheet code.
  </behavior>
  <action>
    Rewrite `HistorySheet` to consume `useConversationHistory()` instead of `PLACEHOLDER_THREADS` and `setChatMessages([])`.

    Convert the search row from static text to an actual input with accessible label/placeholder. Keep the existing sheet shell, Escape close, dimmed-strip close, and New Chat placement.

    Add row-level actions using existing icon/button patterns where available. Keep the UI dense and work-focused; avoid nested cards.

    Update `COPY.HISTORY` strings so they describe real local history, not a future milestone.
  </action>
  <verify>
    <automated>npm --workspace apps/renderer run test -- HistorySheet.test.tsx Chat.test.tsx</automated>
    <automated>npm --workspace apps/renderer run typecheck</automated>
  </verify>
  <done>
    History sheet is the real full session manager for Phase 13 and no longer depends on dev placeholder threads.
  </done>
</task>

</tasks>

<verification>
Run these checks after all tasks:

```powershell
npm --workspace apps/renderer run test -- HistorySheet.test.tsx Chat.test.tsx useStreamingMessages.test.ts
npm --workspace apps/renderer run typecheck
```

Manual/UAT spot check:
- Open History from Chat.
- Create a new chat, complete one LLM turn, close/reopen History, and confirm the session title updates from the first user message.
- Search by part of the title or preview.
- Rename the session and confirm another completed turn does not overwrite the manual title.
- Delete one session and confirm a valid active session remains.
- Trigger or simulate a stream error and confirm the failed turn is not saved.
</verification>

<success_criteria>
- HIST-01 is satisfied from the normal chat/history UI: create, switch, rename/title, delete, and search/filter.
- HIST-02 is satisfied at the renderer integration level: active session messages hydrate into Chat.
- HIST-03 is satisfied: sending and streaming preserve the existing LLM/TTS/VTS pipeline while committing completed turns.
- HIST-05 remains true: no memory/retrieval/per-avatar memory is added.
- Every Phase 13 streaming decision D-12 through D-15 is covered by tests.
- Normal History sheet rendering no longer imports `PLACEHOLDER_THREADS`.
</success_criteria>
