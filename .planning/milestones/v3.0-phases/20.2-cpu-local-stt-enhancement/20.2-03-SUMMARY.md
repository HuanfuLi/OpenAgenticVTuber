---
phase: 20.2-cpu-local-stt-enhancement
plan: 20.2-03
status: complete
completed: 2026-05-12
requirements-completed: [VIN-02, VIN-04, VIN-05]
---

# Phase 20.2 Plan 03: Edit Sent Message And Regenerate From There Summary

Added user-message edit/regenerate without grace-window undo: saving an edit truncates persisted history before that user message, then dispatches the edited text through the normal `text-input` path.

## Key Changes

- Added `truncateConversationBeforeMessage()` to Electron conversation storage.
- Exposed truncation through IPC, preload types, and renderer conversation history state.
- Added compact user-bubble edit UI, enabled only when no turn is active and WebSocket is open.
- Refactored Chat submission to accept authoritative history overrides and reuse stopped user bubbles without duplication.
- Updated sidecar memory sync so renderer-supplied history is authoritative even when it shrinks.

## Verification

- `npm --workspace apps/electron-main run test -- --run conversation ipc-voice-input` passed.
- `npm --workspace apps/renderer run test -- --run ConversationHistory ChatStreaming ChatVoiceInput` passed.
- `npm --workspace apps/renderer run typecheck` passed.
- `cd sidecar; uv run pytest tests/test_orchestrator_turn.py -q` passed.

## Deviations from Plan

None.

## Self-Check: PASSED
