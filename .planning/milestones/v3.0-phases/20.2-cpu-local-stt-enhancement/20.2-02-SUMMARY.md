---
phase: 20.2-cpu-local-stt-enhancement
plan: 20.2-02
status: complete
completed: 2026-05-12
requirements-completed: [VIN-04, VIN-05, PERF-01]
---

# Phase 20.2 Plan 02: Stop-Current-Turn Protocol And Send/Stop UX Summary

Added an explicit Stop action that cancels the active sidecar turn, clears queued/playing renderer audio, removes partial assistant output, and keeps the user message visible.

## Key Changes

- Added typed `stop-turn` WebSocket envelope.
- Made the sidecar turn loop track cancelable active turn tasks and clear pending inputs on stop.
- Added a stop handler that calls orchestrator cancellation without closing the socket.
- Added `stopAudioPlayback()` for renderer queued/current audio cleanup.
- Added stopped-turn streaming state that keeps the user bubble and ignores late partial chain messages.
- Replaced the Send icon with a red square Stop button during active streaming or speaking.

## Verification

- `npm --workspace apps/renderer run test -- --run ChatStreaming ws-store-audio ws-audio-player` passed.
- `npm --workspace apps/renderer run typecheck` passed.
- `cd sidecar; uv run pytest tests/test_orchestrator_turn.py tests/ws/test_stop_turn.py -q` passed.

## Deviations from Plan

None.

## Self-Check: PASSED
