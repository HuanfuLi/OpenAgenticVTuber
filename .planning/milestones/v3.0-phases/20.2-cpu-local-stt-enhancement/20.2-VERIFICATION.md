---
status: passed
phase: 20.2-cpu-local-stt-enhancement
verified: 2026-05-13
---

# Phase 20.2 Verification

## Result

Automated verification passed after the 20.2-05 gap closure, and the original stopped-message edit/regenerate UAT retest passed.

## Evidence

- Contract codegen: `npm run codegen:contracts` passed.
- Preview cleanup: `rg "previewing|previewTranscript|previewSequenceId|applyPreviewResult|setTransientPreview|clearTransientPreview|mode: 'preview'|mode: \"preview\"|VOICE_PREVIEW" apps packages sidecar` returned no active-code matches.
- Renderer: `npm --workspace apps/renderer run test -- --run voice-capture voice-input-store ChatVoiceInput ChatStreaming ws-store-audio ws-audio-player ConversationHistory` passed, 50 tests.
- Renderer typecheck: `npm --workspace apps/renderer run typecheck` passed.
- Electron main: `npm --workspace apps/electron-main run test -- --run conversation ipc-voice-input` passed, 11 tests.
- Electron build: `npm --workspace apps/electron-main run build` passed.
- Sidecar: `cd sidecar; uv run pytest tests/admin/test_audio_voice_input_endpoint.py tests/admin/test_audio_stt_local.py tests/stt/test_faster_whisper_provider.py tests/test_orchestrator_turn.py tests/ws/test_stop_turn.py -q` passed, 55 tests.
- `git diff --check` passed with line-ending warnings only.
- Gap closure focused test: `npm --workspace apps/renderer run test -- --run ChatStreaming` passed, 12 tests.
- Adjacent reducer/voice regression: `npm --workspace apps/renderer run test -- --run useStreamingMessages ChatVoiceInput` passed, 29 tests.

## Must-Haves

- Final-only voice input: passed.
- faster-whisper model quality not downgraded: passed; the selected model id/path are unchanged.
- CPU thread tuning: passed; CPU construction includes bounded `cpu_threads`, CUDA does not.
- Explicit-action warmup: passed; readiness/enable warm local providers without app boot loading.
- Stop-current-turn: passed; typed `stop-turn`, sidecar cancellation, renderer audio stop, and Send-to-Stop UI are covered.
- Edit/regenerate: passed in automated coverage; persisted user-message edit dispatch works, and stopped-message edit now remains restartable by message id after later chat activity.
- Memory resync after truncation: passed; renderer history shrink is authoritative.

## Gap Closure

- 20.2-05 stores stopped-turn restart metadata by message id instead of relying only on the current pending turn.
- Streaming bubbles now carry timestamps, and Chat orders persisted plus transient messages by timestamp so stopped messages remain anchored when later history appears.
- Save passes the stopped message id into restart logic, dispatches the edited text, exits edit mode, and avoids duplicate user bubbles.
- Regression added: delayed stopped-message edit after later history appears.

## Human Verification

- UAT Test 6 retest passed on 2026-05-13: stopped-message Save exits edit mode, dispatches the edited text, avoids duplicate user bubbles, and keeps the stopped message anchored after later chat activity.

## Residual Risks

- faster-whisper CUDA remains environment-dependent; this phase improves CPU local STT and does not claim CUDA is fixed.
- `whisper.cpp` Vulkan remains a future local GPU option, not implemented here.
- Phase 19/20 live STT provider acceptance remains tracked separately from Phase 20.2.
