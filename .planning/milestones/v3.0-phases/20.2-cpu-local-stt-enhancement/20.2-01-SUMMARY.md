---
phase: 20.2-cpu-local-stt-enhancement
plan: 20.2-01
status: complete
completed: 2026-05-12
requirements-completed: [STT-03, STT-06, VIN-02, VIN-04, PERF-01]
---

# Phase 20.2 Plan 01: Final-Only Voice Capture And CPU STT Tuning Summary

Removed live preview transcription from Chat voice input, kept final-only transcription, added bounded faster-whisper CPU thread construction, and warmed local STT providers only after explicit readiness/enable actions.

## Key Changes

- Removed `previewing` and `preview` transcription mode from shared voice-input contracts and regenerated TS/schema artifacts.
- Simplified `VoiceCapture` so MediaRecorder data only accumulates until stop/finalization.
- Removed preview transcript state, UI, copy, and tests from active Chat voice input.
- Added CPU-only `cpu_threads` for faster-whisper, capped at 8, with diagnostics.
- Added best-effort local STT warmup after explicit readiness/enable checks using the existing provider cache.

## Verification

- `npm run codegen:contracts` passed.
- `npm --workspace apps/renderer run test -- --run voice-capture voice-input-store ChatVoiceInput` passed.
- `cd sidecar; uv run pytest tests/admin/test_audio_voice_input_endpoint.py tests/admin/test_audio_stt_local.py tests/stt/test_faster_whisper_provider.py -q` passed.

## Deviations from Plan

`npm run check:contracts` was not used as a pass/fail signal because this repo's script exits nonzero when generated contract files legitimately differ from `HEAD`. `npm run codegen:contracts` was run instead and the generated files were kept.

## Self-Check: PASSED
