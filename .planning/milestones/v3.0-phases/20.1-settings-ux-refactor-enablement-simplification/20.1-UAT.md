---
status: resolved
phase: 20.1-settings-ux-refactor-enablement-simplification
source:
  - 20.1-01-SUMMARY.md
  - 20.1-02-SUMMARY.md
  - 20.1-03-SUMMARY.md
  - 20.1-04-SUMMARY.md
started: 2026-05-12T08:26:01Z
updated: 2026-05-12T09:15:00Z
---

## Current Test

[testing complete - gap closure implemented; 1 item skipped at user request]

## Tests

### 1. Settings Opens To Connection
expected: Open Settings. The first screen should show the Connection panel by default, with a left sidebar of Settings categories. It should not land on Voice settings or a long scrolling page.
result: issue
reported: "Pass but issue found: OpenAI/Gemini/Claude API support showing \"Comming v2\" mock state but V2 has been a while ago. Implementation gap or stale mock?"
severity: major

### 2. Sidebar Category Navigation
expected: The sidebar should show Connection, Voice output, Voice input, Avatars, Integrations, Conversation, Memory, Appearance, Diagnostics, and About. Clicking each category should swap the right-side panel without jumping through a long scroll.
result: pass

### 3. Voice Output Optional Diagnostics
expected: In Voice output, GPT-SoVITS health and synthesis tests should be available as diagnostics, but activating a complete GPT-SoVITS preset should not require running those tests first.
result: issue
reported: "Pass with issue found: Piper local TTS and GOT-SoVITS have different enablement action. There is no way to enable PiperTTS: user can only select it from 2 providers (Piper and GPT-SoVITS), and sidecar does not restart when selecting PiperTTS. While GPT-SoVITS requires pressing Activate voice preset button. Also, in GPT-SoVITS, the buttons are messy and confusing. Most important \"Activate\" button is placed in a bad position, and buttons have different colors/focus but actually same or wrong priority, such as \"Run health check\" vs \"Test synthesis\" and \"New preset\" vs \"Save preset\""
severity: major

### 4. Voice Input Optional Diagnostics
expected: In Voice input, Test selected STT should remain available, but enabling voice input should depend on required setup such as local model cache or cloud consent/API key, not on a previous passing STT test.
result: issue
reported: "Fail: faster-whisper CUDA transcription failed. Check NVIDIA driver, CUDA availability, and ctranslate2 GPU support. This regression is probably due to a previous inline fix that focused on generation speed enhancement"
severity: blocker

### 5. Required Setup Blockers
expected: Missing required inputs should still block clearly: cloud STT requires consent and API key, local STT requires a downloaded/available model, and GPT-SoVITS requires a usable server URL, preset, and reference audio.
result: pass

### 6. Failed Diagnostics Do Not Mutate Enabled Config
expected: A failed GPT-SoVITS or STT diagnostic should show a visible diagnostic result, but it should not silently switch providers, disable voice input, delete active presets, or rewrite saved enabled settings.
result: pass

### 7. Deferred Placeholders Hidden
expected: Settings should not show unusable placeholder sections for Skills, Agent, Scheduler, Form factor, or Hotkeys.
result: pass

### 8. Safety Invariants
expected: Cloud audio remains explicit opt-in, secrets/reference paths/transcript-like diagnostic data remain redacted, provider runtime failures are visible, and the app does not silently fall back between providers mid-turn.
result: skipped

## Summary

total: 8
passed: 4
issues: 3
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "Connection Settings should not show stale mock roadmap copy such as OpenAI/Gemini/Claude API support marked as \"Comming v2\" after v2 has already shipped."
  status: resolved
  reason: "User reported: Pass but issue found: OpenAI/Gemini/Claude API support showing \"Comming v2\" mock state but V2 has been a while ago. Implementation gap or stale mock?"
  diagnosis: "Stale mock roadmap state remains in `apps/renderer/src/lib/copy.ts` (`DISABLED_PROVIDER_TT`, hosted provider statuses) and `apps/renderer/src/screens/LLMSetup/ProviderSelect.tsx` comments/rendering. The UI still frames hosted providers as future-v2 placeholders instead of either implemented options or explicit non-current setup paths."
  severity: major
  test: 1
  artifacts:
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/src/screens/LLMSetup/ProviderSelect.tsx
  missing:
    - "Truthful hosted-provider state in Connection/LLM setup after v2."
    - "Regression coverage preventing stale 'Coming v2' provider copy."
  resolution: "20.1-05 removed stale hosted-provider placeholders, removed Coming v2/Comming/DISABLED_PROVIDER_TT matches from active renderer source/tests, and added LLMSetup regression coverage."
- truth: "Voice output provider enablement should be logically consistent and the highest-priority action should be visually and spatially clear."
  status: resolved
  reason: "User reported: Pass with issue found: Piper local TTS and GOT-SoVITS have different enablement action. There is no way to enable PiperTTS: user can only select it from 2 providers (Piper and GPT-SoVITS), and sidecar does not restart when selecting PiperTTS. While GPT-SoVITS requires pressing Activate voice preset button. Also, in GPT-SoVITS, the buttons are messy and confusing. Most important \"Activate\" button is placed in a bad position, and buttons have different colors/focus but actually same or wrong priority, such as \"Run health check\" vs \"Test synthesis\" and \"New preset\" vs \"Save preset\""
  diagnosis: "Voice output currently mixes provider choice, preset editing, diagnostics, and activation in one panel. Piper saves implicitly through provider selection while GPT-SoVITS requires a separate activation action. The GPT-SoVITS action row gives diagnostics and activation similar visual priority, and preset CRUD actions use priority styles that do not match user workflow importance."
  severity: major
  test: 3
  artifacts:
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/src/index.css
  missing:
    - "One consistent provider activation model for Piper and GPT-SoVITS."
    - "Sidecar/runtime refresh when the active TTS provider changes."
    - "Clear primary action hierarchy for Activate vs diagnostics vs preset CRUD."
  resolution: "20.1-06 added explicit Piper activation, moved GPT-SoVITS activation ahead of diagnostics, demoted diagnostics, and corrected preset action priority with renderer regression coverage."
- truth: "Voice input diagnostics and enablement should work for configured faster-whisper CUDA without regressions from runtime speed changes."
  status: resolved
  reason: "User reported: Fail: faster-whisper CUDA transcription failed. Check NVIDIA driver, CUDA availability, and ctranslate2 GPU support. This regression is probably due to a previous inline fix that focused on generation speed enhancement"
  diagnosis: "The faster-whisper provider now passes `runtime_device='cuda'` directly into `WhisperModel` and reports CUDA runtime/transcription failure only after the selected path is used. Settings allows CUDA selection without a reliable preflight or recovery path, so a speed-oriented runtime selection can regress local transcription instead of surfacing availability before use."
  severity: blocker
  test: 4
  artifacts:
    - sidecar/src/sidecar/stt/providers/faster_whisper_provider.py
    - sidecar/tests/stt/test_faster_whisper_provider.py
    - apps/renderer/src/screens/Settings/Settings.tsx
    - apps/renderer/tests/Settings.test.tsx
  missing:
    - "CUDA runtime validation before users rely on faster-whisper transcription."
    - "Actionable Settings feedback for unsupported CUDA without silent provider fallback."
    - "Regression coverage for CUDA selection, failure reporting, and CPU/default safety."
  resolution: "20.1-07 added faster-whisper runtime validation before transcription, visible CUDA failure diagnostics, explicit CPU recovery, and sidecar/renderer regression coverage. Live CUDA retry remains useful on the user's machine."
