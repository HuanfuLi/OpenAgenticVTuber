---
phase: 17-gpt-sovits-provider-voice-presets
plan: 10
type: execute
wave: 10
depends_on: [17-09]
files_modified:
  - sidecar/src/sidecar/tts/audio_payload_helpers.py
  - sidecar/src/sidecar/tts/tts_manager.py
  - sidecar/src/sidecar/tts/tts_gateway.py
  - sidecar/tests/test_audio_payload_helpers.py
  - sidecar/tests/test_tts_manager.py
  - sidecar/tests/test_tts_gateway.py
  - .planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
autonomous: true
gap_closure: true
requirements: [TTS-06]
must_haves:
  truths:
    - "GPT-SoVITS chat playback and compositor RMS/lipsync use the same stream-rate-aligned PCM duration when the returned WAV sample rate differs from the pre-opened output stream."
    - "Piper and matching-rate GPT-SoVITS continue to use the existing no-op PCM path when provider sample rate equals output stream sample rate."
    - "Phase 17 UAT is reopened/marked partial or testing for the sample-rate/lipsync regression until live retest evidence is recorded."
  artifacts:
    - path: "sidecar/src/sidecar/tts/audio_payload_helpers.py"
      provides: "optional stream-rate PCM resampling before WAV payload and RMS envelope generation"
      contains: "target_sample_rate"
    - path: "sidecar/src/sidecar/tts/tts_manager.py"
      provides: "TTSTaskManager passes output stream sample rate into provider payload preparation and writes aligned int16 PCM"
      contains: "stream_sample_rate"
    - path: "sidecar/tests/test_audio_payload_helpers.py"
      provides: "focused helper coverage for resample and no-op behavior"
      contains: "target_sample_rate"
  key_links:
    - from: "sidecar/src/sidecar/tts/tts_manager.py"
      to: "sidecar/src/sidecar/tts/audio_payload_helpers.py"
      via: "prepare_payload_from_pcm(..., target_sample_rate=self._stream_sample_rate) for provider-backed synthesis"
      pattern: "target_sample_rate=.*_stream_sample_rate"
    - from: "sidecar/src/sidecar/tts/tts_manager.py"
      to: "sounddevice.OutputStream.write"
      via: "pcm_bytes returned from payload helper are converted to int16 and written"
      pattern: "np\.frombuffer\(next_payload\.pcm_bytes, dtype=np\.int16\)"
---

<objective>
Close the Phase 17 GPT-SoVITS sample-rate/lipsync regression only.

Purpose: GPT-SoVITS may return WAV PCM at a different sample rate than the long-lived output stream opened at boot; playback bytes, renderer WAV payload, and compositor RMS envelope must all describe the same stream-rate duration so mouth movement does not end early or late.

Output: Stream-rate-aligned PCM payload preparation, manager/gateway wiring, regression tests, and UAT reopened/partial evidence for this gap.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-09-SUMMARY.md
@sidecar/src/sidecar/tts/audio_payload_helpers.py
@sidecar/src/sidecar/tts/tts_manager.py
@sidecar/src/sidecar/tts/tts_gateway.py
@sidecar/tests/test_audio_payload_helpers.py
@sidecar/tests/test_tts_manager.py
@sidecar/tests/test_tts_gateway.py

<interfaces>
Existing helper contract to extend without breaking current callers:

```python
def prepare_payload_from_pcm(
    pcm_int16: bytes,
    sample_rate: int,
    display_text: DisplayTextField,
    dispatches: list[Dispatch],
    sentence_id: int,
    chunk_length_ms: int = 20,
) -> tuple[AudioPayloadMessage, bytes, int]
```

Current provider-backed manager path:

```python
result = await asyncio.to_thread(self._provider.synthesize, TTSSynthesisRequest(...))
return prepare_payload_from_pcm(result.pcm_int16, result.sample_rate, display_text, dispatches, sentence_id)
```
</interfaces>
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---:|---|---:|---|---|
| GOAL | — | Users can choose GPT-SoVITS for character voice output without losing existing playback/RMS/lipsync safety. | 17-10 | COVERED | This plan covers only stream-rate alignment; weight selection moves to 17-11. |
| REQ | TTS-06 | Active GPT-SoVITS chat turn uses existing renderer audio/RMS/lipsync path. | 17-10 | COVERED | Resamples returned PCM to stream sample rate before WAV payload, RMS, and stream write. |
| CONTEXT | diagnosed gap 1 | Lipsync stops before audible GPT-SoVITS sentence ends due to stream sample-rate mismatch. | 17-10 | COVERED | Tests must prove 32k→24k and 48k→22.05k alignment. |
| CONTEXT | diagnosed gap 2 | GPT-SoVITS may not sound like user's pretrained model due to missing weight selection. | 17-11 | COVERED ELSEWHERE | Explicitly excluded from 17-10 per checker; planned in 17-11. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stream-rate payload helper coverage and resampling</name>
  <files>sidecar/src/sidecar/tts/audio_payload_helpers.py, sidecar/tests/test_audio_payload_helpers.py</files>
  <behavior>
    - When `prepare_payload_from_pcm` receives 32 kHz mono int16 PCM with `target_sample_rate=24000`, the returned PCM byte length, WAV header sample rate, and RMS chunk count all correspond to 24 kHz output.
    - When it receives 48 kHz PCM with `target_sample_rate=22050`, the returned sample count is approximately `original_samples * 22050 / 48000`, the WAV header reports 22050 Hz, and RMS uses the resampled bytes.
    - When `target_sample_rate` is absent or equals `sample_rate`, returned PCM bytes and sample rate are unchanged for Piper-compatible no-op behavior.
  </behavior>
  <action>Add an optional `target_sample_rate: int | None = None` parameter to `prepare_payload_from_pcm`. Implement a small mono int16 resampling helper using existing `numpy` only; use linear interpolation or equivalent deterministic conversion, clamp to int16, and preserve empty/silent fast paths. If target is absent, non-positive, or equal to source, keep existing behavior byte-for-byte. If rates differ, resample before `get_volume_by_chunks`, before WAV header creation, and before returning `pcm_bytes` for `stream.write`. Add tests that decode the returned WAV with `wave.open` and assert header rate, sample-count ratio, and no-op equality.</action>
  <verify>
    <automated>uv run --project sidecar python -m pytest sidecar/tests/test_audio_payload_helpers.py -q</automated>
  </verify>
  <done>Payload helper produces stream-rate WAV/RMS/write bytes for mismatched GPT-SoVITS rates while preserving existing no-op behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire provider playback through stream sample rate without reopening streams</name>
  <files>sidecar/src/sidecar/tts/tts_manager.py, sidecar/src/sidecar/tts/tts_gateway.py, sidecar/tests/test_tts_manager.py, sidecar/tests/test_tts_gateway.py</files>
  <behavior>
    - Provider-backed `TTSTaskManager` synthesis passes the output stream sample rate to `prepare_payload_from_pcm`; playback write, websocket WAV payload, and compositor envelope all use aligned bytes.
    - Piper/non-provider synthesis remains unchanged and does not resample unless existing Piper voice output differs through the helper's default no-op path.
    - `TTSGateway.boot()` still opens one long-lived `sounddevice.OutputStream`; it does not close/reopen the stream per GPT-SoVITS response sample rate.
  </behavior>
  <action>Teach `TTSTaskManager` the output stream sample rate by accepting an optional `stream_sample_rate` constructor argument and/or deriving it from the fake/real stream `samplerate` attribute after `TTSGateway.boot()`. For provider-backed synthesis only, pass that rate as `target_sample_rate` to `prepare_payload_from_pcm`. Keep the long-lived stream lifecycle in `TTSGateway.boot()` unchanged. Update manager/gateway tests with fake streams/providers: one provider returns 32 kHz PCM while the stream is 24 kHz and assertions inspect `stream.write` sample count plus payload/envelope duration; another matching-rate/Piper path asserts existing bytes are unchanged.</action>
  <verify>
    <automated>uv run --project sidecar python -m pytest sidecar/tests/test_tts_manager.py sidecar/tests/test_tts_gateway.py -q</automated>
  </verify>
  <done>GPT-SoVITS mismatched-rate chat audio writes stream-rate PCM and emits matching RMS/WAV payloads without changing Piper or stream lifecycle behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Reopen UAT for sample-rate/lipsync regression without claiming retest pass</name>
  <files>.planning/phases/17-gpt-sovits-provider-voice-presets/17-UAT.md</files>
  <action>Update `17-UAT.md` so Phase 17 is `partial` or `testing`, not complete, while this regression is awaiting execution and live retest. Keep the good existing diagnosis/evidence content. Add a gap for stream/sample-rate lipsync ending early, marked addressed by Plan 17-10 only after implementation. Add manual retest steps: use a GPT-SoVITS server returning a non-stream-rate WAV, activate the preset, send a sentence long enough to observe mouth motion, and confirm audible sentence duration and mouth/RMS duration end together. Do not mark complete until retest evidence is recorded.</action>
  <verify>
    <automated>uv run --project sidecar python -m pytest sidecar/tests/test_audio_payload_helpers.py sidecar/tests/test_tts_manager.py sidecar/tests/test_tts_gateway.py -q</automated>
  </verify>
  <done>UAT truthfully reflects the reopened sample-rate/lipsync gap and contains Plan 17-10 retest criteria without claiming pass before retest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GPT-SoVITS WAV response → audio stream/compositor/renderer | External server returns audio bytes and sample rate that drive playback and lipsync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-17-10-01 | Tampering | GPT-SoVITS returned WAV sample rate | mitigate | Treat provider sample rate as untrusted input and normalize returned PCM to the actual output stream sample rate before playback and RMS. |
| T-17-10-02 | Denial of Service | resampling helper | mitigate | Use bounded in-memory numpy conversion on the already returned sentence PCM; preserve empty/no-op fast paths. |
| T-17-10-03 | Repudiation | UAT completion state | mitigate | Reopen UAT and record automated evidence plus live retest criteria before complete status can be reclaimed. |
</threat_model>

<verification>
Run the focused regression set:

```powershell
uv run --project sidecar python -m pytest sidecar/tests/test_audio_payload_helpers.py sidecar/tests/test_tts_manager.py sidecar/tests/test_tts_gateway.py -q
```
</verification>

<success_criteria>
- 32 kHz and 48 kHz GPT-SoVITS returned WAVs are resampled to the output stream sample rate before both `stream.write` and RMS/WAV payload creation.
- Matching sample-rate Piper/GPT-SoVITS behavior remains unchanged.
- `17-UAT.md` is reopened/partial or testing for this gap and does not claim live retest completion.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-10-SUMMARY.md`.
</output>
