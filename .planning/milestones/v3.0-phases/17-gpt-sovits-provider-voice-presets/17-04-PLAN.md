---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - apps/electron-main/src/ipc.ts
  - apps/electron-main/preload/index.ts
  - apps/electron-main/preload/index.d.ts
  - apps/electron-main/tests/ipc-gpt-sovits-audio.test.ts
autonomous: true
requirements:
  - TTS-01
  - TTS-02
  - TTS-04
  - TTS-06
must_haves:
  truths:
    - "Renderer can request GPT-SoVITS health and test synthesis through Electron preload, not direct sidecar HTTP."
    - "Test synthesis bridge returns preview audio metadata without sending chat, writing history, or activating failed candidates."
  artifacts:
    - path: apps/electron-main/src/ipc.ts
      provides: GPT-SoVITS health/test IPC handlers
    - path: apps/electron-main/preload/index.d.ts
      provides: checkGptSoVitsHealth and testGptSoVitsSynthesis renderer API declarations
  key_links:
    - from: apps/renderer/src/screens/Settings/Settings.tsx
      to: apps/electron-main/preload/index.ts
      via: `window.api.checkGptSoVitsHealth` and `window.api.testGptSoVitsSynthesis`
      pattern: "checkGptSoVitsHealth|testGptSoVitsSynthesis"
---

# 17-04 Plan: Electron GPT-SoVITS Health/Test IPC Bridge

<objective>
Expose the sidecar GPT-SoVITS health and test-synthesis endpoints through concrete Electron IPC/preload methods for Settings.

Purpose: Renderer code needs a narrow, typed preload bridge for candidate validation and audible test synthesis without bypassing Electron's IPC boundary.
Output: IPC handlers, preload methods/declarations, and focused Electron-main tests.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-03-SUMMARY.md

<interfaces>
Use sidecar endpoints from Plan 17-03: `POST /admin/audio/gpt-sovits/health` and `POST /admin/audio/test-synthesis`. Required preload methods: `checkGptSoVitsHealth(candidate)` and `testGptSoVitsSynthesis(candidate)`. Do not make Settings call sidecar HTTP directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GPT-SoVITS health and test synthesis IPC handlers</name>
  <files>apps/electron-main/src/ipc.ts, apps/electron-main/tests/ipc-gpt-sovits-audio.test.ts</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` IPC/preload pattern
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-01, D-03, D-09 through D-12
    - `apps/electron-main/src/ipc.ts`
  </read_first>
  <behavior>
    - Test 1: IPC `gptSovits:checkHealth` proxies `/admin/audio/gpt-sovits/health` with the candidate config body.
    - Test 2: IPC `gptSovits:testSynthesis` proxies `/admin/audio/test-synthesis` and returns base64/audio metadata for renderer playback.
    - Test 3: IPC error paths return typed unavailable/failure responses with redacted diagnostics.
    - Test 4: IPC cleanup unregisters new handlers.
  </behavior>
  <action>Add concrete IPC channels `gptSovits:checkHealth` and `gptSovits:testSynthesis`. Use the existing sidecar admin request helper/pattern from `ipc.ts`; keep request/response shapes generated from contracts. Do not activate provider config, save presets, send chat turns, or write conversation history from these handlers.</action>
  <acceptance_criteria>
    - Tests cover success, sidecar unavailable, failure response, and cleanup unregister behavior.
    - Handler implementation does not import renderer modules or bypass sidecar admin API helpers.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/electron-main run test -- --run ipc-gpt-sovits-audio.test.ts</automated>
  </verify>
  <done>Electron main exposes typed GPT-SoVITS health/test synthesis IPC channels backed by sidecar admin endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preload methods and declarations</name>
  <files>apps/electron-main/preload/index.ts, apps/electron-main/preload/index.d.ts, apps/electron-main/tests/ipc-gpt-sovits-audio.test.ts</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` preload allowlist pattern
    - `apps/electron-main/preload/index.ts`
    - `apps/electron-main/preload/index.d.ts`
  </read_first>
  <behavior>
    - Test 1: `window.api.checkGptSoVitsHealth(candidate)` invokes IPC `gptSovits:checkHealth`.
    - Test 2: `window.api.testGptSoVitsSynthesis(candidate)` invokes IPC `gptSovits:testSynthesis`.
    - Test 3: Both declarations compile with generated contract imports.
  </behavior>
  <action>Add preload methods `checkGptSoVitsHealth` and `testGptSoVitsSynthesis` in `index.ts` and matching types in `index.d.ts`. Keep this as an allowlisted bridge only; Settings consumes these methods in Plan 17-06.</action>
  <acceptance_criteria>
    - `grep -v '^#' apps/electron-main/preload/index.d.ts | grep -c "checkGptSoVitsHealth\|testGptSoVitsSynthesis"` returns at least 2.
    - Electron main build passes with generated contract imports.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/electron-main run test -- --run ipc-gpt-sovits-audio.test.ts</automated>
    <automated>npm --workspace apps/electron-main run build</automated>
  </verify>
  <done>Settings has concrete preload health/test synthesis methods to call in later renderer work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→Electron→sidecar | Candidate health/test requests cross IPC/admin boundaries. |
| sidecar response→renderer | Audio bytes and typed failures are returned to renderer for preview/status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | T/I | GPT-SoVITS IPC request | mitigate | Use generated request types and existing sidecar admin proxy helpers; renderer cannot call arbitrary URLs. |
| T-17-20 | I | test synthesis errors | mitigate | Return concise summaries/redacted diagnostics and route details to logs. |
</threat_model>

<verification>
```powershell
npm --workspace apps/electron-main run test -- --run ipc-gpt-sovits-audio.test.ts
npm --workspace apps/electron-main run build
```
</verification>

<success_criteria>
- GPT-SoVITS health and test synthesis are available through concrete preload methods.
- Renderer cannot bypass Electron IPC to call sidecar HTTP directly for these operations.
- IPC failure responses are typed and redacted.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-04-SUMMARY.md`.
</output>
