---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - sidecar/pyproject.toml
  - sidecar/src/sidecar/tts/gpt_sovits_provider.py
  - sidecar/src/sidecar/tts/tts_gateway.py
  - sidecar/src/sidecar/tts/provider.py
  - sidecar/src/sidecar/admin/audio.py
  - sidecar/src/sidecar/admin/__init__.py
  - sidecar/src/sidecar/ws/server.py
  - sidecar/tests/tts/test_gpt_sovits_provider.py
  - sidecar/tests/admin/test_audio_test_tts_endpoint.py
  - sidecar/tests/test_tts_gateway.py
  - sidecar/tests/test_tts_manager.py
autonomous: true
requirements:
  - TTS-01
  - TTS-02
  - TTS-04
  - TTS-06
  - PRESET-02
  - PRESET-03
must_haves:
  truths:
    - "Sidecar can health-check GPT-SoVITS from one base URL and report typed states."
    - "Test synthesis returns preview audio through sidecar admin endpoints without sending a chat turn, writing history, or activating failed candidates."
    - "GPT-SoVITS chat failures produce failed-audio payloads and never silently switch to Piper mid-turn."
  artifacts:
    - path: sidecar/src/sidecar/tts/gpt_sovits_provider.py
      provides: synthesize-only GPT-SoVITS HTTP adapter
    - path: sidecar/src/sidecar/admin/audio.py
      provides: health and test-synthesis admin endpoints
    - path: sidecar/src/sidecar/tts/tts_gateway.py
      provides: provider construction and active config gate wiring
  key_links:
    - from: sidecar/src/sidecar/tts/tts_gateway.py
      to: sidecar/src/sidecar/tts/gpt_sovits_provider.py
      via: `build_tts_gateway()` active config provider construction
      pattern: "GptSoVitsProvider|gpt_sovits"
    - from: sidecar/src/sidecar/admin/audio.py
      to: sidecar/src/sidecar/tts/gpt_sovits_provider.py
      via: candidate health/test calls with no activation on failure
      pattern: "test-synthesis|gpt-sovits/health"
---

# 17-03 Plan: GPT-SoVITS Sidecar Provider, Health/Test Synthesis, and Failure Wiring

<objective>
Implement the GPT-SoVITS sidecar provider, admin health/test-synthesis endpoints, and no-silent-fallback failure behavior.

Purpose: Users must prove GPT-SoVITS health and hear test synthesis before activation, without sending chat turns or silently falling back.
Output: Provider adapter, admin endpoints, mocked sidecar tests, and queue-safe failure behavior.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-01-SUMMARY.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-02-SUMMARY.md

<interfaces>
Use Phase 16 `TTSProvider`, `TTSSynthesisRequest`, `TTSSynthesisResult`, and `TTSProviderError`. Use contracts from Plans 17-01/17-02. This plan stops at sidecar admin/provider behavior; Plan 17-04 adds the Electron IPC/preload bridge.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GPT-SoVITS HTTP provider adapter</name>
  <files>sidecar/pyproject.toml, sidecar/src/sidecar/tts/gpt_sovits_provider.py, sidecar/src/sidecar/tts/provider.py, sidecar/tests/tts/test_gpt_sovits_provider.py</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-01, D-03, D-09, D-10, D-12, D-14, D-15
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` `Open Questions (RESOLVED)`, `/tts` findings, and pitfalls
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` provider adapter pattern
    - `sidecar/src/sidecar/tts/provider.py`
    - `sidecar/src/sidecar/tts/piper_provider.py`
    - `sidecar/src/sidecar/tts/tts_manager.py`
  </read_first>
  <behavior>
    - Test 1: Provider derives `POST {base_url}/tts` and never stores separate endpoint URLs per D-01.
    - Test 2: Success `audio/wav` decodes to `TTSSynthesisResult(provider_id="gpt_sovits")`.
    - Test 3: HTTP 400 JSON maps to `external_service_failure`, timeout to `timeout`, invalid base URL to `misconfigured`, and unreadable reference path to the resolved reference-path failure summary.
    - Test 4: Payload includes required GPT-SoVITS fields and uses `media_type="wav"`, `streaming_mode=False`.
  </behavior>
  <action>Add `GptSoVitsProvider(config, preset, reference_audio, timeout_ms=30000)`. Use `httpx` and `soundfile==0.13.1` for WAV decode. Keep provider synthesize-only: no sounddevice writes, WebSocket sends, compositor queues, renderer imports, or Piper fallback selection.</action>
  <acceptance_criteria>
    - `grep -v '^#' sidecar/src/sidecar/tts/gpt_sovits_provider.py | grep -c "streaming_mode.*False"` returns at least 1.
    - Provider tests mock success WAV, JSON 400, timeout, and reference-path/server-read failures.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest sidecar/tests/tts/test_gpt_sovits_provider.py -q</automated>
  </verify>
  <done>GPT-SoVITS can synthesize provider-neutral audio artifacts and typed failures from `POST /tts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin health/test endpoints and queue-safe failure wiring</name>
  <files>sidecar/src/sidecar/tts/tts_gateway.py, sidecar/src/sidecar/tts/tts_manager.py, sidecar/src/sidecar/admin/audio.py, sidecar/src/sidecar/admin/__init__.py, sidecar/src/sidecar/ws/server.py, sidecar/tests/admin/test_audio_test_tts_endpoint.py, sidecar/tests/test_tts_gateway.py, sidecar/tests/test_tts_manager.py</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-03, D-09 through D-12
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` Per-Task Verification Map
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` admin audio and TTSTaskManager patterns
    - `sidecar/src/sidecar/admin/audio.py`
    - `sidecar/src/sidecar/tts/tts_gateway.py`
    - `sidecar/src/sidecar/tts/tts_manager.py`
  </read_first>
  <behavior>
    - Test 1: `POST /admin/audio/gpt-sovits/health` returns `AudioProviderHealth` for candidate config without activation.
    - Test 2: `POST /admin/audio/test-synthesis` returns base64 WAV/audio metadata and never enqueues chat, writes history, or saves active config.
    - Test 3: Failed test returns `activation_allowed=false` and previous active provider/preset is unchanged per D-12.
    - Test 4: GPT-SoVITS chat synthesis failure sends ordered `AudioPayloadMessage(audio=None, failed_audio.provider_id="gpt_sovits")` and no Piper synthesis for the same turn.
  </behavior>
  <action>Extend `/admin/audio` with `POST /gpt-sovits/health` and `POST /test-synthesis`. Extend `build_tts_gateway()` to construct `GptSoVitsProvider` only when active config gates passed and active preset/reference exists. Keep `TTSTaskManager` playback/order/RMS ownership unchanged.</action>
  <acceptance_criteria>
    - Endpoint tests verify no mutation of `app.state.audio_config` active provider on failed candidate test.
    - Manager tests assert failed GPT-SoVITS sentence text is emitted and no silent Piper switch occurs.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest sidecar/tests/admin/test_audio_test_tts_endpoint.py sidecar/tests/admin/test_audio_status_endpoint.py sidecar/tests/test_tts_gateway.py sidecar/tests/test_tts_manager.py -q</automated>
  </verify>
  <done>Health/test synthesis and chat failures obey activation and no-silent-fallback decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar→external GPT-SoVITS HTTP | Sidecar sends text, reference path, and tuning to a user-configured service. |
| provider failure→chat payload | External errors become failed-audio payload metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | T/I | GPT-SoVITS base URL | mitigate | Validate http(s) base URL, derive `/tts`, and expose non-localhost warning in UI plan. |
| T-17-08 | D | HTTP synthesis/test | mitigate | Use explicit timeouts and provider work off the event loop. |
| T-17-09 | I | test synthesis errors | mitigate | Return concise summaries/redacted diagnostics and route details to logs. |
</threat_model>

<verification>
```powershell
python -m pytest sidecar/tests/tts/test_gpt_sovits_provider.py sidecar/tests/admin/test_audio_test_tts_endpoint.py sidecar/tests/admin/test_audio_status_endpoint.py sidecar/tests/test_tts_gateway.py sidecar/tests/test_tts_manager.py -q
```
</verification>

<success_criteria>
- GPT-SoVITS health and test synthesis are available through sidecar admin endpoints.
- Test synthesis returns audio for preview and never sends chat/history or activates failed candidates.
- GPT-SoVITS provider failures never silently switch to Piper mid-turn.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-03-SUMMARY.md`.
</output>
