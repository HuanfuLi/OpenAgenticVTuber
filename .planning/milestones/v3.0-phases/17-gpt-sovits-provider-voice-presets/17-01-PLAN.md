---
phase: 17-gpt-sovits-provider-voice-presets
plan: 17-01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/py/contracts/audio_provider.py
  - packages/contracts/py/contracts/voice_preset.py
  - packages/contracts/py/contracts/audio_payload.py
  - packages/contracts/py/contracts/__init__.py
  - packages/contracts/scripts/codegen.py
  - packages/contracts/ts/audio-provider.ts
  - packages/contracts/ts/voice-preset.ts
  - packages/contracts/ts/audio-payload.ts
  - packages/contracts/ts/index.ts
  - packages/contracts/generated/json-schema/audio-provider.schema.json
  - packages/contracts/generated/json-schema/voice-preset.schema.json
  - packages/contracts/generated/json-schema/audio-payload.schema.json
  - packages/contracts/tests/test_codegen.py
  - packages/contracts/tests/test_codegen_drift.py
autonomous: false
requirements:
  - TTS-01
  - TTS-02
  - TTS-03
  - TTS-04
  - TTS-06
  - PRESET-01
  - PRESET-02
  - PRESET-03
  - PRESET-04
must_haves:
  truths:
    - "Phase 17 execution is blocked until Phase 16 manual UAT is approved in STATE.md or 16-UAT evidence."
    - "Contracts can represent Piper or gated GPT-SoVITS, test synthesis audio, failed-audio metadata, global presets, reference assets, and active avatar/session preset associations."
    - "Presets own provider-specific voice knobs and reference metadata but never duplicate GPT-SoVITS base URL or launch command."
  artifacts:
    - path: packages/contracts/py/contracts/voice_preset.py
      provides: VoicePreset, ReferenceAudioAsset, and VoicePresetLibrary source contracts
    - path: packages/contracts/py/contracts/audio_provider.py
      provides: GPT-SoVITS config, health/test request/response, and activation gate fields
    - path: packages/contracts/py/contracts/audio_payload.py
      provides: failed_audio metadata and test synthesis audio payload contract
  key_links:
    - from: packages/contracts/py/contracts/voice_preset.py
      to: packages/contracts/ts/voice-preset.ts
      via: contract codegen
      pattern: "VoicePreset|ReferenceAudioAsset"
    - from: packages/contracts/py/contracts/audio_provider.py
      to: packages/contracts/ts/audio-provider.ts
      via: contract codegen
      pattern: "GptSoVits|TestSynthesis|activation"
---

# 17-01 Plan: Contracts and Blocking Phase-16 UAT Gate

<objective>
Create the Phase 17 contract foundation and enforce the Phase 16 dependency boundary before any executable GPT-SoVITS work starts.

Purpose: Downstream sidecar, Electron, and renderer plans need stable generated contracts, and Phase 17 must not execute while Phase 16 manual UAT is still pending.
Output: Pydantic contracts, generated TypeScript/JSON schema outputs, contract tests, and a blocking human verification gate.
</objective>

<execution_context>
@C:/Users/16079/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md
@.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md
@.planning/phases/16-audio-contracts-tts-provider-shell/16-UAT.md

<interfaces>
Phase 17 depends on Phase 16's audio-provider shell, Piper adapter, typed provider health, and non-silent failure semantics. If Phase 16 file names differ from its plans, preserve the executed Phase 16 API rather than creating parallel contracts.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Confirm Phase 16 manual UAT approval before Phase 17 execution</name>
  <files>.planning/STATE.md, .planning/phases/16-audio-contracts-tts-provider-shell/16-UAT.md</files>
  <read_first>
    - `.planning/STATE.md` current position/status lines showing whether Phase 16 manual UAT is still pending
    - `.planning/phases/16-audio-contracts-tts-provider-shell/16-UAT.md` if present
  </read_first>
  <action>Before modifying code, verify Phase 16 manual UAT is already approved/complete. This checkpoint is a blocking precondition only; do not perform or complete Phase 16 UAT here. If STATE.md still says `human_verification_required`, `awaiting manual UAT`, or equivalent, stop Phase 17 execution and report that Phase 16 UAT must be approved first.</action>
  <acceptance_criteria>Phase 17 proceeds only when Phase 16 UAT evidence or STATE.md confirms manual approval/completion.</acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$s=Get-Content -LiteralPath '.planning/STATE.md' -Raw; if ($s -match 'human_verification_required|awaiting manual UAT|manual UAT pending') { exit 1 } else { exit 0 }"</automated>
  </verify>
  <what-built>No Phase 17 code is built until Phase 16 UAT is complete.</what-built>
  <how-to-verify>Confirm `.planning/STATE.md` no longer reports Phase 16 manual UAT pending, or that `16-UAT.md` records approval.</how-to-verify>
  <resume-signal>Type "approved" only after Phase 16 manual UAT is complete.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend Python contracts for GPT-SoVITS, presets, reference audio, and test audio</name>
  <files>packages/contracts/py/contracts/audio_provider.py, packages/contracts/py/contracts/voice_preset.py, packages/contracts/py/contracts/audio_payload.py, packages/contracts/py/contracts/__init__.py, packages/contracts/scripts/codegen.py, packages/contracts/tests/test_codegen.py</files>
  <read_first>
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-CONTEXT.md` decisions D-01 through D-16
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-RESEARCH.md` `Open Questions (RESOLVED)`, GPT-SoVITS API fields, and Validation Architecture
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-VALIDATION.md` Wave 0 requirements
    - `.planning/phases/17-gpt-sovits-provider-voice-presets/17-PATTERNS.md` contract and audio failure patterns
    - `packages/contracts/py/contracts/audio_provider.py`
    - `packages/contracts/py/contracts/audio_payload.py`
  </read_first>
  <behavior>
    - Test 1: GPT-SoVITS provider config accepts one `base_url`, optional user-command launch fields, explicit activation gates, and no separate endpoint URLs per D-01/D-03.
    - Test 2: `VoicePreset` stores provider-specific GPT-SoVITS knobs/reference text/language/reference audio id but not `base_url`, launch command, or credentials per D-07.
    - Test 3: `ReferenceAudioAsset` records generated id, sanitized managed filename/path token, display basename, duration/format metadata, transcript text, and language per D-13/D-15.
    - Test 4: Test synthesis response carries base64/audio metadata for preview, and `AudioPayloadMessage` can carry failed-audio metadata while preserving display text per D-09/D-11.
  </behavior>
  <action>Add/extend concrete Pydantic contracts: `GptSoVitsProviderConfig`, `GptSoVitsHealthRequest`, `GptSoVitsTestSynthesisRequest`, `GptSoVitsTestSynthesisResult`, `GptSoVitsPresetConfig`, `VoicePreset`, `ReferenceAudioAsset`, `ActivePresetAssociation`, and `VoicePresetLibrary`. Safe tuning defaults: `top_k=15`, `top_p=1.0`, `temperature=1.0`, `text_split_method='cut5'`, `batch_size=1`, `speed_factor=1.0`, `repetition_penalty=1.35`, `media_type='wav'`, `streaming_mode=False`. Do not add STT, mic, PTT/VAD, installer, training, cloning, or model-weight-switching UI fields.</action>
  <acceptance_criteria>
    - `grep -v '^#' packages/contracts/py/contracts/voice_preset.py | grep -c "class VoicePreset"` returns at least 1.
    - `grep -v '^#' packages/contracts/py/contracts/voice_preset.py | grep -c "base_url"` returns 0.
    - Contract tests cover invalid languages/provider ids, activation gate fields, and test synthesis audio result serialization.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest packages/contracts/tests/test_codegen.py -q</automated>
  </verify>
  <done>Contracts encode Phase 17 provider, preset, reference-audio, test-synthesis, and failed-audio data without scope creep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate cross-language contract outputs</name>
  <files>packages/contracts/ts/audio-provider.ts, packages/contracts/ts/voice-preset.ts, packages/contracts/ts/audio-payload.ts, packages/contracts/ts/index.ts, packages/contracts/generated/json-schema/audio-provider.schema.json, packages/contracts/generated/json-schema/voice-preset.schema.json, packages/contracts/generated/json-schema/audio-payload.schema.json, packages/contracts/tests/test_codegen_drift.py</files>
  <read_first>
    - `packages/contracts/scripts/codegen.py`
    - `packages/contracts/tests/test_codegen_drift.py`
    - `packages/contracts/py/contracts/audio_provider.py`
    - `packages/contracts/py/contracts/voice_preset.py`
    - `packages/contracts/py/contracts/audio_payload.py`
  </read_first>
  <action>Register `voice-preset` and any new/changed audio-provider/audio-payload contract targets, regenerate TypeScript and JSON Schema, and export all new types through `packages/contracts/ts/index.ts`.</action>
  <acceptance_criteria>
    - `packages/contracts/ts/voice-preset.ts` exports `VoicePreset` and `ReferenceAudioAsset`.
    - `packages/contracts/ts/audio-provider.ts` exports GPT-SoVITS health/test synthesis request and response types.
    - `npm run check:contracts` reports no generated drift.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:contracts</automated>
  </verify>
  <done>Electron and renderer can use generated Phase 17 contracts without hand-written type drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted config→sidecar/provider | Contract data later drives local process launch, external HTTP calls, and synthesis. |
| sidecar audio payload→renderer | Failed provider metadata and test audio cross to renderer UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T/I | `voice_preset.py` | mitigate | Presets exclude provider connection fields; base URL/launch config remain provider-level per D-07. |
| T-17-02 | T/D | activation gate fields | mitigate | GPT-SoVITS can be active only after health and user-triggered test synthesis pass per D-03. |
| T-17-03 | I | failed/test audio contracts | mitigate | Contracts support concise failure summaries and redacted diagnostics; raw provider output is not required in main UI. |
</threat_model>

<verification>
Run after all tasks:

```powershell
npm run check:contracts
```
</verification>

<success_criteria>
- Phase 17 cannot execute before Phase 16 manual UAT is approved.
- All Phase 17 data surfaces have generated Python/TS contracts.
- Contracts honor resolved research questions and decisions D-01 through D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gpt-sovits-provider-voice-presets/17-01-SUMMARY.md`.
</output>
