---
phase: 07-three-category-code-parsing-dispatch
plan: 07
type: execute
wave: 4
depends_on:
  - 07-04
  - 07-06
files_modified:
  - sidecar/src/sidecar/ws/server.py
  - sidecar/src/sidecar/orchestrator/orchestrator.py
  - sidecar/src/sidecar/orchestrator/transformers.py
  - sidecar/tests/test_sidecar_boot.py
  - sidecar/tests/orchestrator/test_dispatch_routing.py
  - apps/renderer/src/chrome/LogsDrawer.tsx
  - apps/renderer/src/lib/copy.ts
  - apps/renderer/tests/logs-drawer-intent.test.tsx
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - PARSE-06
  - PARSE-07
must_haves:
  truths:
    - "Boot validation runs before VTS connection and blocks reserved names or collisions."
    - "Orchestrator constructs code_extractor from active plugin action codes and avatar catalogs."
    - "Boot wires PluginAdapter, VariantStateManager, EventCompletionTracker, and DiscreteDispatcher into Orchestrator."
    - "Renderer log highlighting recognizes [DISPATCH], not [INTENT]."
    - "All new VTS hotkey paths flow through DiscreteDispatcher/PyvtsSafeWriter."
  artifacts:
    - path: "sidecar/src/sidecar/ws/server.py"
      provides: "boot validation and runtime manager wiring"
      contains: "validate_reserved_names"
    - path: "apps/renderer/src/chrome/LogsDrawer.tsx"
      provides: "[DISPATCH] log highlighting"
      contains: "[DISPATCH]"
  key_links:
    - from: "sidecar/src/sidecar/ws/server.py"
      to: "sidecar/src/sidecar/parser/reserved.py"
      via: "validate_reserved_names before connect/authenticate"
      pattern: "validate_reserved_names"
    - from: "sidecar/src/sidecar/ws/server.py"
      to: "sidecar/src/sidecar/orchestrator/orchestrator.py"
      via: "passes plugin_adapter and VTS managers"
      pattern: "plugin_adapter"
---

<objective>
Wire Phase 7 dispatch into sidecar boot and renderer log presentation.

Purpose: This is the integration plan that connects the smaller parser,
validation, plugin-delivery, and VTS-manager plans into the running sidecar.

Output: Boot validation order, runtime manager construction, orchestrator parser
construction, shutdown cleanup, renderer `[DISPATCH]` log prefix, and tests.
</objective>

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md
@.planning/phases/06-plugin-runtime-default-plugin/06-02-PLAN.md
@.planning/phases/08-avatar-import-catalogs/08-02-SUMMARY.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-04-PLAN.md
@.planning/phases/07-three-category-code-parsing-dispatch/07-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire boot validation, catalog lookups, baseline reset, and shutdown cleanup</name>
  <files>
    sidecar/src/sidecar/ws/server.py,
    sidecar/src/sidecar/orchestrator/orchestrator.py,
    sidecar/src/sidecar/orchestrator/transformers.py,
    sidecar/tests/test_sidecar_boot.py,
    sidecar/tests/orchestrator/test_dispatch_routing.py
  </files>
  <read_first>
    - .planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md (D-D1, D-D4, and boot integration points)
    - .planning/phases/07-three-category-code-parsing-dispatch/07-RESEARCH.md (boot integration and reset hotkey lookup)
    - sidecar/src/sidecar/ws/server.py
    - sidecar/src/sidecar/avatar/overrides.py
    - sidecar/src/sidecar/avatar/rig_capabilities.py
    - sidecar/src/sidecar/parser/reserved.py
    - sidecar/src/sidecar/vts/variant_state_manager.py
    - sidecar/src/sidecar/vts/event_completion_tracker.py
  </read_first>
  <behavior>
    - Test 1: boot with plugin action code `joy` and variant code `joy` raises before `connect_and_authenticate`.
    - Test 2: boot with event code `think` raises `ReservedNameError` before `connect_and_authenticate`.
    - Test 3: clean boot constructs `VariantStateManager`, `EventCompletionTracker`, and calls `reset_to_baseline()` after writer handshake.
    - Test 4: `code_extractor` is constructed from plugin action codes, `overrides.variants`, and `overrides.events`.
    - Test 5: shutdown calls `EventCompletionTracker.close()` before `writer.close()`.
  </behavior>
  <action>
Update `sidecar/src/sidecar/ws/server.py` after Phase 6 boot rewiring is
present. Immediately after loading overrides, capabilities, and the active
plugin manifest, call:
```python
validate_reserved_names(
    plugin_action_codes={getattr(action, "code", action) for action in plugin_manifest.action_codes},
    variants=overrides.variants,
    events=overrides.events,
)
```
This call must occur before any VTS `connect_and_authenticate` scheduling.

Find the reset-all hotkey after capabilities load with:
```python
reset_hotkey = next(
    (
        hotkey
        for hotkey in capabilities.hotkeys
        if hotkey.type == "RemoveAllExpressions" or hotkey.name == "RemoveAllExpressions"
    ),
    None,
)
```

After constructing `DiscreteDispatcher(writer)`, construct
`VariantStateManager(discrete_dispatcher, reset_hotkey_id=reset_hotkey.hotkey_id
if reset_hotkey else None)` and `EventCompletionTracker()`.

After writer handshake succeeds and before compositor starts, call
`await variant_state_manager.reset_to_baseline()`. If VTS is unavailable or
baseline reset fails, log and continue with no active variant state; do not
block sidecar READY on baseline reset failure.

Pass `plugin_adapter`, `variant_state_manager`, `discrete_dispatcher`, and
`event_completion_tracker` to `Orchestrator(...)`. Store them on `app.state`
along with `action_code_queue` if one remains for compatibility.

Ensure Orchestrator builds `code_extractor` with
`plugin_action_codes`, `avatar_overrides.variants`, and
`avatar_overrides.events`. If Orchestrator does not already store these, add
constructor args `plugin_action_codes: set[str]` and
`avatar_overrides: AvatarOverrides`.

During FastAPI lifespan shutdown, call `await event_completion_tracker.close()`
before `writer.close()`.

Do not import `pyvts` in any new file. Do not introduce a second class
that wraps `PyvtsSafeWriter` and owns its own VTS plugin identity
(`plugin_name` kwarg) — 06-07 deleted the only such case
(`PyVTSParameterWriter`); reintroducing one violates ARCH-06 even if it
satisfies the legacy `import pyvts` grep. Use the existing single
`PyvtsSafeWriter` for variant + event fires.
  </action>
  <verify>
    <automated>cd sidecar && uv run pytest tests/test_sidecar_boot.py tests/orchestrator/test_dispatch_routing.py -x --no-header</automated>
    <automated>cd sidecar && uv run pytest tests/test_arch06_single_writer.py -x --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `sidecar/src/sidecar/ws/server.py` contains `validate_reserved_names`.
    - `sidecar/src/sidecar/ws/server.py` contains `VariantStateManager`.
    - `sidecar/src/sidecar/ws/server.py` contains `EventCompletionTracker`.
    - `sidecar/src/sidecar/ws/server.py` contains `reset_to_baseline`.
    - `sidecar/src/sidecar/ws/server.py` contains `event_completion_tracker.close`.
    - `sidecar/src/sidecar/orchestrator/orchestrator.py` contains `code_extractor`.
    - `sidecar/src/sidecar/orchestrator/orchestrator.py` contains `plugin_action_codes`.
    - `sidecar/src/sidecar/orchestrator/orchestrator.py` contains `avatar_overrides`.
    - `sidecar/tests/test_sidecar_boot.py` contains `test_collision_raises_before_vts_connect`.
    - `cd sidecar && uv run pytest tests/test_sidecar_boot.py tests/orchestrator/test_dispatch_routing.py -x --no-header` exits 0.
    - `cd sidecar && uv run pytest tests/test_arch06_single_writer.py -x --no-header` exits 0 (ARCH-06 single-writer guard from 06-07 still passes — no new `PyvtsSafeWriter` wrapper, no new `plugin_name` site, no new `requestSetParameterValue` / `requestInjectParameterData` callsite outside `pyvts_writer.py`).
  </acceptance_criteria>
  <done>Sidecar boot validates vocabularies, constructs runtime managers, resets variant baseline, and shuts event tracker tasks down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update renderer log prefix from INTENT to DISPATCH</name>
  <files>
    apps/renderer/src/chrome/LogsDrawer.tsx,
    apps/renderer/src/lib/copy.ts,
    apps/renderer/tests/logs-drawer-intent.test.tsx
  </files>
  <read_first>
    - .planning/phases/07-three-category-code-parsing-dispatch/07-CONTEXT.md (Specifics: [INTENT] becomes [DISPATCH])
    - apps/renderer/src/chrome/LogsDrawer.tsx
    - apps/renderer/src/lib/copy.ts
    - apps/renderer/tests/logs-drawer-intent.test.tsx
  </read_first>
  <behavior>
    - Test 1: renderer LogsDrawer highlights `[DISPATCH] kind=action name=joy`.
    - Test 2: renderer no longer special-cases `[INTENT]`.
  </behavior>
  <action>
In `apps/renderer/src/lib/copy.ts`, replace `INTENT_PREFIX: '[INTENT]'` with
`DISPATCH_PREFIX: '[DISPATCH]'`.

In `apps/renderer/src/chrome/LogsDrawer.tsx`, replace any
`line.startsWith('[INTENT]')` branch with `line.startsWith('[DISPATCH]')`, and
render the `[DISPATCH]` prefix in `var(--success)`.

Update `apps/renderer/tests/logs-drawer-intent.test.tsx` data and assertions so
the sample log line is `[DISPATCH] kind=action name=joy`. The file may keep its
old filename if renaming would create extra churn; assertions must no longer
expect `[INTENT]`.
  </action>
  <verify>
    <automated>cd apps/renderer && npm test -- --run logs-drawer-intent</automated>
  </verify>
  <acceptance_criteria>
    - `apps/renderer/src/chrome/LogsDrawer.tsx` contains `[DISPATCH]`.
    - `apps/renderer/src/chrome/LogsDrawer.tsx` does not contain `[INTENT]`.
    - `apps/renderer/src/lib/copy.ts` contains `DISPATCH_PREFIX`.
    - `apps/renderer/tests/logs-drawer-intent.test.tsx` contains `[DISPATCH] kind=action name=joy`.
    - `cd apps/renderer && npm test -- --run logs-drawer-intent` exits 0.
  </acceptance_criteria>
  <done>Renderer log highlighting recognizes `[DISPATCH]` and no longer treats `[INTENT]` as the Phase 7 prefix.</done>
</task>

</tasks>

<verification>
Run from repo root:
```powershell
cd sidecar
uv run pytest tests/test_sidecar_boot.py tests/orchestrator/test_dispatch_routing.py -q
cd ..
cd apps/renderer
npm test -- --run logs-drawer-intent
```
</verification>

<success_criteria>
- Boot validation blocks reserved names and cross-category collisions before VTS connection.
- Runtime managers are constructed and passed into Orchestrator.
- Event tracker cleanup runs during shutdown.
- Renderer recognizes `[DISPATCH]` logs.
- `cd sidecar && uv run pytest tests/test_arch06_single_writer.py` still passes — ARCH-06 single-writer rule preserved (06-07 hardened guard, supersedes the legacy `rg 'import pyvts'` count check).
</success_criteria>

<output>
After completion, create `.planning/phases/07-three-category-code-parsing-dispatch/07-07-SUMMARY.md`.
</output>
