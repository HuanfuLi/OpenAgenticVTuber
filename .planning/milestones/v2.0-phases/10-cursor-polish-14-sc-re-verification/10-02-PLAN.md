---
phase: 10-cursor-polish-14-sc-re-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/skeleton-verification.md
  - .planning/baselines/v2.0/lipsync-phase10-replay.json
  - .planning/baselines/v2.0/idle-phase10-replay.json
autonomous: false
requirements:
  - VFY-03
  - VFY-04
  - VFY-05
must_haves:
  truths:
    - "skeleton-verification.md exists with the four sections required by D-D1 (§14 SC Verdicts, Automated Baseline Replay, v2.0 Surfaces Verified in Their Own Phases, Milestone v2.0 Close Decision)"
    - "All six §14 SC entries are present in the verdict table with PASS / PARTIAL / FAIL recorded plus concrete observation"
    - "Plumbing harness replay produces JSON files for both lipsync (pearson_r >= 0.7) and idle (0 < variance_sum < 0.5) modes, committed at .planning/baselines/v2.0/{lipsync,idle}-phase10-replay.json"
    - "Operator ceremony script captures the SC #2 [smirk] + SC #4 long-utterance prompts, visual checklists, and verdict rubric per D-B3 / D-B4 / D-B5"
    - "SC #6 (OLVT WS shape) records PASS with the 'M1 Phase 1+2 verified, v2.0 surfaces extend without changing envelope' rationale per RESEARCH.md SC #6 paragraph"
    - "SC #5 (cursor) verdict is recorded based on Plan 10-01's outcome plus operator visual confirmation of in-window + outside-window tracking"
    - "The v2.0 milestone close decision (PASS / PARTIAL / FAIL) is recorded with explicit decision basis"
  artifacts:
    - path: ".planning/skeleton-verification.md"
      provides: "Phase 10 skeleton-verification deliverable; closes M1 SC-01 deferred from Phase 5"
      min_lines: 80
      contains: "Milestone v2.0 Close Decision"
    - path: ".planning/baselines/v2.0/lipsync-phase10-replay.json"
      provides: "Phase 10 harness replay output for SC #1 lipsync"
      contains: "pearson_r"
    - path: ".planning/baselines/v2.0/idle-phase10-replay.json"
      provides: "Phase 10 harness replay output for SC #3 idle"
      contains: "variance_sum"
  key_links:
    - from: ".planning/skeleton-verification.md"
      to: ".planning/baselines/v2.0/lipsync-phase10-replay.json"
      via: "Automated Baseline Replay table cites the replay JSON path with timestamp"
      pattern: "lipsync-phase10-replay.json"
    - from: ".planning/skeleton-verification.md"
      to: ".planning/baselines/v2.0/idle-phase10-replay.json"
      via: "Automated Baseline Replay table cites the replay JSON path with timestamp"
      pattern: "idle-phase10-replay.json"
    - from: ".planning/skeleton-verification.md"
      to: ".planning/phases/06-plugin-runtime-default-plugin/06-VERIFICATION.md"
      via: "v2.0 Surfaces Verified cross-reference table per D-C2"
      pattern: "06-VERIFICATION.md"
    - from: ".planning/skeleton-verification.md"
      to: ".planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md"
      via: "SC #5 verdict cites the cursor-fix evidence"
      pattern: "10-01-SUMMARY.md"
---

<objective>
Run the §14 SC re-verification ceremony against the v2.0 refactored architecture and commit `.planning/skeleton-verification.md` as the deferred milestone-1 SC-01 deliverable. Implements VFY-03 (re-run all six §14 SCs), VFY-04 (skeleton-verification.md commit), and VFY-05 (side-by-side baseline harness replay against Phase 6 baselines for SC #1 lipsync + SC #3 idle).

Purpose: Close milestone v2.0 by recording PASS / PARTIAL / FAIL verdicts for all six §14 SCs with concrete observations. Operator ceremony covers SC #2 (`[smirk]` smooth blend, replacing M1's removed `[joy]`), SC #4 (body sway through long utterance), and SC #5 (cursor — verified working post-Plan 10-01). Automated harness replay covers SC #1 + SC #3. SC #6 (OLVT WS shape) records PASS with bookkeeping rationale.

Output: `skeleton-verification.md` committed with the four-section structure locked by D-D1; two replay JSON files committed alongside the M1 baselines (preserving M1 baselines as immutable historical reference per Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/10-CONTEXT.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/10-RESEARCH.md
@.planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md
@.planning/baselines/v2.0/lipsync.json
@.planning/baselines/v2.0/idle.json
@sidecar/scripts/plumbing_harness.py
@plugins/default/plugin.yaml
@avatars/重音テト/_avatar_overrides.yaml
@.planning/phases/06-plugin-runtime-default-plugin/06-VERIFICATION.md
@.planning/phases/06-plugin-runtime-default-plugin/06-HUMAN-UAT.md
@.planning/phases/09-slider-hud-per-param-lock/09-VERIFICATION.md
@.planning/phases/09-slider-hud-per-param-lock/09-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do NOT explore the codebase to find these. -->

From sidecar/scripts/plumbing_harness.py (existing — runs as a CLI, replayed verbatim):
```
CLI: uv run python scripts/plumbing_harness.py --mode {lipsync|idle} --out PATH
Exit code: 0 on PASS, 1 on FAIL.
Output JSON shape (lipsync mode):
  {"mode": "lipsync", "pearson_r": float, "threshold": 0.7, "passed": bool, "sample_count": 90}
Output JSON shape (idle mode):
  {"mode": "idle", "variance_sum": float, "variance_ceiling": 0.5, "passed": bool, "duration_seconds": 30.0}
The harness DOES NOT spawn a sidecar and DOES NOT need VTS running.
```

Phase 6 baselines (committed, MUST stay immutable per Pitfall 5):
- `.planning/baselines/v2.0/lipsync.json` — pearson_r=0.9747730195034283, threshold=0.7, passed=true
- `.planning/baselines/v2.0/idle.json`   — variance_sum=0.06643749130899018, variance_ceiling=0.5, passed=true

Phase 10 replay outputs (created by this plan, NEW files):
- `.planning/baselines/v2.0/lipsync-phase10-replay.json`
- `.planning/baselines/v2.0/idle-phase10-replay.json`

§14 SC ↔ Verdict-source map (the table that fills skeleton-verification.md §1):
| # | Criterion | Verdict source |
|---|-----------|----------------|
| 1 | Lipsync RMS-vs-MouthOpen | `lipsync-phase10-replay.json` (passed: true) → PASS |
| 2 | `[smirk]` smooth blend (M1's `[joy]` is obsolete per 06-08) | Operator visual review per D-B3 / D-B5 |
| 3 | Idle micro-motion non-zero variance | `idle-phase10-replay.json` (0 < variance_sum < 0.5) → PASS |
| 4 | Body sway through ~30s utterance | Operator visual review per D-B4 / D-B5 |
| 5 | Cursor tracking | Plan 10-01 outcome + operator visual confirmation |
| 6 | OLVT WS protocol shape | Bookkeeping — PASS with rationale, no re-test |

Active rig + plugin state (verified in RESEARCH.md):
- Active plugin: `plugins/default/plugin.yaml` declares `[smirk]` in vocabulary (line 17-18, action_codes section)
- Active avatar: `avatars/重音テト/_avatar_overrides.yaml` — body_sway_strategy: head_only, 14 variants registered, events: []
- Active rig: Live2D Cubism 4 (Teto rig at `Live2D/重音テト/`)
</interfaces>

<scs_already_judged>
- **SC #6 (OLVT WS protocol shape) — PASS by bookkeeping.** Rationale, locked by RESEARCH.md `### SC #6 (WS protocol shape) — bookkeeping rationale`:
  > PASS — verified M1 Phase 1+2 (PLUMB-03), v2.0 surfaces extend the envelope without changing OLVT shape. Phase 1 PLUMB-03 closed the OLVT-shape WS envelope (TextInput / DisplayText / Shutdown discriminated union, Pydantic source-of-truth, hand-written TS mirror at the time, codegen-generated since Phase 5). Phase 7 added `Dispatch` (ActionCode | VariantToggle | EventFire) and Phase 9 added `HudMessageS2C` / `HudMessageC2S` — both are new `type` values inside the existing envelope, not changes to envelope structure.
  No new test, no operator step. Recorded in skeleton-verification.md §1 SC #6 row as PASS with this paragraph as the observation.

- **SC #1 (lipsync) and SC #3 (idle) — PASS by harness replay**, contingent on Task 2 (harness exit 0 + JSON `passed: true`). Already PASSED at 1.39× / 7.5× margin during Phase 6 06-02; replay is overdetermined but committed for v2.0 close.
</scs_already_judged>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the §14 SC ceremony script + skeleton-verification.md skeleton (autonomous markdown deliverable)</name>

  <files>
    - .planning/skeleton-verification.md (CREATE - 4-section deliverable per D-D1)
  </files>

  <read_first>
    - .planning/phases/10-cursor-polish-14-sc-re-verification/10-CONTEXT.md (D-A1..D-D2 — all 14 locked decisions)
    - .planning/phases/10-cursor-polish-14-sc-re-verification/10-RESEARCH.md (`## skeleton-verification.md Structure` section + `## Operator Ceremony Script` section — copy verbatim where templates are provided)
    - .planning/phases/06-plugin-runtime-default-plugin/06-HUMAN-UAT.md (operator UAT report shape — frontmatter + per-test-block + verdict slot)
    - plugins/default/plugin.yaml (confirm `smirk` is in active vocabulary before referring to it in the ceremony)
    - avatars/重音テト/_avatar_overrides.yaml (confirm body_sway_strategy + variants list — ceremony preconditions)
    - .planning/baselines/v2.0/lipsync.json (immutable M1 baseline — referenced for delta in §2)
    - .planning/baselines/v2.0/idle.json (immutable M1 baseline — referenced for delta in §2)
    - .planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md (Plan 10-01 outcome — referenced as SC #5 evidence link)
  </read_first>

  <action>
    Create `.planning/skeleton-verification.md` with the exact four-section structure locked by D-D1. Use the markdown template below — fill placeholders that are determinable now (active rig, requirement IDs, baseline values, harness commands, cross-reference table, milestone close criteria); leave operator-judged verdicts as `___PENDING___` slots that Task 3 fills during the live ceremony.

    **File path:** `.planning/skeleton-verification.md` (NEW; does not yet exist)

    **Exact content to write:**

    ```markdown
    # §14 Walking Skeleton Verification — v2.0 Refactored Architecture

    **Verified:** ___PENDING___ (filled by operator at ceremony time)
    **Milestone:** v2.0 Plugin + Animation Control
    **Architecture:** Refactored — plugin runtime (Phase 6) + dispatch (Phase 7) + import (Phase 8) + HUD (Phase 9) + cursor namespace fix (Phase 10)
    **Verifier:** ___PENDING___ (operator name)
    **Active rig:** Teto (Cubism 4, 重音テト)
    **Active plugin:** plugins/default (action vocabulary: anger, disgust, fear, neutral, sadness, smirk, surprise)
    **Origin:** This deliverable closes the M1 SC-01 ceremony deferred from Phase 5 on 2026-05-08; lands here per the Phase 10 mandate (REQUIREMENTS.md VFY-03 / VFY-04).

    ---

    ## §14 Success Criteria Verdicts (M1 re-run ceremony)

    Phase 10 re-runs all six §14 success criteria from `PROJECT_DESIGN.md §14` against the v2.0 refactored architecture. SC #2 (`[smirk]` smooth blend, replacing M1's removed `[joy]` per 06-08) and SC #4 (body sway through full utterance) are operator-judged via the ceremony scripts below per Phase 6 discuss-phase decision (head_only ship state acknowledged as mediocre; auto-diff against M1 visual baseline would punish improved future implementations). SC #1 (lipsync) and SC #3 (idle) are verified by automated harness replay against the Phase 6 06-02 baselines (preserved as immutable historical reference). SC #5 (cursor) verdict comes from Phase 10 Plan 10-01 outcome plus operator visual confirmation. SC #6 (OLVT WS shape) records PASS with bookkeeping rationale.

    | # | Criterion | Verdict | Observation | Evidence |
    |---|-----------|---------|-------------|----------|
    | 1 | Lipsync RMS-vs-MouthOpen tracking (TTS-04 / AVT-03 carry-through) | ___PENDING___ | pearson_r = ___PENDING___ from Phase 10 replay | `.planning/baselines/v2.0/lipsync-phase10-replay.json` |
    | 2 | `[smirk]` smooth blend (operator-judged; replaces M1 `[joy]` per 06-08) | ___PENDING___ | ___PENDING___ checklist score (see ceremony §SC #2 below) | This file, §"SC #2 Ceremony Log" |
    | 3 | Idle micro-motion non-zero variance (AVT-02 carry-through) | ___PENDING___ | variance_sum = ___PENDING___ from Phase 10 replay | `.planning/baselines/v2.0/idle-phase10-replay.json` |
    | 4 | Body sway through full utterance (operator-judged) | ___PENDING___ | ___PENDING___ checklist score (see ceremony §SC #4 below) | This file, §"SC #4 Ceremony Log" |
    | 5 | Cursor tracking (operator-judged post-Plan 10-01 fix) | ___PENDING___ | Cursor namespace fix landed; in-window + outside-window tracking observed: ___PENDING___ | `.planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md` + this file §"SC #5 Ceremony Log" |
    | 6 | OLVT WS protocol shape (bookkeeping) | PASS | M1 Phase 1+2 (PLUMB-03) verified the OLVT-shape envelope; v2.0 Phase 7 `Dispatch` and Phase 9 `HudMessageS2C/C2S` are new `type` values inside the existing envelope, not envelope-structure changes. | `.planning/phases/01-plumbing-process-lifecycle/01-02-SUMMARY.md`, `.planning/phases/02-conversation-pipeline/`, `.planning/phases/07-three-category-code-parsing-dispatch/`, `.planning/phases/09-slider-hud-per-param-lock/` |

    ### SC #1 Ceremony Log (automated)

    Phase 10 harness replay (Task 2 in Plan 10-02) — re-runs `sidecar/scripts/plumbing_harness.py --mode lipsync` against the synthetic envelope used in Phase 6 06-02. Threshold: `pearson_r >= 0.7`. M1 baseline at 06-02 produced `pearson_r=0.9747730195034283` (1.39× over threshold). Tolerance bands ±100ms / ±0.05 per D-E1 are wider than the Phase 6 margin (0.27); replay-time PASS is overdetermined.

    **Replay command:**
    ```bash
    cd sidecar
    uv run python scripts/plumbing_harness.py --mode lipsync --out ../.planning/baselines/v2.0/lipsync-phase10-replay.json
    ```

    **Replay verdict:** ___PENDING___ (auto-fills from JSON `passed` field after Task 2)

    ### SC #2 Ceremony Log (operator-judged — `[smirk]` smooth blend)

    **Precondition checks** (operator confirms before recording verdict):
    - [ ] Separate HUD window closed; if it was opened for setup smoke, any locks have been cleared first (per "Lock means lock" carryover)
    - [ ] LM Studio running (or active provider passes /admin/llm-test)
    - [ ] VTube Studio running with Teto rig loaded
    - [ ] Sidecar [READY] line visible in logs drawer
    - [ ] App is on the chat panel (not Settings, not Avatar Import)

    **Steps:**
    1. Open the app to the chat panel.
    2. Paste the prompt verbatim:
       > Reply with exactly one sentence about a cat sneaking into a bakery, and emit `[smirk]` somewhere in your reply.
    3. Send. Observe the avatar's face during the reply.
    4. Open the logs drawer; confirm a `[DISPATCH]` line for `ActionCode(code='smirk')` appears within the reply window. (Per Pitfall 4: if absent, the LLM did not honor the prompt — retry with a more directive prompt or switch model. Without `[DISPATCH]`, this ceremony cannot judge the compositor + plugin path.)

    **Visual checklist (per D-B3):**
    - [ ] (1) Expression entry is gradual (faded in), not a hotkey-style pop.
    - [ ] (2) Full fade-in process is visible from `[smirk]` token trigger to end of sentence.
    - [ ] (3) After sentence completion, expression decays gradually back, not abruptly cut.

    **Verdict (per D-B5):**
    - 3/3 → PASS
    - 1-2/3 → PARTIAL
    - 0/3 → FAIL

    **Recorded verdict:** ___PENDING___
    **Recorded observation:** ___PENDING___
    **Evidence link:** ___PENDING___ (chat-log timestamp range or screen-capture clip path)

    ### SC #3 Ceremony Log (automated)

    Phase 10 harness replay — re-runs `sidecar/scripts/plumbing_harness.py --mode idle` against IdleDriver(seed=42) for 30 seconds at 30 Hz. Ceiling: `0 < variance_sum < 0.5`. M1 baseline at 06-02 produced `variance_sum=0.06643749130899018` (7.5× under ceiling).

    **Replay command:**
    ```bash
    cd sidecar
    uv run python scripts/plumbing_harness.py --mode idle --out ../.planning/baselines/v2.0/idle-phase10-replay.json
    ```

    **Replay verdict:** ___PENDING___ (auto-fills from JSON `passed` field after Task 2)

    ### SC #4 Ceremony Log (operator-judged — body sway through ~30-45s utterance)

    **Precondition checks** (same as SC #2; HUD-closed precondition is critical here too):
    - [ ] Separate HUD window closed; if it was opened for setup smoke, any locks have been cleared first
    - [ ] LM Studio running
    - [ ] VTube Studio running with Teto rig loaded
    - [ ] Sidecar [READY] line visible in logs drawer
    - [ ] App is on the chat panel

    **Steps:**
    1. Open the app to the chat panel.
    2. Paste the prompt verbatim:
       > Tell me a 60-word funny story about a cat who somehow ends up in space. Take your time and include vivid details.
    3. Send. Observe the avatar's body/head sway through the entire reply (~30-45 seconds of TTS audio expected).
    4. After the reply finishes, open the logs drawer and confirm `[SPEECH-DRIVER]` and `[BODY-SWAY]` log lines were emitted during the utterance window — confirms the body-sway path was active.

    **Visual checklist (per D-B4):**
    - [ ] (1) Visible body motion (not flat/static through the utterance).
    - [ ] (2) Through ~30 seconds, motion does not freeze on one side or get stuck centered.
    - [ ] (3) Sway feels coherent with TTS rhythm — not jarringly out of sync.

    **Verdict (per D-B5):** Same rubric as SC #2.

    **Recorded verdict:** ___PENDING___
    **Recorded observation:** ___PENDING___
    **Evidence link:** ___PENDING___

    ### SC #5 Ceremony Log (operator-judged post-Plan 10-01 fix)

    **Plan 10-01 outcome:** Cursor namespace fix landed. Root cause: `cursor_driver.py` returned Cubism input names (`ParamAngleX/Y`, `ParamEyeBallX/Y`) instead of VTS tracking-input names (`FaceAngleX/Y`, `EyeLeftX`, `EyeRightY`). Translation via `resolve_param_id(name, "vts")` now applied at driver edge. Drop-in-VTS-window gate removed. Synthetic-canvas fallback (primary monitor) added when no VTS rect detected. Regression test `test_cursor_driver_namespace.py` in CI prevents future regressions. ARCH-06 single-writer rule preserved (1 file imports pyvts). See `.planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md`.

    **Precondition checks:**
    - [ ] Plan 10-01 SUMMARY committed; cursor namespace regression test passes in CI
    - [ ] VTube Studio running with Teto rig loaded; sidecar authenticated
    - [ ] Sidecar [READY] line visible in logs drawer

    **Steps:**
    1. With cursor over the VTube Studio window, move the cursor in a slow circle around the avatar's face. Observe head + eyes tracking.
    2. Move cursor outside the VTS window (still on primary monitor — e.g., over the chat panel or another app). Observe head + eyes continuing to track (synthetic-canvas fallback per VFY-02).
    3. Stop moving the cursor at a point off-axis from face center. Observe head/eyes remain at the deflected pose (steady-state — no jitter).
    4. (Optional) Close VTube Studio entirely. Observe sidecar logs do not error; cursor continues to be polled (synthetic-canvas mode is harmless when no rig is rendered).

    **Visual checklist:**
    - [ ] (1) Cursor over VTS window: head + eye tracking visible.
    - [ ] (2) Cursor outside VTS window (on primary monitor): head + eye tracking still visible (synthetic-canvas fallback).
    - [ ] (3) Steady-state hold: no head twitches when cursor is stationary.

    **Verdict:**
    - 3/3 → PASS
    - 1-2/3 → PARTIAL
    - 0/3 → FAIL (and the diagnosis paragraph from Plan 10-01 SUMMARY is the FAIL evidence per D-A6)

    **Recorded verdict:** ___PENDING___
    **Recorded observation:** ___PENDING___
    **Evidence link:** ___PENDING___ (screen-capture clip showing tracking, or sidecar log timestamps for `[CURSOR]` or `[COMPOSITOR]` evidence of writes)

    ### SC #6 Ceremony Log (bookkeeping — no re-test)

    **Verdict:** PASS — verified M1 Phase 1+2 (PLUMB-03), v2.0 surfaces extend the envelope without changing OLVT shape.

    **Observation:** Phase 1 PLUMB-03 closed the OLVT-shape WS envelope (TextInput / DisplayText / Shutdown discriminated union, Pydantic source-of-truth, hand-written TS mirror at the time, codegen-generated since Phase 5). Phase 7 added `Dispatch` (ActionCode | VariantToggle | EventFire) and Phase 9 added `HudMessageS2C` / `HudMessageC2S` — both are new `type` values inside the existing envelope, not changes to envelope structure. The `/hud/ws` endpoint introduced in Phase 9 is a separate WS surface that complements `/ws`; both honor the OLVT discriminated-union shape.

    **Evidence:** `.planning/phases/01-plumbing-process-lifecycle/01-02-SUMMARY.md` (PLUMB-03 closure), `.planning/phases/07-three-category-code-parsing-dispatch/` (Dispatch addition), `.planning/phases/09-slider-hud-per-param-lock/` (HudMessage addition).

    No automated re-test. SC #6 is recorded-only.

    ---

    ## Automated Baseline Replay (VFY-05)

    Phase 10 re-runs the plumbing harness built in Phase 6 06-02 against the same synthetic inputs. M1 baseline files at `.planning/baselines/v2.0/{lipsync,idle}.json` remain immutable (preserved as historical reference per Pitfall 5); replay outputs are written to `*-phase10-replay.json` filenames.

    Harness re-run on ___PENDING___ (date filled at ceremony time):

    | Mode | Phase 6 baseline (06-02, 2026-05-08) | Phase 10 replay | Tolerance | Pass? |
    |------|--------------------------------------|-----------------|-----------|-------|
    | lipsync | `pearson_r=0.9747730195034283` | `pearson_r=___PENDING___` | `>= 0.7` | ___PENDING___ |
    | idle | `variance_sum=0.06643749130899018` | `variance_sum=___PENDING___` | `0 < x < 0.5` | ___PENDING___ |

    Tolerance bands per D-E1: ±100ms latency / ±0.05 param values for SC #1 + SC #3 only. Both Phase 6 baselines exceed thresholds with substantial margin (lipsync 1.39× over; idle 7.5× under), so replay-time PASS is overdetermined — the harness's `passed: true` JSON field is the gate.

    Replay commands (locked):

    ```bash
    cd sidecar
    uv run python scripts/plumbing_harness.py --mode lipsync --out ../.planning/baselines/v2.0/lipsync-phase10-replay.json
    uv run python scripts/plumbing_harness.py --mode idle --out ../.planning/baselines/v2.0/idle-phase10-replay.json
    ```

    Replay JSON files committed at:
    - `.planning/baselines/v2.0/lipsync-phase10-replay.json`
    - `.planning/baselines/v2.0/idle-phase10-replay.json`

    What the harness does NOT cover (per RESEARCH.md `### What the harness does NOT cover`):
    - SC #2 (`[smirk]` smooth blend) — operator-judged per D-B1 / D-B3.
    - SC #4 (body sway through utterance) — operator-judged per D-B2 / D-B4.
    - SC #5 (cursor) — verdict from Plan 10-01 outcome + operator visual confirmation.
    - SC #6 (WS protocol shape) — bookkeeping rationale, not a runtime check.

    ---

    ## v2.0 Surfaces Verified in Their Own Phases (cross-reference, per D-C2)

    Cross-reference table — does NOT duplicate verdicts. Verdicts live in each phase's own VERIFICATION.md per D-C3; Phase 9's live HUD UAT approval is recorded in `09-02-SUMMARY.md` and summarized by `09-VERIFICATION.md`.

    | Surface | Phase | Verification artifact | Operator UAT artifact |
    |---------|-------|----------------------|----------------------|
    | Plugin runtime + default plugin (`[smirk]` action-code dispatch path; ARCH-01..12; PLG-01..10) | 6 | `.planning/phases/06-plugin-runtime-default-plugin/06-VERIFICATION.md` | `.planning/phases/06-plugin-runtime-default-plugin/06-HUMAN-UAT.md`, `.planning/phases/06-plugin-runtime-default-plugin/06-UAT.md` |
    | Three-category dispatch (`{variant}` radio-button + `<event>` motion auto-completion; PARSE-01..08) | 7 | `.planning/phases/07-three-category-code-parsing-dispatch/07-VERIFICATION.md` | `.planning/phases/07-three-category-code-parsing-dispatch/07-HUMAN-UAT.md` |
    | Avatar import + catalogs (`AvatarOverrides` schema; review screen; IMP-01..10) | 8 | `.planning/phases/08-avatar-import-catalogs/08-VERIFICATION.md` | `.planning/phases/08-avatar-import-catalogs/08-HUMAN-UAT.md` |
    | HUD slider + per-param lock (`/hud/ws`, `set-lock`, focused `hud_visible_param_ids`, system-primitive exclusion; HUD-01..08) | 9 | `.planning/phases/09-slider-hud-per-param-lock/09-VERIFICATION.md` | `.planning/phases/09-slider-hud-per-param-lock/09-02-SUMMARY.md` |

    ---

    ## Milestone v2.0 Close Decision (per D-D2)

    ### Ship verdict

    **Criteria:**
    - **PASS** if all six §14 SCs are PASS, OR PARTIAL with documented future-direction rationale.
    - **PARTIAL** if any SC is PARTIAL with no rationale, OR exactly one SC is FAIL with documented rationale.
    - **FAIL** if more than one SC is FAIL, OR any SC is FAIL with no rationale.

    **Recorded verdict:** ___PENDING___ (filled by operator at ceremony time)

    **Decision basis:** ___PENDING___ (operator's brief rationale — e.g., "All six SCs PASS post-Phase-10 cursor fix; SC #4 PASS at 2/3 with note that head_only ship state is acknowledged-mediocre and physics-chain investigation is deferred to next milestone")

    ### Open issues for next milestone

    - **DPI awareness** — cursor projection on high-DPI displays uses raw pixel coordinates; on 200% scaling the synthetic-canvas projection skews toward one corner (VFY-02 deferred this).
    - **Multi-monitor cursor robustness** — current synthetic-canvas fallback projects against primary monitor only; users on multi-monitor setups will see cursor tracking only when cursor is on primary screen.
    - **Native Cubism rendering integration** — VTube Studio + pyvts is the v1 rendering path; native Cubism (alongside pixi-live2d-display-advanced) is a v1.5 / v2 candidate per CLAUDE.md "What NOT to Use" + PROJECT_DESIGN.md §11.
    - **Body-sway physics-chain investigation** — head_only ship state acknowledged as mediocre per Phase 6 discuss-phase decision; AVT-06 R-OPEN-1 remains open. Future milestone may investigate `<model>.vtube.json` physics-chain proxy or `.exp3.json` body-pose modulation by RMS.
    - **Multi-avatar identity persistence** — v1-horizon HEADLINE value, NOT v2.0. Per-avatar episodic Chroma stores + shared user-facts bucket + RRF-hybrid retrieval + write-on-promotion are the next milestone's deliverable (REQUIREMENTS MEM-01..MEM-07).

    ### v1-horizon progress note

    The v2.0 milestone delivers the architectural foundation needed for v1-horizon multi-avatar identity persistence:

    - **Plugin-driven motion (Phase 6)** — different avatars can have different motion plugins or share defaults; `BodyMotionPlugin` ABC + `RigCapabilities` rig-introspection contract makes plugins rig-agnostic.
    - **Formalized three-category code system (Phase 7)** — avatars own `{variant}` and `<event>` codes via their `_avatar_overrides.yaml`; plugins own `[action]` codes via their `plugin.yaml`. Cross-category uniqueness enforced at boot.
    - **Avatar-import flow with curated catalogs (Phase 8)** — `_avatar_overrides.yaml` per avatar is the persistence anchor for variant + event + emotion-binding catalogs. Re-openable from Settings.
    - **HUD/lock primitives (Phase 9)** — focused 18-row Teto `hud_visible_param_ids` discovery + tuning surface for new rigs; non-blocking `/hud/ws` telemetry; session-only locks with system-primitive exclusion enforced at the merge layer.
    - **Cursor namespace fix (Phase 10)** — first-time-working cursor tracking unblocks the §14 SC #5 verdict.

    The v2.0 milestone does NOT yet ship multi-avatar episodic memory or the shared user-facts bucket. Single-avatar walking skeleton works end-to-end with the refactored architecture; per-avatar Chroma stores + RRF-hybrid retrieval + write-on-promotion are the next milestone's headline value (REQUIREMENTS MEM-01..MEM-07).

    ---

    *Phase: 10-cursor-polish-14-sc-re-verification*
    *Origin: M1 SC-01 deferred from Phase 5 on 2026-05-08; closes here per VFY-04.*
    ```

    **Why this content is the right shape:**
    - Four sections in the order locked by D-D1: §14 SC Verdicts → Automated Baseline Replay → v2.0 Surfaces (cross-reference) → Milestone Close Decision.
    - Each SC entry has its OWN ceremony log sub-section so the operator's verdict + observation + evidence are recorded next to the precondition + steps + checklist (single document, no scattered evidence).
    - SC #6 records PASS NOW (no operator step needed) per RESEARCH.md SC #6 paragraph; eliminates one ceremony step.
    - Cross-reference table lists Phase 6 / 7 / 8 / 9 VERIFICATION + UAT/summary artifacts per D-C2; verdicts live in those files per D-C3.
    - Milestone close criteria (PASS / PARTIAL / FAIL) is OPERATOR-COMPLETABLE — ship verdict requires operator judgment of how SCs settled.
    - All `___PENDING___` slots are explicit so Task 3 (operator ceremony) knows exactly what to fill.
  </action>

  <verify>
    <automated>powershell -Command "if (Test-Path '.planning/skeleton-verification.md') { Select-String -Path '.planning/skeleton-verification.md' -Pattern '^## Milestone v2.0 Close Decision','^## Automated Baseline Replay','^## §14 Success Criteria Verdicts','^## v2.0 Surfaces Verified' | Measure-Object | ForEach-Object Count } else { 'MISSING' }"</automated>
    Expected output: `4` — all four required section headers present.

    Equivalent bash check (if PowerShell unavailable):
    `test -f .planning/skeleton-verification.md && grep -c '^## ' .planning/skeleton-verification.md` — expected `>= 4`.
  </verify>

  <done>
    - File `.planning/skeleton-verification.md` exists at the repo root of `.planning/`
    - All four D-D1 sections present in correct order (verifiable by `grep -n '^## '`)
    - Six SC table rows with three (SC #1, SC #3, SC #5) referencing the right evidence files; SC #6 verdict pre-filled as PASS with bookkeeping rationale
    - Per-SC ceremony log subsections with preconditions + steps + checklists + verdict slots present for SC #2, SC #4, SC #5
    - SC #1 + SC #3 ceremony log subsections include the locked harness replay commands
    - v2.0 Surfaces cross-reference table cites Phase 6/7/8/9 VERIFICATION + UAT/summary artifacts
    - Milestone close decision section has criteria + ship-verdict slot + open-issues list + v1-horizon progress note
    - All operator-facing pending values are marked `___PENDING___` for unambiguous Task 3 fill-in
  </done>
</task>

<task type="auto">
  <name>Task 2: Run plumbing harness replay → write phase-10 replay JSONs</name>

  <files>
    - .planning/baselines/v2.0/lipsync-phase10-replay.json (CREATE via harness)
    - .planning/baselines/v2.0/idle-phase10-replay.json (CREATE via harness)
    - .planning/skeleton-verification.md (UPDATE in-place - fill SC #1 + SC #3 verdicts and §2 replay table values)
  </files>

  <read_first>
    - sidecar/scripts/plumbing_harness.py (already-built; read once to confirm CLI surface --mode {lipsync,idle} --out PATH)
    - .planning/baselines/v2.0/lipsync.json (Phase 6 baseline — must remain immutable)
    - .planning/baselines/v2.0/idle.json (Phase 6 baseline — must remain immutable)
  </read_first>

  <action>
    **Step 1: Confirm the M1 baselines are present and unchanged before running replay.**

    Read `.planning/baselines/v2.0/lipsync.json` — confirm `pearson_r=0.9747730195034283`, `passed=true`, `threshold=0.7`. Read `.planning/baselines/v2.0/idle.json` — confirm `variance_sum=0.06643749130899018`, `passed=true`, `variance_ceiling=0.5`. If either file has been modified, STOP and investigate; do not proceed until baselines are in their committed state. Per Pitfall 5, these are the immutable historical reference and must not be overwritten by replay.

    **Step 2: Run the lipsync replay against a separate filename per Pitfall 5.**

    From the repo root:
    ```bash
    cd sidecar
    uv run python scripts/plumbing_harness.py --mode lipsync --out ../.planning/baselines/v2.0/lipsync-phase10-replay.json
    ```

    Expected:
    - Exit code 0 (PASS — `pearson_r >= 0.7`)
    - File `.planning/baselines/v2.0/lipsync-phase10-replay.json` created with shape `{"mode": "lipsync", "passed": true, "pearson_r": <float>, "sample_count": 90, "threshold": 0.7}`
    - The replay value should be identical to the M1 baseline (`pearson_r=0.9747730195034283`) because the harness uses synthetic inputs with seed determinism. Any divergence means SpeechDriver behavior changed since 06-02 — INVESTIGATE before proceeding (could indicate an unintended regression).

    **Step 3: Run the idle replay against a separate filename.**

    From the repo root:
    ```bash
    cd sidecar
    uv run python scripts/plumbing_harness.py --mode idle --out ../.planning/baselines/v2.0/idle-phase10-replay.json
    ```

    Expected:
    - Exit code 0 (PASS — `0 < variance_sum < 0.5`)
    - File `.planning/baselines/v2.0/idle-phase10-replay.json` created with shape `{"duration_seconds": 30.0, "mode": "idle", "passed": true, "variance_ceiling": 0.5, "variance_sum": <float>}`
    - Replay value should match `variance_sum=0.06643749130899018` exactly (IdleDriver(seed=42) is deterministic).

    **Step 4: Read both replay JSON files and update `.planning/skeleton-verification.md` §"Automated Baseline Replay" table with concrete values.**

    Specifically:
    - Replace `pearson_r=___PENDING___` in the SC #1 ceremony log AND in the §2 replay table with the actual replay value.
    - Replace `variance_sum=___PENDING___` in the SC #3 ceremony log AND in the §2 replay table with the actual replay value.
    - Set `Pass?` cell to `yes` (both modes are expected to PASS).
    - Set "Harness re-run on ___PENDING___" date placeholder to today's date in YYYY-MM-DD form.
    - For the §1 verdict table SC #1 + SC #3 rows: change `Verdict` from `___PENDING___` to `PASS` (the harness's `passed: true` field is the gate per RESEARCH.md `### Tolerance band check`); write the observation as e.g., `pearson_r=0.9747730195034283 (1.39× over 0.7 threshold)`.

    Leave SC #2, SC #4, SC #5 verdict slots as `___PENDING___` — those are operator-judged and filled in Task 3.

    **Step 5: Confirm M1 baselines were not overwritten (Pitfall 5 final check).**

    Re-read `.planning/baselines/v2.0/lipsync.json` and `.planning/baselines/v2.0/idle.json`. They should be byte-identical to their pre-Task-2 content. If `git status` shows them modified, STOP — the wrong `--out` path was used. Restore from `git checkout` and re-run with the `*-phase10-replay.json` filenames.
  </action>

  <verify>
    <automated>powershell -Command "$lp = Test-Path '.planning/baselines/v2.0/lipsync-phase10-replay.json'; $ip = Test-Path '.planning/baselines/v2.0/idle-phase10-replay.json'; if ($lp -and $ip) { $l = Get-Content '.planning/baselines/v2.0/lipsync-phase10-replay.json' | ConvertFrom-Json; $i = Get-Content '.planning/baselines/v2.0/idle-phase10-replay.json' | ConvertFrom-Json; if ($l.passed -and $i.passed) { 'BOTH-PASS' } else { 'HARNESS-FAIL' } } else { 'MISSING-FILES' }"</automated>
    Expected output: `BOTH-PASS`

    Bash equivalent: `test -f .planning/baselines/v2.0/lipsync-phase10-replay.json && test -f .planning/baselines/v2.0/idle-phase10-replay.json && python -c "import json; l=json.load(open('.planning/baselines/v2.0/lipsync-phase10-replay.json')); i=json.load(open('.planning/baselines/v2.0/idle-phase10-replay.json')); print('BOTH-PASS' if l['passed'] and i['passed'] else 'HARNESS-FAIL')"` — expected `BOTH-PASS`.

    Also verify M1 baselines unchanged: `git diff --exit-code .planning/baselines/v2.0/lipsync.json .planning/baselines/v2.0/idle.json` — expected exit 0 (no changes).
  </verify>

  <done>
    - Files `.planning/baselines/v2.0/lipsync-phase10-replay.json` and `.planning/baselines/v2.0/idle-phase10-replay.json` exist with `passed: true`
    - Replay values match Phase 6 baselines (deterministic harness)
    - `.planning/skeleton-verification.md` §"Automated Baseline Replay" table has concrete pearson_r + variance_sum values + replay date
    - SC #1 + SC #3 verdicts in §1 table are `PASS` with observations
    - M1 baseline files (`lipsync.json`, `idle.json`) are byte-identical to pre-task state (Pitfall 5 verified)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator §14 SC re-verification ceremony — fill skeleton-verification.md verdicts</name>

  <files>
    - .planning/skeleton-verification.md (UPDATE in-place - operator fills SC #2/4/5 verdicts + milestone close decision)
  </files>

  <read_first>
    - .planning/skeleton-verification.md (the deliverable from Tasks 1+2; ceremony scripts + harness verdicts already in place)
    - .planning/phases/10-cursor-polish-14-sc-re-verification/10-01-SUMMARY.md (Plan 10-01 cursor-fix evidence)
    - plugins/default/plugin.yaml (confirm [smirk] is in active vocabulary)
    - avatars/重音テト/_avatar_overrides.yaml (confirm body_sway_strategy: head_only)
  </read_first>

  <what-built>
    Plan 10-01 has fixed the cursor namespace bug, dropped the in-VTS-window gate, and added the synthetic-canvas fallback. Plan 10-02 Task 1 has authored `.planning/skeleton-verification.md` with the four-section structure and operator ceremony scripts for SC #2 (`[smirk]` smooth blend), SC #4 (body sway through long utterance), and SC #5 (cursor tracking). Plan 10-02 Task 2 has run the plumbing harness replay and filled SC #1 + SC #3 verdicts as PASS. SC #6 is pre-filled as PASS by bookkeeping.

    Three SCs remain operator-judged: SC #2, SC #4, SC #5. The ceremony scripts in `.planning/skeleton-verification.md` provide preconditions, steps, visual checklists, and verdict rubric for each. After all three are verdict-recorded, the operator records the v2.0 milestone close decision (PASS / PARTIAL / FAIL) and decision basis.
  </what-built>

  <action>
    **Operator runs each ceremony block from `.planning/skeleton-verification.md` in order. Each block is self-contained with its preconditions and steps.**

    **Pre-flight (operator confirms before any ceremony):**
    1. App is running (Electron shell + Python sidecar both alive; logs drawer shows sidecar `[READY]`).
    2. VTube Studio is running with the Teto rig (重音テト) loaded; sidecar plugin is authenticated against VTS.
    3. LM Studio (or active LLM provider) is running and `/admin/llm-test` returns 200.
    4. Optional Phase 9 carry-forward smoke: open the HUD, confirm the Teto slider surface is the focused `hud_visible_param_ids` set (18 rows in the approved Phase 9 live run), confirm `/hud/ws` is live, lock and clear `FaceAngleX`, then close the separate HUD window.
    5. Separate HUD window is closed, with no active locks left behind if it was opened for setup smoke (not necessary for SC #5; mandatory for SC #2 and SC #4 per "Lock means lock" carryover).
    6. Phase 10 Plan 10-01 has been merged — confirm `git log --oneline | head -5` shows the cursor-fix commit.

    **Step 1 — SC #2 ceremony (`[smirk]` smooth blend; ~2 minutes):**
    - Open the app to the chat panel.
    - Paste the prompt from `.planning/skeleton-verification.md` §"SC #2 Ceremony Log":
      `Reply with exactly one sentence about a cat sneaking into a bakery, and emit [smirk] somewhere in your reply.`
    - Send. Watch the avatar's face during the reply.
    - Open logs drawer. Confirm a `[DISPATCH]` line for `ActionCode(code='smirk')` appears within the reply window. If absent, the LLM did not honor the prompt — retry or switch model. Record this as part of the observation.
    - Score the visual checklist (1 of 3 / 2 of 3 / 3 of 3) per D-B3.
    - Write the verdict (PASS / PARTIAL / FAIL per D-B5) into `Recorded verdict:` slot.
    - Write a 1-2 sentence observation into `Recorded observation:` slot (what you actually saw — fade smoothness, decay quality).
    - Optionally capture a screen-recording clip; record the path or chat-log timestamp range in `Evidence link:` slot.

    **Step 2 — SC #4 ceremony (body sway through ~30-45s utterance; ~3 minutes):**
    - Same panel state as SC #2.
    - Paste the prompt from `.planning/skeleton-verification.md` §"SC #4 Ceremony Log":
      `Tell me a 60-word funny story about a cat who somehow ends up in space. Take your time and include vivid details.`
    - Send. Watch the avatar's body/head sway through the entire reply (~30-45s of TTS audio).
    - After reply finishes, open logs drawer. Confirm `[SPEECH-DRIVER]` and `[BODY-SWAY]` log lines appear during the utterance window.
    - Score the visual checklist (1 of 3 / 2 of 3 / 3 of 3) per D-B4.
    - Write verdict, observation, and evidence link as in SC #2.

    **Step 3 — SC #5 ceremony (cursor tracking, post-fix; ~2 minutes):**
    - With cursor over the VTube Studio window, move cursor in a slow circle around the avatar's face. Observe head + eye tracking.
    - Move cursor outside the VTS window (over the chat panel or another visible app). Observe head + eyes continuing to track (synthetic-canvas fallback per VFY-02).
    - Stop moving cursor at an off-axis position. Observe the avatar holds the deflected pose without jitter.
    - (Optional) Close VTube Studio, observe sidecar logs do not error. Re-open VTS.
    - Score the visual checklist (1 of 3 / 2 of 3 / 3 of 3).
    - Write verdict, observation, and evidence link.

    **Step 4 — Update §1 verdict table summary rows:**
    - Replace `___PENDING___` in the SC #2, SC #4, SC #5 rows of the top verdict table with the recorded verdict.
    - Replace `___PENDING___` observations with brief one-line summaries (e.g., "3-of-3 checks observed; smooth fade-in and decay clearly visible").

    **Step 5 — Milestone close decision:**
    - Apply the criteria from §"Milestone v2.0 Close Decision":
      - PASS if all six SCs are PASS, OR PARTIAL with documented future-direction rationale.
      - PARTIAL if any SC is PARTIAL with no rationale, OR exactly one SC is FAIL with documented rationale.
      - FAIL otherwise.
    - Note: SC #4 PARTIAL is anticipated (head_only ship state per Phase 6 discuss-phase decision is acknowledged as mediocre); the open-issues list already documents the future-direction rationale (body-sway physics-chain investigation), so SC #4 PARTIAL is rationale-backed and does not block PASS.
    - Write the ship verdict into `Recorded verdict:` slot.
    - Write a 1-3 sentence decision basis into `Decision basis:` slot.

    **Step 6 — Top-of-file metadata:**
    - Replace `**Verified:** ___PENDING___` with today's date (YYYY-MM-DD).
    - Replace `**Verifier:** ___PENDING___` with operator name or handle.

    **Step 7 — Sanity sweep:**
    - Run `grep -n "___PENDING___" .planning/skeleton-verification.md` (or PowerShell equivalent: `Select-String -Path '.planning/skeleton-verification.md' -Pattern '___PENDING___'`).
    - Expected: zero matches. If matches remain, fill them or explicitly mark "N/A — not applicable" with a one-line reason.

    **Step 8 — Commit:**
    - Stage `.planning/skeleton-verification.md` and the two replay JSON files.
    - Commit message: `docs(10): record §14 SC ceremony verdicts + milestone v2.0 close decision`

    **If a ceremony cannot run** (e.g., VTS hangs, LLM doesn't emit `[smirk]`, cursor still doesn't track post-fix):
    - Record the ceremony as FAIL or PARTIAL with the specific failure mode in `Recorded observation:`.
    - For SC #5 specifically, per D-A6: a FAIL verdict here records the diagnosis from Plan 10-01 SUMMARY plus the observed-broken-behavior — that itself is a valid v2.0 close artifact.
    - Recompute the milestone close decision based on the actual verdicts.
  </action>

  <verify>
    <automated>powershell -Command "$count = (Select-String -Path '.planning/skeleton-verification.md' -Pattern '___PENDING___' | Measure-Object).Count; if ($count -eq 0) { 'CEREMONY-COMPLETE' } else { \"PENDING-MARKERS-REMAIN: $count\" }"</automated>
    Expected output: CEREMONY-COMPLETE — zero ___PENDING___ markers remain in skeleton-verification.md.

    Bash equivalent: `grep -c "___PENDING___" .planning/skeleton-verification.md` — expected: 0; emit "CEREMONY-COMPLETE" if 0.

    Plus: `git status` should show `.planning/skeleton-verification.md` and `.planning/baselines/v2.0/*-phase10-replay.json` as modified or staged.
  </verify>

  <done>
    - All `___PENDING___` markers in `.planning/skeleton-verification.md` replaced with concrete verdicts / observations / evidence links (or explicit "FAIL with rationale")
    - Top-of-file `**Verified:**` and `**Verifier:**` placeholders replaced with date + operator name
    - `.planning/skeleton-verification.md` + replay JSONs (`*-phase10-replay.json`) committed via `git commit`
    - SC #2 / SC #4 / SC #5 verdicts each backed by ceremony observation paragraph + 3-check tally per D-B5 rubric
    - Milestone v2.0 close decision section completed with ship verdict + open issues + v1-horizon progress note
  </done>

  <resume-signal>
    Type "approved" once `.planning/skeleton-verification.md` has zero `___PENDING___` markers and is committed. If issues encountered (LLM doesn't emit `[smirk]`, cursor still broken, harness replay regressed), describe them and we'll diagnose before resuming. The ceremony cannot be skipped — VFY-04 requires this commit as the milestone close artifact.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase-level gate for Plan 10-02:**

1. **VFY-03 satisfied** (all six §14 SCs verified):
   - SC #1 — PASS via Task 2 harness replay (`lipsync-phase10-replay.json passed: true`)
   - SC #2 — Operator-judged via Task 3 ceremony (recorded in skeleton-verification.md §"SC #2 Ceremony Log")
   - SC #3 — PASS via Task 2 harness replay (`idle-phase10-replay.json passed: true`)
   - SC #4 — Operator-judged via Task 3 ceremony
   - SC #5 — Operator-judged via Task 3 ceremony based on Plan 10-01 cursor-fix outcome
   - SC #6 — PASS by bookkeeping (recorded in Task 1)

2. **VFY-04 satisfied** (skeleton-verification.md committed):
   - File `.planning/skeleton-verification.md` exists at the `.planning/` root
   - Contains all four D-D1 sections in order
   - All `___PENDING___` markers replaced with concrete values or explicit N/A reasons
   - Committed via Task 3 commit step

3. **VFY-05 satisfied** (side-by-side harness replay):
   - `lipsync-phase10-replay.json` + `idle-phase10-replay.json` committed
   - Both `passed: true`
   - M1 baselines (`lipsync.json`, `idle.json`) unchanged from pre-Phase-10 state (Pitfall 5)

4. **ARCH-06 carry-through preserved** — Plan 10-01 already verified; Plan 10-02 makes no source code changes.
</verification>

<success_criteria>
- [ ] `.planning/skeleton-verification.md` exists with all four D-D1 sections
- [ ] All six §14 SC verdicts are recorded in the §1 table (no `___PENDING___` remaining)
- [ ] SC #2 ceremony log has all three checklist items marked + verdict + observation + evidence
- [ ] SC #4 ceremony log has all three checklist items marked + verdict + observation + evidence
- [ ] SC #5 ceremony log has all three checklist items marked + verdict + observation + evidence
- [ ] Automated Baseline Replay table has concrete pearson_r and variance_sum values from Phase 10 replay
- [ ] v2.0 Surfaces cross-reference table cites Phase 6/7/8/9 verification artifacts (already filled in Task 1)
- [ ] Milestone v2.0 Close Decision: ship verdict + decision basis recorded
- [ ] Open issues list and v1-horizon progress note are present (already filled in Task 1)
- [ ] `lipsync-phase10-replay.json` and `idle-phase10-replay.json` files exist with `passed: true`
- [ ] M1 baselines (`lipsync.json`, `idle.json`) are unchanged
- [ ] Documents committed via Task 3 commit step
</success_criteria>

<output>
After completion, create `.planning/phases/10-cursor-polish-14-sc-re-verification/10-02-SUMMARY.md` documenting:
- The skeleton-verification.md commit (the deferred M1 SC-01 deliverable, now realized)
- Each SC's verdict + brief observation
- The harness replay values + delta from M1 baselines (expected: zero delta — deterministic)
- Milestone v2.0 ship verdict
- Hand-off to next milestone (memory subsystem per REQUIREMENTS MEM-01..MEM-07)
</output>
